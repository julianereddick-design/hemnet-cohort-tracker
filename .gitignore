node_modules/
.env
.spike_*.html
.spike_*.json
.claude/
view-data/
cohort-*-views.csv
views-wide-*.csv

# Sold-match spike: cache + large data artifacts (keep code + reports)
verf-soldspike/cache/
verf-soldspike/recon/*.html
verf-soldspike/**/*.html
verf-soldspike/**/photos/

# Sold-match spike: any-workspace cache + html artifacts
verf-soldspike*/cache/
verf-soldspike*/**/*.html
verf-soldspike*/recon/*.html
verf-soldspike*/**/photos/

# Phase 28: large source workbook (Julian ARPL model) — do not commit
data/*.xlsx
.superpowers/

# The ad-cost crawler's Python venv on the droplet. Its CONTENTS are ignored;
# the pins that rebuild it are tracked, at scripts/requirements-adcost.txt.
.venv-adcost/
