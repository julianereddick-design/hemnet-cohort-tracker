---
phase: 09-production-cutover-self-hosted-scraper-launch
plan: 2.5
type: execute
wave: 2.5
depends_on: ["09-02"]
files_modified:
  - migrate-booli-listing-add-fields.js
  - lib/booli-fetch.js
  - lib/booli-to-hemnet-mapping.js
  - booli-targeted-discovery.js
  - booli-targeted-refresh.js
  - hemnet-targeted-match.js
autonomous: false
requirements: []
assumptions:
  - "TASKS 1-6 ALREADY SHIPPED in commit 618c896 (2026-05-15). Schema migration ran live, lib/booli-fetch.js parser extended, lib/booli-to-hemnet-mapping.js created, Job C INSERT/UPDATE extended, Job D UPDATE+defensive-INSERT extended. Smoke tests for lib/booli-fetch.js (18 pass), Job C (20 pass), Job D (10 pass), mapping (14 pass). End-to-end verified on 3 random Booli URLs — all 5 new fields populated. This plan documents that work as Tasks 1-6 for the GSD audit trail."
  - "TASK 7 (Job B rewrite) is the remaining implementation. Hard deadline: Mon 2026-05-18 03:00 UTC for Job B's cron slot. If Job B rewrite doesn't ship by then, W21 cohort_pairs is built with the OLD location-paginated Job B and the ~26.7% match rate persists. Acceptable to defer one week — green-week observation re-runs on W22."
  - "Field capture write semantics are COALESCE-preserve (D-24): EXCLUDED.x is OR-ed against the existing column value so transient null parses don't clobber valid data. This is the explicit design choice; the alternative (let null wins) was rejected because Plan 09-01's diagnostic regressed times_viewed values that way."
  - "agent_id semantics diverge from Django's historical values (D-22). Our agent_id = Booli's Source.id (broker chain id, 1-9 digits depending on broker). Django captured a different field. Metabase reports keyed on Django agent_id may need rebuilding — operator action item, NOT in plan scope."
  - "Job B rewrite uses ±5% price tolerance, exact rooms match, and the BOOLI_TO_HEMNET_ITEM_TYPE 8-entry mapping with fallback drop-filter strategy when source fields are null (D-29). Tolerance values are starting hypotheses — first wet-run informs whether they need adjustment."
  - "Diagnostic bucket-level breakdown of cohort_unmatched is DEFERRED. NOT EXISTS scans against hemnet_listingv2's LOWER(TRIM(street_address)) time out without a functional index. Operator already committed to Job B as the primary fix regardless of bucket distribution."
  - "Plan 09-02's Task 5 wet-run gate (D-19) was SKIPPED at operator direction. Job D + Job A worker-pool conc-8 sizing is unexercised at scale; first real test is Tue 2026-05-19 14:00 UTC cron run."
must_haves:
  truths:
    - "booli_listing schema has 3 new columns (rooms numeric, object_type varchar, living_area numeric) added via migrate-booli-listing-add-fields.js per D-23"
    - "lib/booli-fetch.js parseBooliListing extracts price/rooms/livingArea/objectType/agentId from Apollo state with null-safe coercion per D-21"
    - "lib/booli-to-hemnet-mapping.js BOOLI_TO_HEMNET_ITEM_TYPE maps Booli's 8 Swedish objectType values to Hemnet's 6 valid URL tokens per D-25"
    - "Job C (booli-targeted-discovery.js) INSERT writes all 5 new fields; ON CONFLICT UPDATE uses COALESCE-preserve per D-24"
    - "Job D (booli-targeted-refresh.js) UPDATE refreshes all 5 new fields using COALESCE-preserve per D-24; defensive INSERT also populates them"
    - "Job B (hemnet-targeted-match.js) per-Booli-row search switches from location-paginated to discriminator-filtered: price band + exact rooms + item_types[] + location_ids[] per D-26..D-29"
    - "Job B falls back gracefully when source fields are null: drop item_types[] first, then price, then rooms; never drop location_ids per D-29"
    - "Mon 2026-05-18 06:00 UTC cohort-create run uses W21 cohort_pairs built by hardened Job B against new Booli fields"
  artifacts:
    - path: "migrate-booli-listing-add-fields.js"
      provides: "Idempotent ALTER TABLE for booli_listing adding rooms/object_type/living_area columns"
      contains: "ADD COLUMN IF NOT EXISTS"
    - path: "lib/booli-fetch.js"
      provides: "Extended parseBooliListing extracting Apollo paths for the 5 new fields"
      contains: "Listing.listPrice.raw"
      min_lines: 0
    - path: "lib/booli-to-hemnet-mapping.js"
      provides: "BOOLI_TO_HEMNET_ITEM_TYPE 8-entry mapping + booliObjectTypeToHemnet helper"
      contains: "BOOLI_TO_HEMNET_ITEM_TYPE"
    - path: "booli-targeted-discovery.js"
      provides: "Job C INSERT/ON CONFLICT UPDATE populating new 5 fields with COALESCE-preserve"
      contains: "object_type"
    - path: "booli-targeted-refresh.js"
      provides: "Job D UPDATE refreshing new 5 fields with COALESCE-preserve; defensive INSERT also populates"
      contains: "COALESCE($3, price)"
    - path: "hemnet-targeted-match.js"
      provides: "Job B targeted-search rewrite using price/rooms/item_types/location URL filters per D-26..D-29"
      contains: "price_min"
  key_links:
    - from: "Job C upsertBooliRow()"
      to: "booli_listing INSERT INTO ... (price, rooms, living_area, object_type, agent_id) VALUES ($10, $11, $12, $13, $14)"
      via: "parseBooliListing output piped through 5 new shape fields"
      pattern: "INSERT INTO booli_listing.*price.*agent_id"
    - from: "Job D worker SET clause"
      to: "UPDATE booli_listing SET ... price = COALESCE($3, price)"
      via: "Per-iteration shapeBooliForUpdate emits the 5 new fields; UPDATE applies COALESCE"
      pattern: "COALESCE.*price"
    - from: "Job B per-Booli-row search URL builder"
      to: "Hemnet's /bostader endpoint with price_min/max + rooms_min/max + item_types[] + location_ids[]"
      via: "booliObjectTypeToHemnet for item_types; ±5% price band; rooms_min=rooms_max=N exact match"
      pattern: "price_min.*rooms_min.*item_types"
---

<objective>
Take over Booli-side enrichment fields that the Django scraper was supplying (until its 2026-05-15 decommission), AND rewrite Job B's Hemnet seeding to use those new fields as targeted-search filters — closing the 26.7% aggregate cohort match rate gap before Monday's cohort-create run.

Tasks 1-6 of this plan have ALREADY SHIPPED in commit `618c896` (2026-05-15). They cover the field-capture half: schema migration, parser extension, Job C/Job D INSERT/UPDATE wiring, mapping constant. The remaining task (Task 7) is the Job B rewrite — the half of the plan that actually USES the new fields to improve match rate.

Purpose: Close the matching gap surfaced during Plan 09-02 execution. The 26.7% aggregate match rate over the last 8 weeks (5484 matched / 15036 unmatched) is far worse than the 42.4% VERF-05 snapshot the ROADMAP carries as a known issue. Operator hypothesis (confirmed in session) is that Job B is the dominant bottleneck — its 15-page location-paginated search drops listings that Hemnet has but aren't in the newest-15-pages window or don't match the strict cardMatches predicate. Switching Job B to a discriminator-filtered search (price + rooms + object_type + location) should narrow candidate sets from ~750/Booli-row to ~3-10/Booli-row, dramatically improving hit rate while preserving Oxylabs cost neutrality.

Output: A Job B that ships before Mon 2026-05-18 03:00 UTC (Job B's cron slot) so W21's cohort_pairs is built with the new targeted-search logic; W21 cohort-create at Mon 06:00 UTC produces a meaningfully higher match rate than the historical 26.7%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-production-cutover-self-hosted-scraper-launch/09-CONTEXT.md
@.planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-CONTEXT.md
@.planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-DISCUSSION-LOG.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@lib/booli-fetch.js
@lib/booli-to-hemnet-mapping.js
@lib/hemnet-fetch.js
@lib/scrape-http.js
@lib/hemnet-locations.js
@hemnet-targeted-match.js
@booli-targeted-discovery.js
@booli-targeted-refresh.js
@cron-wrapper.js

<interfaces>
<!-- Contracts the Job B rewrite consumes. -->

From lib/booli-to-hemnet-mapping.js:
```js
const { booliObjectTypeToHemnet, BOOLI_TO_HEMNET_ITEM_TYPE } = require('./lib/booli-to-hemnet-mapping');
// booliObjectTypeToHemnet('Villa') → 'villa'
// booliObjectTypeToHemnet('Lägenhet') → 'bostadsratt'
// booliObjectTypeToHemnet('Slott') → null  (unknown, callers OMIT item_types filter)
```

From booli_listing (post-migration):
```
booli_id BIGINT, url VARCHAR, listed DATE, title VARCHAR, postcode INT,
price INT NULL, rooms NUMERIC(3,1) NULL, living_area NUMERIC(6,1) NULL,
object_type VARCHAR(40) NULL, agent_id BIGINT NULL, ...
```
Note: price/rooms/object_type may be null for legacy rows (pre-2026-05-15) and
for any row where Booli's Apollo state was malformed at fetch time. Job B MUST
handle null fields with fallback per D-29.

From lib/hemnet-fetch.js (existing — may need extension):
```js
async function fetchSearch(locationId, opts) → { cards, totalCount? }
```
Takes a single locationId; constructs `?location_ids=X&page=N`. Does NOT support
price/rooms/item_types filters today. Job B rewrite needs either:
(a) extend fetchSearch's signature to take a filter object, OR
(b) introduce a new fetchSearchFiltered(filters, opts) sibling, OR
(c) construct the URL directly in Job B and use getWithRetry from lib/scrape-http.

Option (b) is recommended — keeps fetchSearch backwards-compatible for any non-Job-B callers.

From lib/hemnet-locations.js:
```js
function getLocationId(municipalityName) → number  (the Hemnet location_id for that muni)
```
Already used by Job B today. Continues to work — the URL just gets more query params.

From lib/scrape-http.js:
```js
async function getWithRetry(url, opts) → { status, html }
```
Universal HTTP/Oxylabs-fallback path. Already works for Hemnet (probe-oxylabs-hemnet.js 12/12 PASS) and Booli. No change.
</interfaces>

<canonical_refs>
- `lib/booli-fetch.js:69-150` — parseBooliListing (Tasks 1-6, shipped). Extracts the 5 fields.
- `lib/booli-to-hemnet-mapping.js` — BOOLI_TO_HEMNET_ITEM_TYPE constant (Task 6, shipped).
- `lib/hemnet-fetch.js:172-178` — fetchSearch (current signature; Task 7 may extend).
- `lib/hemnet-locations.js` — getLocationId (unchanged).
- `hemnet-targeted-match.js` — Job B. Target of Task 7 rewrite.
- `hemnet-targeted-match.js:156-161` — cardMatches predicate. Continues to run as confirmation step on the narrower candidate set.
- `migrate-booli-listing-add-fields.js` — migration (Task 1, shipped, ran live).
- `booli-targeted-discovery.js:299-340` — Job C upsertBooliRow (Tasks 4, shipped).
- `booli-targeted-refresh.js:84-105` + UPDATE at `:135-159` — Job D refresh (Task 5, shipped).
- `cron-wrapper.js:57-141` — runJob contract (unchanged).
</canonical_refs>
</context>

<tasks>

<task id="1" type="shipped">
<name>Schema migration — add 3 booli_listing columns</name>
<wave>1</wave>
<status>completed in commit 618c896</status>

<action>
ALTER TABLE booli_listing ADD COLUMN IF NOT EXISTS:
  - rooms        NUMERIC(3,1) NULL
  - object_type  VARCHAR(40)  NULL
  - living_area  NUMERIC(6,1) NULL

Existing price (integer) and agent_id (bigint) columns are unchanged. All new columns NULL-able. No backfill. Idempotent.

Implementation: migrate-booli-listing-add-fields.js. Ran live 2026-05-15 (commit 618c896).
</action>

<read_first>
- .planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-CONTEXT.md (D-23)
- repair-migrate-schema.js (template/convention)
</read_first>

<acceptance_criteria>
- migrate-booli-listing-add-fields.js exists in repo root
- node migrate-booli-listing-add-fields.js exits 0 (verified live 2026-05-15)
- information_schema.columns for booli_listing returns 23 columns including rooms/object_type/living_area
- Schema verification block at end of migration script prints all 5 fields as nullable=YES
</acceptance_criteria>
</task>

<task id="2" type="shipped">
<name>Extend parseBooliListing parser</name>
<wave>1</wave>
<status>completed in commit 618c896</status>

<action>
Extend lib/booli-fetch.js parseBooliListing to extract from Apollo state:
  - price (Listing.listPrice.raw → int or null)
  - rooms (Listing.rooms.raw → numeric or null)
  - livingArea (Listing.livingArea.raw → numeric or null)
  - objectType (Listing.objectType → string or null)
  - agentId (apolloState[Listing.source.__ref].id parseInt → bigint or null)

All fields coerce to null on path miss. Source.__ref lookup uses optional-chain semantics.

Add 3 new smoke tests:
  - Full-parse with all FormattedValue objects populated
  - Missing FormattedValue fields → null (not crash)
  - Source __ref points to missing Source object → agentId null
</action>

<read_first>
- lib/booli-fetch.js (whole file)
- .planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-CONTEXT.md (D-21, D-22)
</read_first>

<acceptance_criteria>
- lib/booli-fetch.js contains 'Listing.listPrice.raw' (in comment or code)
- lib/booli-fetch.js parseBooliListing return object includes keys: price, rooms, livingArea, objectType, agentId
- node lib/booli-fetch.js --smoke reports >= 18 pass, 0 fail
- End-to-end live fetch via fetchBooliDetail of an active Booli URL returns populated values for all 5 fields
</acceptance_criteria>
</task>

<task id="3" type="shipped">
<name>Create BOOLI_TO_HEMNET_ITEM_TYPE mapping module</name>
<wave>1</wave>
<status>completed in commit 618c896</status>

<action>
Create lib/booli-to-hemnet-mapping.js exporting:
  - BOOLI_TO_HEMNET_ITEM_TYPE: frozen 8-entry object mapping Booli Swedish strings to Hemnet URL tokens
  - booliObjectTypeToHemnet(s): helper returning the mapped token or null on unknown

8 entries (verified via live Hemnet probe):
  Lägenhet → bostadsratt | Villa → villa | Radhus → radhus
  Kedjehus → villa (Hemnet bundles into HOUSES) | Parhus → villa (bundled)
  Fritidshus → fritidshus | Tomt/Mark → tomt | Gård → gard

Include --smoke block testing all 8 mappings + null/unknown handling + immutability.
</action>

<read_first>
- .planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-CONTEXT.md (D-25)
- .planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-DISCUSSION-LOG.md (Thread 7)
</read_first>

<acceptance_criteria>
- lib/booli-to-hemnet-mapping.js exists
- module.exports.BOOLI_TO_HEMNET_ITEM_TYPE has exactly 8 keys
- booliObjectTypeToHemnet('Lägenhet') === 'bostadsratt'
- booliObjectTypeToHemnet('Kedjehus') === 'villa'
- booliObjectTypeToHemnet('Unknown') === null
- BOOLI_TO_HEMNET_ITEM_TYPE is Object.freeze'd (throws on mutation)
- node lib/booli-to-hemnet-mapping.js --smoke reports >= 14 pass, 0 fail
</acceptance_criteria>
</task>

<task id="4" type="shipped">
<name>Job C INSERT + ON CONFLICT UPDATE extension</name>
<wave>1</wave>
<status>completed in commit 618c896</status>

<action>
booli-targeted-discovery.js upsertBooliRow:
  - INSERT column list adds: price, rooms, living_area, object_type, agent_id
  - VALUES adds $10, $11, $12, $13, $14 for those fields
  - ON CONFLICT (url) DO UPDATE SET clause uses COALESCE(EXCLUDED.x, booli_listing.x) for each new field to preserve existing values when re-parse produces null
  - Function signature unchanged (takes a parsed listing object) — just plumbs new fields through

Coerce input null appropriately at the JS layer: l.price != null ? l.price : null (same pattern for all 5).
</action>

<read_first>
- booli-targeted-discovery.js (whole file)
- .planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-CONTEXT.md (D-20, D-24)
</read_first>

<acceptance_criteria>
- booli-targeted-discovery.js INSERT INTO booli_listing column list includes: price, rooms, living_area, object_type, agent_id
- ON CONFLICT clause contains 'COALESCE(EXCLUDED.price'
- node --check booli-targeted-discovery.js → OK
- node booli-targeted-discovery.js --smoke reports >= 20 pass, 0 fail
</acceptance_criteria>
</task>

<task id="5" type="shipped">
<name>Job D UPDATE + defensive INSERT extension</name>
<wave>1</wave>
<status>completed in commit 618c896</status>

<action>
booli-targeted-refresh.js:
  - shapeBooliForUpdate: emits 5 new fields (price, rooms, livingArea, objectType, agentId) with null-safe coercion
  - UPDATE booli_listing SET clause: adds COALESCE($N, column) for each of the 5 fields
  - Defensive INSERT (the UPDATE-found-zero-rows path): column list adds 5 new fields, VALUES adds 5 new $N positionals
  - Add 2 new smoke tests: full-population + missing-fields-to-null

Backfill expectation: post-deploy, Job D's first cron cycle (Tue 2026-05-19 14:00 UTC) will refresh the 610 broken-Django rows from 2026-05-15 because COALESCE($x, column) overwrites column when $x is non-null and existing column is null.
</action>

<read_first>
- booli-targeted-refresh.js (whole file)
- .planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-CONTEXT.md (D-24)
</read_first>

<acceptance_criteria>
- booli-targeted-refresh.js UPDATE clause contains 'COALESCE($3, price)' or equivalent for all 5 fields
- Defensive INSERT column list includes all 5 new fields
- node --check booli-targeted-refresh.js → OK
- node booli-targeted-refresh.js --smoke reports >= 10 pass, 0 fail
</acceptance_criteria>
</task>

<task id="6" type="shipped">
<name>End-to-end live verification of Tasks 1-5</name>
<wave>1</wave>
<status>completed in commit 618c896</status>

<action>
Verify the field-capture path end-to-end by fetching 3 random active Booli URLs through the updated parser and confirming all 5 new fields populate.

Command (from session log):
  node -e "..."  hitting fetchBooliDetail against 3 random booli_listing rows, printing the populated object.

Expected output: all 3 results show price/rooms/livingArea/objectType/agentId populated (or null for known-broken legacy rows).
</action>

<read_first>
- .planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-DISCUSSION-LOG.md (Thread 8)
</read_first>

<acceptance_criteria>
- 3 random Booli URLs return parsed objects with objectType: 'Villa'|'Lägenhet'|etc (Swedish string)
- price values are integers (or null)
- rooms values are numeric (or null)
- agentId values are integers (or null)
- Verified 2026-05-15 (session log): all 3 sample URLs populated cleanly
</acceptance_criteria>
</task>

<task id="7" type="pending">
<name>Job B targeted-search rewrite</name>
<wave>2</wave>
<status>NOT STARTED — implementation pending</status>

<action>
Rewrite hemnet-targeted-match.js (Job B) to use Booli's price/rooms/object_type fields as URL filters when searching Hemnet, instead of paginating location-only results.

Implementation steps:

1. **Read booli_listing's new fields per row.** Job B's main loop already iterates Booli rows for the cohort week. Extend the SELECT to include price, rooms, object_type alongside existing fields.

2. **Build the narrowed Hemnet search URL per Booli row.** Use a helper like:
   ```js
   function buildHemnetSearchUrl(booli, locationId) {
     const params = new URLSearchParams();
     params.append('location_ids[]', String(locationId));
     // Drop-filter fallback (D-29) — only add filter if source field is populated:
     if (booli.price != null) {
       params.append('price_min', String(Math.floor(booli.price * 0.95)));
       params.append('price_max', String(Math.ceil(booli.price * 1.05)));
     }
     if (booli.rooms != null) {
       const r = Math.round(Number(booli.rooms)); // integer floor for Hemnet
       params.append('rooms_min', String(r));
       params.append('rooms_max', String(r));
     }
     const item_type = booliObjectTypeToHemnet(booli.object_type);
     if (item_type) params.append('item_types[]', item_type);
     return `https://www.hemnet.se/bostader?${params.toString()}`;
   }
   ```

3. **Add `fetchSearchFiltered(url, opts)` to lib/hemnet-fetch.js** OR call `getWithRetry(url, opts)` directly and reuse the existing search-result Apollo parser. Recommended: new sibling function in lib/hemnet-fetch.js to keep Job B clean.

4. **Replace Job B's per-Booli-row fetchSearch call.** The current code at hemnet-targeted-match.js (around where fetchSearch is invoked) walks `?areaIds={muni}&page=N` for MAX_PAGES=15. Replace with a single call to the narrowed URL — typically returns 3-10 cards in result.

5. **Keep the cardMatches predicate** (hemnet-targeted-match.js:156-161) as confirmation. The narrowed search may still return 5-10 candidates; cardMatches filters to the exact street + ±7 days.

6. **For each confirmed match, fetch the Hemnet detail page** (already in Job B today) and UPSERT into hemnet_listingv2 (same INSERT path at :485+).

7. **Update Job B's --smoke block** with a fixture that exercises buildHemnetSearchUrl across the 3 source-field fallback states (all-populated, rooms-null, object_type-null+price-null).

8. **Preserve existing safeguards:** the within-run search-page cache + in-flight Promise dedup (currently keyed by `${muniId}|${page}`) — re-key by the narrowed-URL hash so we don't refetch the same query twice. The MAX_PAGES=15 budget becomes irrelevant for the targeted path (1 page is enough), but keep it as a fallback ceiling.

9. **Run cron-wrapper-compatible smoke** before deploy: `node hemnet-targeted-match.js --smoke` then `node hemnet-targeted-match.js --dry-run --limit 3 --week 2026-05-11` (use last full cohort week's data to dry-run without writes).

**Decision values locked here (from D-27, D-28, D-29):**
- Price tolerance: ±5%
- Rooms: exact match (min=max=round(booli.rooms))
- object_type: mapping via booliObjectTypeToHemnet; null → drop filter
- Fallback drop-order: item_types[] first, then price, then rooms; never location_ids
</action>

<read_first>
- hemnet-targeted-match.js (whole file — large; pay attention to fetchSearch usage, cardMatches predicate, summary structure, smoke block)
- lib/hemnet-fetch.js (fetchSearch signature; consider extending)
- lib/booli-to-hemnet-mapping.js (the mapping helper)
- .planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-CONTEXT.md (D-26, D-27, D-28, D-29)
- .planning/phases/09-production-cutover-self-hosted-scraper-launch/09-2.5-DISCUSSION-LOG.md (Thread 2, Thread 11)
</read_first>

<acceptance_criteria>
- hemnet-targeted-match.js contains references to 'price_min' and 'rooms_min' and 'item_types' in URL construction
- hemnet-targeted-match.js requires lib/booli-to-hemnet-mapping
- node --check hemnet-targeted-match.js → OK
- node hemnet-targeted-match.js --smoke pass count >= existing (don't regress) + new buildHemnetSearchUrl tests
- node hemnet-targeted-match.js --dry-run --limit 3 --week 2026-05-11 runs without throwing and emits per-row "narrowed search hit N cards" log lines
- Updated function (or new sibling) in lib/hemnet-fetch.js supports the new URL shape
- Smoke fixture covers: all-fields-populated, rooms-null, price-null, object_type-null, all-null (location_ids only)
</acceptance_criteria>
</task>

<task id="8" type="pending">
<name>Deploy to droplet</name>
<wave>3</wave>
<status>NOT STARTED — depends on Task 7</status>

<action>
Deploy commits 618c896 (Tasks 1-6) AND the Job B rewrite commit (Task 7) to the droplet via git pull. Follow project_deploy_process.md memory:

  ssh dropletuser@droplet
  cd ~/hemnet-cohort-tracker
  git fetch && git pull
  node migrate-booli-listing-add-fields.js  # idempotent — safe to re-run
  node lib/booli-fetch.js --smoke
  node lib/booli-to-hemnet-mapping.js --smoke
  node booli-targeted-discovery.js --smoke
  node booli-targeted-refresh.js --smoke
  node hemnet-targeted-match.js --smoke

All five smoke runs must pass before exit. No code edits on droplet — git pull only.

Deploy deadline: before Sun 2026-05-17 22:00 UTC for Tasks 1-6 (so Job C cron uses new code), before Mon 2026-05-18 03:00 UTC for Task 7 (so Job B cron uses new code).
</action>

<read_first>
- memory.project_deploy_process.md
- memory.project_ip_whitelist.md (in case DB check fails on droplet)
</read_first>

<acceptance_criteria>
- Droplet's git HEAD matches the local master branch SHA after commit of Task 7
- All five --smoke commands on droplet exit 0 (operator-confirmed)
- Migration script reports schema present on droplet's DB connection (idempotent)
- Sun 2026-05-17 22:00 UTC Job C cron run completes with Final: line and writes new fields on W21 Booli rows
</acceptance_criteria>
</task>

<task id="9" type="pending checkpoint">
<name>Sunday + Monday wet-cron verification</name>
<wave>4</wave>
<status>NOT STARTED — depends on Task 8</status>

<action>
Post-deploy, the natural Sun/Mon cron grid validates Tasks 1-7 end-to-end:

- **Sun 2026-05-17 22:00 UTC: Job C cron run** (booli-targeted-discovery weekly). Operator monitors Slack alerts + cron_job_log for Final: line. Sample 10 newly-inserted booli_listing rows from this run — confirm price/rooms/living_area/object_type/agent_id are populated.

- **Mon 2026-05-18 03:00 UTC: Job B cron run** (hemnet-targeted-match weekly). Operator monitors Slack + cron_job_log. Expected output: per-muni log lines showing narrowed-search hit counts (e.g. "muniId=898623 booli_rows=120 hemnet_candidates_total=480 matched=89") instead of the old 15-page walk.

- **Mon 2026-05-18 06:00 UTC: cohort-create cron run.** Produces W21 cohort. Sample the resulting match rate:
  ```sql
  SELECT cohort_id,
         COUNT(*) FILTER (WHERE id IS NOT NULL) AS matched,
         (SELECT COUNT(*) FROM cohort_unmatched WHERE cohort_id = c.cohort_id) AS unmatched
  FROM cohorts c
  LEFT JOIN cohort_pairs p ON p.cohort_id = c.cohort_id
  WHERE c.cohort_id = '2026-W21'
  GROUP BY c.cohort_id
  ```

**Success criteria:**
- Job C cron: Final: line, EXIT=0, ≥ 95% of newly-inserted rows have non-null price+rooms+object_type
- Job B cron: Final: line, EXIT=0, no worker-uncaught lines, narrowed-search log lines present
- cohort-create cron: Final: line, W21 match rate ≥ 40% (loose target — 26.7% baseline)

**On any failure:** operator pings via standard process. Recovery options:
1. Quick fix + redeploy (small bug)
2. Revert to pre-09-2.5 master and let W22 absorb the work (acceptable per D-31)
3. Disable the new Job B path via env flag fallback to legacy (would require building such a flag — not in scope unless we hit it)
</action>

<read_first>
- cron-wrapper.js (logging contract)
- memory.project_deploy_process.md
</read_first>

<acceptance_criteria>
- verf09-2-5-logs/sun-job-c.log exists with Final: line + EXIT=0
- verf09-2-5-logs/mon-job-b.log exists with Final: line + EXIT=0 + narrowed-search log lines
- verf09-2-5-logs/mon-cohort-create.log exists with Final: line
- W21 match rate (from cohort_pairs JOIN cohorts) ≥ 40%
- Sample 10 W21 booli_listing rows show price/rooms/object_type all non-null on ≥ 9/10
</acceptance_criteria>

<resume-signal>
Pause here for operator confirmation post-cron-runs. Three resume signals:
- `success: <details>` — all three cron runs clean, W21 match rate ≥ 40%, proceed to SUMMARY.md.
- `partial: <details>` — Job C + Job B clean, but W21 match rate < 40%. Investigate: is the issue (a) Job B still missing Hemnet listings (consider widening price band), (b) cohort-create's SQL needs the deferred postcode/street fixes, or (c) genuine Hemnet ceiling? Operator decides whether to ship as-is and revisit on W22, or to patch.
- `failed: <details>` — any of the three cron runs failed. Recovery path per the action block.
</resume-signal>
</task>

</tasks>

<verification>
Plan succeeds when:

1. **Tasks 1-6 already done** (commit 618c896): verified locally.
2. **Task 7 ships** (Job B rewrite) before Mon 2026-05-18 03:00 UTC.
3. **Task 8 deploys** to droplet before Mon 2026-05-18 03:00 UTC.
4. **Task 9 verifies** that Sun Job C + Mon Job B + Mon cohort-create all complete with Final: lines and W21 match rate ≥ 40%.

Acceptable degraded outcomes:
- Task 7 slips one week → W21 ships with old Job B (~26.7% rate), W22 absorbs the rewrite. Phase 9 close-out delayed by one week.
- W21 match rate < 40% but > 27% → improvement is real but smaller than target; investigate and patch, don't revert.

Failure modes that trigger immediate revert:
- Job B cron fails with worker-uncaught or EXIT=1 unrelated to Oxylabs transient errors.
- Job C cron starts writing NULL prices on rows where the parser should be capturing them (regression in field capture path — unlikely given live verification on 3 URLs but possible at scale).
- cohort-create cron crashes due to W21 cohort_pairs having unexpected nulls in non-null columns (schema integrity issue).
</verification>

---

*Phase: 09-production-cutover-self-hosted-scraper-launch*
*Plan: 09-2.5*
*Plan written: 2026-05-15*
*Tasks 1-6 already shipped in commit 618c896; Task 7-9 pending*
