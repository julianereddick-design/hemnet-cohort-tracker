# Phase 9 / Plan 09-1.5: Oxylabs detail-page + county-loop triage - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `09-1.5-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-05-15
**Phase:** 09-production-cutover-self-hosted-scraper-launch
**Plan:** 09-1.5
**Areas discussed:** Two-issues-as-one hypothesis, Oxylabs plan scoping (user reframed "cost tolerance")

---

## Pre-discussion analysis (Claude)

Re-read `verf09-1-logs/wet-run.log` lines 9–135 to ground the gray areas in evidence. Key finding surfaced to user before gray-area selection:

- Stockholm pages 1–16 succeeded via Oxylabs (16/16 `oxylabs-fallback-success`), `directSuccessCount=0`.
- Stockholm page 17 + VG/Skåne/Uppsala page 1 all returned `OXYLABS_API_NON_200` — the Oxylabs API itself, not the target page (per `lib/scrape-http.js:140-145`).
- The "county-loop bail" symptom is downstream of Oxylabs-side rejection kicking in at the ~16-call threshold, NOT a state leak in `walkCountySearch`.

This reframed the gray area menu by suggesting issues #1 and #2 collapse to one root cause.

---

## Gray area menu (initial multi-select)

| Option | Description | Selected |
|--------|-------------|----------|
| Two-issues-as-one hypothesis | Lock the working hypothesis that Oxylabs `OXYLABS_API_NON_200` is the SHARED root cause of both symptoms. | ✓ |
| Diagnostic approach & artifacts | Probe-script first vs. live Oxylabs API curl. Whether to produce a `scripts/diagnose-09-1-5.md` root-cause doc. | |
| Oxylabs cost tolerance | If root cause is account/quota/render-config: budget ceiling? Stay on Web Scraper API or open to vendor swap? | ✓ (user-reframed) |
| Verification bar tightening | Tighten `<0.5` failure rate to `<0.2`? Make 4/4 counties strict vs. 3/4 acceptable? | |

**User's choice:** "Two-issues-as-one hypothesis, Scoping which Oxylabs plan to go to" — user accepted the first area as offered and reframed the third as a more concrete "which Oxylabs plan" question.

---

## Area 1: Two-issues-as-one hypothesis

| Option | Description | Selected |
|--------|-------------|----------|
| Verify-first, then converge or branch | First task proves/disproves the shared-root-cause hypothesis; plan branches based on evidence. (Claude-recommended) | |
| Lock the hypothesis, plan one fix path | Treat as locked; plan a single fix path. Re-plan if wet-run disproves it. | ✓ |
| Treat as two independent investigations | Don't lock; two parallel investigation tasks. | |

**User's choice:** "Lock the hypothesis, plan one fix path"
**Notes:** Captured as **D-13** in CONTEXT.md. Bold call given log evidence is strong but not airtight. Risk-mitigation: CONTEXT.md explicitly says "re-plan rather than back-filling tasks" if the wet-run disproves the hypothesis.

---

## Area 2: Scoping which Oxylabs plan to go to

**User reveal during discussion:** Previous Oxylabs access was a **trial account** that has effectively expired. User is committing to paying for a paid plan but needs help scoping which one.

This reveal changed the question from "cost tolerance" to "which paid plan."

| Option | Description | Selected |
|--------|-------------|----------|
| Planner produces shortlist; user picks | Plan 09-1.5 includes a research task: estimate volume, research Oxylabs plans, produce 2-3 option shortlist with costs. User picks mid-execute. (Claude-recommended) | ✓ |
| Lock 'Web Scraper API, smallest paid tier' | Skip comparison; assume smallest paid tier. Re-scope if volume blows past tier limits. | |
| User researches plan separately; planner waits | User handles Oxylabs subscription out-of-band; plan ships in-code change only. Wet-run gates on active subscription. | |
| Probe-then-decide | Pay for smallest top-up first, run a 10-URL probe (5 search + 5 detail) to identify what config works, then pick plan. | |

**User's choice:** "Planner produces shortlist; user picks"
**Notes:** Captured as **D-14 / D-14a / D-14b / D-14c** in CONTEXT.md. The plan must include a scoping task with volume estimate + plan comparison. Wet-run gates on the user having active paid creds. Vendor swap is out of scope for 09-1.5.

---

## Area 3: Wrap-up confirmation

| Option | Description | Selected |
|--------|-------------|----------|
| Write CONTEXT.md now | Two decisions locked; rest defers to planner discretion + existing plan body criteria. (Claude-recommended) | ✓ |
| Discuss diagnostic artifacts | Whether to produce `scripts/diagnose-09-1-5.md` root-cause doc. | |
| Discuss verification bar | Tighten failure-rate threshold or county-coverage strictness. | |
| Discuss probe-script scoping | Whether to write `scripts/probe-oxylabs-booli.js` vs. wet-run only. | |

**User's choice:** "Write CONTEXT.md now"

---

## Claude's Discretion

The following are NOT user-locked — captured in CONTEXT.md `<decisions>` § "Claude's Discretion":

- **Diagnostic root-cause doc (`scripts/diagnose-09-1-5.md`)** — planner decides. Strong default is yes (parallel to Plan 09-01's `scripts/diagnose-verf-b2.md`), but optional.
- **Probe-script (`scripts/probe-oxylabs-booli.js`)** — planner decides. Strong default is yes given paid-call iteration cost.
- **Verification bar** — stays at current `09-1.5-PLAN.md` `<verification>` block. User declined to tighten.

## Deferred Ideas

Captured in CONTEXT.md `<deferred>`:

- `[[oxylabs-cost-optimization]]` — post-cutover plan-tier optimization based on observed production volume.
- `[[diagnose-09-1-5-doc]]` — root-cause doc, planner discretion.
- `[[probe-oxylabs-booli]]` — probe script, planner discretion.
- `[[vendor-swap-if-oxylabs-fails]]` — vendor swap (BrightData, ScrapingBee, residential proxies), only revisit if paid Oxylabs still fails.
