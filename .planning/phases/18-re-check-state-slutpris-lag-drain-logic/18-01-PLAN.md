---
phase: 18-re-check-state-slutpris-lag-drain-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrate-sold-recheck-phase18.js
autonomous: true
requirements: [RECHECK-01]
must_haves:
  truths:
    - "sold_match carries first_unmatched_at, recheck_until, next_recheck_at columns after the migration runs"
    - "Re-running the migration is a no-op (IF NOT EXISTS) and never errors"
    - "The migration reports the three columns present via a read-back verify"
  artifacts:
    - path: "migrate-sold-recheck-phase18.js"
      provides: "Idempotent ALTER TABLE adding the three re-check scheduling columns to sold_match + a read-back verify"
      contains: "ADD COLUMN IF NOT EXISTS recheck_until"
  key_links:
    - from: "migrate-sold-recheck-phase18.js"
      to: "sold_match"
      via: "ALTER TABLE sold_match ADD COLUMN IF NOT EXISTS"
      pattern: "ALTER TABLE sold_match ADD COLUMN IF NOT EXISTS"
---

<objective>
Extend the existing `sold_match` table (created by `migrate-sold-phase16.js`, UNIQUE(booli_id)) with three nullable re-check scheduling columns, as a NEW idempotent migration. This is the schema half of RECHECK-01: an unmatched `booli_only` verdict row gains scheduling state so it can be found "due for re-check" on a later run and eventually settled.

Purpose: The drain loop (Plan 04) and the store-layer scheduling helpers (Plan 03) read/write these columns. They must exist first.
Output: `migrate-sold-recheck-phase18.js` — a standalone, operator-runnable, re-runnable migration following the `migrate-sold-phase16.js` style exactly (ALTER TABLE ... ADD COLUMN IF NOT EXISTS, read-back verify, parameterized $1, client.end() in finally).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- The existing sold_match DDL (migrate-sold-phase16.js:99-114). The three new columns
     attach to THIS table. UNIQUE(booli_id) already exists — do NOT re-create the table. -->
sold_match (
  id SERIAL PRIMARY KEY,
  booli_id BIGINT NOT NULL,
  matched_hemnet_slug TEXT,
  verdict TEXT,                -- 'matched' | 'booli_only' | 'uncertain' (no CHECK constraint)
  match_method TEXT,
  evidence JSONB,
  segment TEXT,
  window_start DATE,
  window_end DATE,
  adjudicated_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(booli_id)
)

<!-- The migration style to mirror: createClient → connect → query → read-back verify
     → client.end() in finally. The read-back verify idiom (migrate-sold-phase16.js:136-142)
     uses a parameterized $1::text[] ANY(...) query. -->
const { createClient } = require('./db');   // db.js exports createClient
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migrate-sold-recheck-phase18.js (ALTER TABLE + read-back verify)</name>
  <read_first>
    - migrate-sold-phase16.js (the migration style to copy verbatim: createClient/connect, query, the parameterized read-back verify at lines 136-142, the WR-01 client.end() in finally at lines 143-147, and the run().catch(process.exit(1)) tail)
    - lib/sold-store.js (lines 99-125, upsertSoldVerdict — confirms the sold_match column set the new columns extend; do NOT edit this file in this plan)
  </read_first>
  <action>
    Create a NEW file `migrate-sold-recheck-phase18.js` at repo root (do NOT edit migrate-sold-phase16.js). Copy the structure of migrate-sold-phase16.js exactly:

    1. `'use strict';` line 1, then a header comment block explaining: this adds Phase-18 re-check scheduling state to sold_match; run manually `node migrate-sold-recheck-phase18.js`; idempotent (ADD COLUMN IF NOT EXISTS).
    2. `const { createClient } = require('./db');`
    3. `async function run() { const client = createClient(); await client.connect(); try { ... } finally { await client.end(); } }`
    4. Inside try, issue ONE `await client.query(...)` with a single ALTER TABLE statement adding all three columns, EACH guarded by IF NOT EXISTS so a re-run is a no-op:
       ```sql
       ALTER TABLE sold_match
         ADD COLUMN IF NOT EXISTS first_unmatched_at TIMESTAMPTZ,
         ADD COLUMN IF NOT EXISTS recheck_until      TIMESTAMPTZ,
         ADD COLUMN IF NOT EXISTS next_recheck_at    TIMESTAMPTZ
       ```
       Add a comment above it stating the semantics: first_unmatched_at = when the row first became a booli_only candidate for re-check; recheck_until = first_unmatched_at + RECHECK_WINDOW_DAYS (settle deadline); next_recheck_at = when the row is next eligible for a re-check search (advances by RECHECK_INTERVAL_DAYS each pass). All TIMESTAMPTZ, all nullable (a matched/uncertain row leaves them null).
       `console.log('Altered table: sold_match (+first_unmatched_at, +recheck_until, +next_recheck_at)');`
    5. Read-back verify using the migrate-sold-phase16.js idiom but against information_schema.columns, parameterized — NO string interpolation:
       ```js
       const check = await client.query(
         `SELECT column_name FROM information_schema.columns
            WHERE table_schema = 'public' AND table_name = 'sold_match'
              AND column_name = ANY($1::text[])
            ORDER BY column_name`,
         [['first_unmatched_at', 'next_recheck_at', 'recheck_until']]
       );
       console.log('Re-check columns present:', check.rows.map(r => r.column_name).join(', '));
       ```
    6. Tail: `run().catch(err => { console.error('Error:', err.message); process.exit(1); });`

    Use parameterized `$1` only (ASVS V5 / SQL-injection guard) — there is no user input here, but the read-back ANY($1::text[]) keeps the established no-interpolation idiom. The live DDL run against prod is operator-gated (same gate as migrate-sold-phase16.js) and is NOT a plan acceptance criterion.
  </action>
  <verify>
    <automated>node -c migrate-sold-recheck-phase18.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -c migrate-sold-recheck-phase18.js` exits 0 (syntax valid)
    - `grep -q "ADD COLUMN IF NOT EXISTS first_unmatched_at" migrate-sold-recheck-phase18.js`
    - `grep -q "ADD COLUMN IF NOT EXISTS recheck_until" migrate-sold-recheck-phase18.js`
    - `grep -q "ADD COLUMN IF NOT EXISTS next_recheck_at" migrate-sold-recheck-phase18.js`
    - `grep -q "ALTER TABLE sold_match" migrate-sold-recheck-phase18.js`
    - `grep -q "information_schema.columns" migrate-sold-recheck-phase18.js` (read-back verify present)
    - `grep -q "ANY(\$1::text\[\])" migrate-sold-recheck-phase18.js` (parameterized read-back, no interpolation)
    - `grep -q "await client.end()" migrate-sold-recheck-phase18.js` (WR-01 release in finally)
    - migrate-sold-phase16.js is unchanged (NOT in this plan's edit set)
    - The new file contains NO `${` inside any SQL template literal (no string interpolation into SQL)
  </acceptance_criteria>
  <done>
    A standalone, re-runnable migration file exists that adds the three TIMESTAMPTZ scheduling columns to sold_match via ADD COLUMN IF NOT EXISTS and confirms them with a parameterized read-back verify; node -c passes; migrate-sold-phase16.js untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → DB (DDL) | Operator runs the migration against the shared prod DB; the only "input" is the hard-coded SQL in the file (no external/user input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering (SQL injection) | migrate-sold-recheck-phase18.js read-back query | mitigate | Read-back verify uses parameterized `ANY($1::text[])`; the ALTER statement is a static literal with no interpolation (`grep` confirms no `${` in SQL). No user/external input reaches any query. |
| T-18-02 | Denial of Service (destructive DDL) | ALTER TABLE on shared sold_match | accept | Only ADD COLUMN IF NOT EXISTS (additive, idempotent, non-destructive); no DROP/ALTER COLUMN. Live run operator-gated. Re-run is a no-op. Severity LOW. |
</threat_model>

<verification>
- `node -c migrate-sold-recheck-phase18.js` passes.
- All grep gates in the task acceptance criteria pass.
- Offline only: live DDL execution against prod is an operator-gated step, NOT a plan acceptance criterion.
</verification>

<success_criteria>
- migrate-sold-recheck-phase18.js exists, is syntactically valid, and adds first_unmatched_at / recheck_until / next_recheck_at to sold_match idempotently with a parameterized read-back verify.
- migrate-sold-phase16.js is unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/18-re-check-state-slutpris-lag-drain-logic/18-01-SUMMARY.md`
</output>
