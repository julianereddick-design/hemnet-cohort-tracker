---
phase: 18-re-check-state-slutpris-lag-drain-logic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sold-config.js
autonomous: true
requirements: [RECHECK-04]
must_haves:
  truths:
    - "RECHECK_WINDOW_DAYS defaults to 28 (~4 weeks) and is overridable via env with no code edit"
    - "RECHECK_INTERVAL_DAYS defaults to 7 and is overridable via env with no code edit"
    - "An invalid env override (non-positive / non-numeric) falls back to the default rather than corrupting the window"
  artifacts:
    - path: "lib/sold-config.js"
      provides: "RECHECK_WINDOW_DAYS + RECHECK_INTERVAL_DAYS config constants with env override + validation, exported"
      contains: "RECHECK_WINDOW_DAYS"
  key_links:
    - from: "lib/sold-config.js"
      to: "process.env.RECHECK_WINDOW_DAYS"
      via: "validated env override with default 28"
      pattern: "RECHECK_WINDOW_DAYS"
---

<objective>
Add the re-check window length and cadence to `lib/sold-config.js` as configurable constants (RECHECK-04): `RECHECK_WINDOW_DAYS` (default 28, ~4 weeks) and `RECHECK_INTERVAL_DAYS` (default 7), each readable from an env override with validation, exported alongside the existing constants. "Adjustable without code changes" means an operator sets `RECHECK_WINDOW_DAYS=21` in the environment and the drain behavior changes — no source edit.

Purpose: Plan 03 (store-layer "due" query) and Plan 04 (drain loop: enroll computes recheck_until = first_unmatched_at + window, next_recheck_at advances by interval, settle fires past the window) read these. They must exist and be exported first.
Output: two new exported constants + a small `posIntEnv` validation helper, plus matching `--smoke` assertions, in the existing config module — no new file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- lib/sold-config.js current shape (the file you extend). Mirror this style exactly:
     a documented const, then add it to module.exports, then add --smoke assertions. -->
const SOLD_DATE_WINDOW_DAYS = 10;
const READ_TIME_EXCLUDE_DAYS = 90;       // existing plain-number constant style
const DEFAULT_TARGET_PER_SEGMENT = 300;
function daysAgoISO(n, fromISO) { ... }  // existing date helper (returns YYYY-MM-DD)
module.exports = { SEGMENTS, MARKET_SOLD_TYPES, isTitleTransfer, PRICE_AGREE_PCT,
  AREA_AGREE_PCT, PRICE_BAND, SOLD_DATE_WINDOW_DAYS, READ_TIME_EXCLUDE_DAYS,
  DEFAULT_TARGET_PER_SEGMENT, daysAgoISO };
<!-- The inline --smoke block (lines 76-145) uses check(name, fn) + assert.strictEqual,
     prints `smoke: N pass, M fail`, process.exit(fail === 0 ? 0 : 1). Add to it; do not rewrite it. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RECHECK_WINDOW_DAYS + RECHECK_INTERVAL_DAYS with validated env override</name>
  <read_first>
    - lib/sold-config.js (the whole file — the constant-declaration style at lines 38-49, the module.exports block at lines 60-71, and the inline --smoke block at lines 76-145 you will extend)
    - scripts/sold-match-run.js (lines 61-66 validateDate — the ASVS-V5 input-validation idiom to mirror in spirit: reject bad input, never trust raw env)
  </read_first>
  <behavior>
    - posIntEnv('RECHECK_WINDOW_DAYS', 28) with env unset → 28
    - posIntEnv with env='21' → 21
    - posIntEnv with env='0' → 28 (non-positive rejected → default)
    - posIntEnv with env='-5' → 28 (negative rejected → default)
    - posIntEnv with env='abc' → 28 (non-numeric rejected → default)
    - RECHECK_INTERVAL_DAYS default is 7
    - RECHECK_WINDOW_DAYS is exported and is a positive integer
  </behavior>
  <action>
    Edit `lib/sold-config.js`:

    1. Add a private validation helper (place near the other helpers, before the constants that use it):
       ```js
       // posIntEnv — read a positive-integer env override (RECHECK-04), else the default.
       // ASVS V5: never trust raw env. Reject NaN / <= 0 / non-integer and fall back to
       // the documented default so a typo can never widen/zero the re-check window.
       function posIntEnv(name, dflt) {
         const raw = process.env[name];
         if (raw == null || raw === '') return dflt;
         const n = Number(raw);
         if (!Number.isInteger(n) || n <= 0) return dflt;
         return n;
       }
       ```
    2. Add the two constants (mirror the existing READ_TIME_EXCLUDE_DAYS comment-then-const style):
       ```js
       // Re-check drain window (RECHECK-04, Phase 18). An unmatched booli_only row is
       // re-attempted against Hemnet /salda until first_unmatched_at + RECHECK_WINDOW_DAYS,
       // re-searched no more often than every RECHECK_INTERVAL_DAYS, then settled terminal.
       // Both adjustable via env with NO code edit (default ~4 weeks / weekly cadence).
       const RECHECK_WINDOW_DAYS = posIntEnv('RECHECK_WINDOW_DAYS', 28);
       const RECHECK_INTERVAL_DAYS = posIntEnv('RECHECK_INTERVAL_DAYS', 7);
       ```
    3. Add `RECHECK_WINDOW_DAYS`, `RECHECK_INTERVAL_DAYS`, and `posIntEnv` to `module.exports`.
    4. Add `--smoke` assertions to the existing inline block (use the existing `check(name, fn)` + `assert`):
       - `check('RECHECK_WINDOW_DAYS default is 28', () => assert.strictEqual(posIntEnv('RECHECK_WINDOW_DAYS', 28), 28));`
       - `check('RECHECK_INTERVAL_DAYS default is 7', () => assert.strictEqual(RECHECK_INTERVAL_DAYS, 7));`
       - `check('posIntEnv: valid override honored', () => { process.env.__T = '21'; assert.strictEqual(posIntEnv('__T', 28), 21); delete process.env.__T; });`
       - `check('posIntEnv: zero rejected → default', () => { process.env.__T = '0'; assert.strictEqual(posIntEnv('__T', 28), 28); delete process.env.__T; });`
       - `check('posIntEnv: negative rejected → default', () => { process.env.__T = '-5'; assert.strictEqual(posIntEnv('__T', 28), 28); delete process.env.__T; });`
       - `check('posIntEnv: non-numeric rejected → default', () => { process.env.__T = 'abc'; assert.strictEqual(posIntEnv('__T', 28), 28); delete process.env.__T; });`
       - `check('RECHECK_WINDOW_DAYS is a positive integer', () => assert.ok(Number.isInteger(RECHECK_WINDOW_DAYS) && RECHECK_WINDOW_DAYS > 0));`

    Do NOT remove or renumber existing constants/exports/smoke checks (Pitfall 7 backward-compat: Phase 15/16/17 import from this module). The existing smoke count must only grow.
  </action>
  <verify>
    <automated>node lib/sold-config.js --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `node lib/sold-config.js --smoke` exits 0 and prints `0 fail`
    - `grep -q "RECHECK_WINDOW_DAYS" lib/sold-config.js`
    - `grep -q "RECHECK_INTERVAL_DAYS" lib/sold-config.js`
    - `grep -q "function posIntEnv" lib/sold-config.js`
    - `node -e "const c=require('./lib/sold-config'); if(c.RECHECK_WINDOW_DAYS!==28) process.exit(1); if(c.RECHECK_INTERVAL_DAYS!==7) process.exit(1)"` exits 0 (defaults exported correctly)
    - `RECHECK_WINDOW_DAYS=21 node -e "const c=require('./lib/sold-config'); if(c.RECHECK_WINDOW_DAYS!==21) process.exit(1)"` exits 0 (env override changes behavior, no code edit — RECHECK-04)
    - `RECHECK_WINDOW_DAYS=0 node -e "const c=require('./lib/sold-config'); if(c.RECHECK_WINDOW_DAYS!==28) process.exit(1)"` exits 0 (invalid override → default)
    - Existing exports (SEGMENTS, isTitleTransfer, daysAgoISO, READ_TIME_EXCLUDE_DAYS) still present in module.exports
  </acceptance_criteria>
  <done>
    RECHECK_WINDOW_DAYS (default 28) and RECHECK_INTERVAL_DAYS (default 7) are exported from lib/sold-config.js, settable via validated env override (invalid → default), proven by the env-override one-liners and the extended --smoke passing with 0 failures; no existing export removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment → config | RECHECK_WINDOW_DAYS / RECHECK_INTERVAL_DAYS read from process.env (operator-controlled, but untrusted-input class). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering (config input validation) | posIntEnv env parse | mitigate | `posIntEnv` rejects NaN / <= 0 / non-integer and falls back to the default, so a typo can't zero (settle-everything-immediately) or unbounded-widen the re-check window. Mirrors the validateDate ASVS-V5 idiom. |
| T-18-04 | Denial of Service (unbounded re-search) | RECHECK_WINDOW_DAYS magnitude | accept | A maliciously huge window would prolong re-searching, but the batch-wide Oxylabs spend ceiling (Phase 16/19) caps actual spend regardless; config is operator-set, not external. Severity LOW. |
</threat_model>

<verification>
- `node lib/sold-config.js --smoke` exits 0 with 0 fail.
- The env-override one-liners (default 28, override 21, invalid→28) all exit 0.
- All offline; no DB, no network.
</verification>

<success_criteria>
- Re-check window + interval are configuration (default 28 / 7), validated, and change behavior via env with no source edit — RECHECK-04 satisfied offline.
- No existing config export removed (backward compat for Phase 15/16/17).
</success_criteria>

<output>
After completion, create `.planning/phases/18-re-check-state-slutpris-lag-drain-logic/18-02-SUMMARY.md`
</output>
