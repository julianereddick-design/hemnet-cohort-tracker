---
phase: 14-spot-check-verdict-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/spotcheck-dhash.js
  - scripts/probe-dhash-sizing.js
autonomous: false
requirements:
  - branch2-use-dhash-not-hasphotos
  - harden-dhash-autoconfirm-shared-stock-floorplan
must_haves:
  truths:
    - "Operator can read a PROBE-DHASH-<cohortId>.md showing, per pair, the current verdict vs the proposed (D-02/D-05) verdict and WHY (D-01 trust deliverable)"
    - "Operator sees how many of the ~265 Branch-2 silent confirms would NO LONGER free-confirm under the proposed dHash-required rule"
    - "Operator sees the dHash min-distance histogram over all pairs with both galleries (D-01)"
    - "Operator sees vision call $ projections for 2-3 candidate routings (D-03) and chooses one"
    - "lib/spotcheck-dhash.js exports sharedPhotoPairs and filterDiscriminatingFiles, both --smoke covered (D-05 load-bearing primitives)"
  artifacts:
    - path: "scripts/probe-dhash-sizing.js"
      provides: "D-01 sizing/trust probe — runs current vs proposed rules side-by-side on N=200+, writes PROBE-DHASH md"
      min_lines: 200
    - path: "lib/spotcheck-dhash.js"
      provides: "sharedPhotoPairs (all cross-pairs within threshold + distinct check) and filterDiscriminatingFiles (drop floorplan/property_map/nearby_area)"
      contains: "module.exports = { minDHashDistance, sharedPhotoPairs, filterDiscriminatingFiles }"
  key_links:
    - from: "scripts/probe-dhash-sizing.js"
      to: "lib/spotcheck-dhash.js sharedPhotoPairs + filterDiscriminatingFiles"
      via: "require('../lib/spotcheck-dhash')"
      pattern: "sharedPhotoPairs|filterDiscriminatingFiles"
    - from: "scripts/probe-dhash-sizing.js"
      to: "lib/spotcheck-adjudicate.js adjudicatePair"
      via: "require to compute current_verdict per pair"
      pattern: "adjudicatePair"
---

<objective>
Build the D-01 sizing/trust probe and the two pure dHash primitives it depends on.

The probe runs the spot-check pipeline on a full recent cohort (N=200+), then applies BOTH the current Branch-2 rule AND the proposed D-02/D-05 rule to every pair, and writes an operator-readable report. The report doubles as the operator's first clear view of WHY each verdict happens (the operator does not currently trust the verdicts) and as the sizing input for the routing decision (how many pairs would lose their free-confirm, and what the implied vision $ cost is at 2-3 candidate routings).

Purpose: The operator decides the routing split (vision-only / vision-then-human / thresholds) AFTER seeing real $ figures. Do NOT hard-code a routing in this plan — present options and stop at the checkpoint.

Output:
- `lib/spotcheck-dhash.js` gains `sharedPhotoPairs` and `filterDiscriminatingFiles` (pure, --smoke covered) — these are load-bearing for D-05 and consumed by the probe.
- `scripts/probe-dhash-sizing.js` (new one-off) producing `PROBE-DHASH-<cohortId>.md` in the artifact dir.
- A checkpoint presenting the probe results to the operator for the routing decision (feeds Wave 2 plans 14-02/14-03/14-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-spot-check-verdict-quality/14-CONTEXT.md
@.planning/phases/14-spot-check-verdict-quality/14-RESEARCH.md
@.planning/phases/14-spot-check-verdict-quality/14-PATTERNS.md
@.planning/todos/pending/branch2-use-dhash-not-hasphotos.md
@.planning/todos/pending/harden-dhash-autoconfirm-shared-stock-floorplan.md

<interfaces>
<!-- Current lib/spotcheck-dhash.js exports (extracted, verified 2026-06-11). -->
<!-- hashAll() and hamming() are module-scope helpers, NOT exported — the new functions live in the same file and call them directly. -->

From lib/spotcheck-dhash.js:
```javascript
// module-scope (unexported) helpers, available to new functions in the same file:
function hamming(a, b)                       // bit-diff count over two 64-char bit strings
async function hashImage(file)               // → { file: basename, d: '<64-bit string>' }
async function hashAll(files)                // → [{ file, d }], skips unreadable (console.warn, never throws)

// currently the ONLY export:
async function minDHashDistance(booliFiles, hemnetFiles)  // → { minDist, bFile, hFile }; minDist=64 sentinel
module.exports = { minDHashDistance };
```

From lib/spotcheck-adjudicate.js (the probe imports this to compute current_verdict):
```javascript
function adjudicatePair(record, { visionResult } = {})   // → { verdict, source, reason }
module.exports = { adjudicatePair, adjudicatePairs };
```

From lib/spotcheck-summary.js (for Wilson CI in the report):
```javascript
// wilson95(successes, n) → [lo, hi]   (already exported; do not re-implement)
const { wilson95 } = require('../lib/spotcheck-summary');
```

Pair record shape after spotcheck-photos.js --gallery --all (from artifact JSON):
```javascript
{
  pair_id, county, provisional,        // 'likely-match' | 'suspect' | 'low-signal'
  deltas: { price_pct_diff, area_pct_diff },   // price_pct_diff null if Hemnet 404
  photos: {
    hemnet_gallery: [{ file, label }],  // label ALWAYS null for Hemnet
    booli_gallery:  [{ file, label }],  // label = Booli primaryLabel (e.g. 'floorplan')
  },
  street_address, postcode,
}
```
</interfaces>

<probe_analog>
<!-- scripts/probe-dhash-sizing.js structure follows scripts/probe-oxylabs-booli.js + cohort-spotcheck-gate.js. -->
findArtifactDir(cohortId) — copy verbatim from cohort-spotcheck-gate.js lines 90-104.
tsStamp(d) for any new dir name — copy from cohort-spotcheck.js (see PATTERNS.md §probe).
The gate ALREADY runs `spotcheck-photos.js <artifactDir> --gallery --all --max 6 --conc 5` (cohort-spotcheck-gate.js lines 160-171) — the probe reuses this exact execFileSync argv pattern when it needs to fetch galleries itself.
</probe_analog>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sharedPhotoPairs + filterDiscriminatingFiles to lib/spotcheck-dhash.js</name>
  <read_first>
    - lib/spotcheck-dhash.js (FULL — see current hamming/hashImage/hashAll/minDHashDistance and the --smoke async block at lines 79-146; new functions go in the same module so they can call hashAll/hamming directly)
    - .planning/phases/14-spot-check-verdict-quality/14-PATTERNS.md (lines 138-237 — exact proposed function bodies + the async --smoke checkAsync pattern)
    - .planning/phases/14-spot-check-verdict-quality/14-RESEARCH.md (lines 207-235 — dHash robustness facts, distinct-shared-photos requirement, Pitfall 4 same-image-twice)
  </read_first>
  <action>
In lib/spotcheck-dhash.js, ADD two pure functions and export them. Do NOT change minDHashDistance, hamming, hashImage, or hashAll.

(1) Module-level constant (place near the top, after the requires):
```javascript
// Booli primaryLabel values that are NOT proof of same-property (D-05).
// A shared floorplan/site-map/area-map must never satisfy the dHash auto-confirm.
const NON_DISCRIMINATING_LABELS = new Set(['floorplan', 'property_map', 'nearby_area']);
```

(2) filterDiscriminatingFiles(galleryEntries) — sync. galleryEntries is an array of `{ file: absolutePath, label: string|null }`. Returns an array of absolute file-path STRINGS (not objects) with non-discriminating labels dropped. Hemnet entries have label===null and pass through (the Hemnet whiteness/saturation heuristic is deferred per RESEARCH OQ-3 — do NOT implement it here):
```javascript
function filterDiscriminatingFiles(galleryEntries) {
  return (galleryEntries || [])
    .filter((g) => !(g.label && NON_DISCRIMINATING_LABELS.has(g.label)))
    .map((g) => g.file);
}
```

(3) sharedPhotoPairs(booliFiles, hemnetFiles, threshold) — async. booliFiles/hemnetFiles are arrays of absolute path STRINGS (already filtered by the caller). Returns `{ pairs, sharedCount, distinct }` where:
   - `pairs` = ALL cross-pairs `{ bFile, hFile, dist }` with Hamming dist <= threshold
   - `sharedCount` = pairs.length
   - `distinct` = true UNLESS the matched booli images are near-duplicates of EACH OTHER (Hamming <= threshold among matched booli files) OR the matched hemnet images are near-duplicates of each other — this defeats Pitfall 4 (same hero photo matched twice counted as 2 scenes). When fewer than 2 matched images on a side, that side cannot fail the distinct check.
Use the existing hashAll() and hamming() helpers. Never throw (hashAll already swallows unreadable files). Copy the body from 14-PATTERNS.md lines 159-191 verbatim.

(4) Update the export line to exactly:
```javascript
module.exports = { minDHashDistance, sharedPhotoPairs, filterDiscriminatingFiles };
```

(5) Add --smoke cases inside the existing async IIFE block (the `(async () => { ... })()` at lines 111-145), following the existing checkAsync pattern. Add at minimum:
   - 'filterDiscriminatingFiles: removes floorplan label' — input `[{file:'a.jpg',label:'floorplan'},{file:'b.jpg',label:'interior'}]` → result `['b.jpg']`
   - 'filterDiscriminatingFiles: removes property_map and nearby_area labels'
   - 'filterDiscriminatingFiles: passes through null label (Hemnet)' — `[{file:'h.jpg',label:null}]` → `['h.jpg']`
   - 'filterDiscriminatingFiles: passes through interior label'
   - 'sharedPhotoPairs([], [], 6) → { sharedCount: 0, pairs: [] }'
   - 'sharedPhotoPairs(null, null, 6) → { sharedCount: 0 } no throw'
Use synchronous `check()` for the filterDiscriminatingFiles cases (pure, no await) and `checkAsync()` for the sharedPhotoPairs cases. Keep the final `console.log(\`smoke: ${pass} pass, ${fail} fail\`)` and `process.exit(fail === 0 ? 0 : 1)`.
  </action>
  <verify>
    <automated>node lib/spotcheck-dhash.js --smoke</automated>
  </verify>
  <acceptance_criteria>
    - `node lib/spotcheck-dhash.js --smoke` exits 0 and prints `smoke: N pass, 0 fail` with N >= 9 (3 original hamming + 4 original minDHashDistance + new cases)
    - `grep -n "module.exports = { minDHashDistance, sharedPhotoPairs, filterDiscriminatingFiles }" lib/spotcheck-dhash.js` returns exactly one match
    - `grep -c "NON_DISCRIMINATING_LABELS" lib/spotcheck-dhash.js` returns >= 2 (definition + use)
    - `grep -n "function filterDiscriminatingFiles" lib/spotcheck-dhash.js` returns one match
    - `grep -n "async function sharedPhotoPairs" lib/spotcheck-dhash.js` returns one match
    - `grep -vE '^\s*//' lib/spotcheck-dhash.js | grep -c "distinct"` returns >= 1 (distinct logic present in code, not just comments)
  </acceptance_criteria>
  <done>Both functions exist, are exported, and all dHash smoke cases pass offline.</done>
</task>

<task type="auto">
  <name>Task 2: Build scripts/probe-dhash-sizing.js (current-vs-proposed, N=200+, PROBE-DHASH md)</name>
  <read_first>
    - scripts/probe-oxylabs-booli.js (FULL — script header, dotenv, createClient connect/release, log helper, main async IIFE with pass/fail exit — copy these structural patterns)
    - cohort-spotcheck-gate.js lines 90-104 (findArtifactDir — copy verbatim) and lines 137-224 (the exact execFileSync calls for cohort-spotcheck.js + spotcheck-photos.js --gallery --all, and the dHash loop the probe mirrors)
    - lib/spotcheck-dhash.js (the sharedPhotoPairs + filterDiscriminatingFiles added in Task 1)
    - lib/spotcheck-adjudicate.js (adjudicatePair — for current_verdict)
    - lib/spotcheck-summary.js (wilson95 export — for the CI line)
    - .planning/phases/14-spot-check-verdict-quality/14-RESEARCH.md lines 314-359 (probe design: what to measure, sample size, reuse, cost) and 362-391 (Pitfalls 1-5, esp. Pitfall 3 gallery-coverage separation)
    - .planning/phases/14-spot-check-verdict-quality/14-PATTERNS.md lines 434-573 (probe analog structure + per-pair output record shape)
  </read_first>
  <action>
Create scripts/probe-dhash-sizing.js as a one-off (NOT a cron job; do NOT wrap in runJob). Header doc comment must state: purpose (D-01 sizing/trust probe), Oxylabs cost estimate (~$4.31 for a full fresh run, ~861 calls), usage, and that it makes NO Anthropic API calls (vision $ is projected, not spent).

CLI flags:
- `--cohort <id>` (required if no --dir)
- `--dir <path>` — use an existing artifact dir (skip the fetch steps; fastest for iterating)
- `--rate <f>` default 0.20 (yields ~287 pairs on W23's 1,434 → exceeds N=200)
- `--threshold <n>` default 6 (DHASH_THRESHOLD; allow override to test recalibration per Claude's Discretion)
- `--conc <n>` default 5

Flow:
1. dotenv + createClient (require('../db')). Connect; on failure log FAIL and process.exit(1). (Multi-unit detection in the probe uses the in-memory sample per RESEARCH Option 2 — see step 5 — so the DB client is used only for cohort resolution if --cohort omitted; keep the connect/release pattern from the analog.)
2. Resolve artifactDir: if --dir given use it; else findArtifactDir(cohortId); if none, run the TWO child processes via execFileSync argv arrays (NEVER shell strings) exactly as cohort-spotcheck-gate.js does:
   - `[process.execPath, cohort-spotcheck.js, '--cohort', cohortId, '--rate', String(rate), '--conc', String(conc)]`
   - then findArtifactDir again, then `[process.execPath, spotcheck-photos.js, artifactDir, '--gallery', '--all', '--max', '6', '--conc', String(conc)]`
3. Parse the artifact JSON (the `spotcheck-*.json` in artifactDir) with the try/catch wrapping pattern from cohort-spotcheck-gate.js lines 182-190; assert Array.isArray(artifact.pairs).
4. Compute multi-unit set IN-MEMORY from the sample (RESEARCH Option 2 — acceptable for sizing): build a Map of `${(street_address||'').toLowerCase().trim()}|${postcode||''}` → count over artifact.pairs; a pair isMultiUnit when its key count > 1.
5. Per pair, compute and record:
   - `provisional`, `priceAgrees` = (deltas.price_pct_diff != null && deltas.price_pct_diff <= 0.05)
   - `hasBothGalleries` = booli_gallery.length>0 && hemnet_gallery.length>0
   - Resolve absolute file paths: `path.join(artifactDir, g.file)` for each gallery entry, preserving `{ file: absPath, label: g.label || null }`
   - `booliFiltered = filterDiscriminatingFiles(booliEntries)`, `hemnetFiltered = filterDiscriminatingFiles(hemnetEntries)`
   - If hasBothGalleries: `const sp = await sharedPhotoPairs(booliFiltered, hemnetFiltered, threshold)` → minDist = sp.pairs.length ? Math.min(...sp.pairs.map(x=>x.dist)) : 64; else minDist=64, sp={sharedCount:0,distinct:true,pairs:[]}
   - `hasFloorplanOnly` = booliEntries.length>0 && booliFiltered.length===0
   - `hasFloorplanBooli` = booliEntries.some(e => e.label==='floorplan')
   - `isMultiUnit` from step 4
   - `current_verdict` = adjudicatePair(pair).verdict  (Mode A — the live Branch-2 behavior)
   - `proposed_verdict`: compute the D-02/D-05 rule INLINE in the probe (do NOT modify adjudicate yet — that's Plan 14-02):
       dhashConfirmed = sp.sharedCount > 0 && sp.sharedCount >= 2 && !hasFloorplanOnly && !isMultiUnit && minDist <= threshold && sp.distinct
       if priceAgrees && provisional==='likely-match' && dhashConfirmed → 'CONFIRMED_MATCH'
       else if !hasBothGalleries → 'UNCERTAIN' (no photos — never routes to vision)
       else if priceAgrees && provisional==='likely-match' → 'WOULD_ROUTE'  (lost its free-confirm; candidate for vision/human per D-03)
       else → current_verdict (unchanged for suspect/mismatch/other — Branch 1/3/4/5 logic is out of scope here)
   - `wouldRouteToVision` = proposed_verdict==='WOULD_ROUTE' && hasBothGalleries
6. Build the histogram of minDist over pairs WITH both galleries only (bins: 0-3, 4-6, 7-10, 11-15, 16-30, 31-64). Separate the count of (a) no-gallery pairs, (b) gallery + dHash-confirm, (c) gallery + would-route — per Pitfall 3 so the vision projection is not inflated by gallery-miss pairs.
7. Vision $ projections: per-call cost constant `VISION_COST = 0.042` (document the A1 assumption in a comment: claude-sonnet-4-6 ~$3/MTok in, ~$15/MTok out, ~13,460 in + ~100 out tokens/call; tell the operator to confirm at anthropic.com/pricing). Project for at least 3 candidate routings and label them:
   - Routing A "all would-route → vision": n = count(wouldRouteToVision); cost = n * VISION_COST
   - Routing B "would-route AND narrow price gap (≤2%) → vision, rest → human": split by deltas.price_pct_diff <= 0.02
   - Routing C "all would-route → human only (0 vision)": cost $0, queue size = n
   Scale each to a weekly figure (sample is 20% → multiply by 5 for the full cohort if the operator ran the gate at 100%, but ALSO report the as-sampled count; label both clearly).
8. Wilson CI: `const [lo, hi] = wilson95(nWouldRoute, nWithBothGalleries)` for the "would lose free-confirm" rate; report as a percentage.
9. Write `PROBE-DHASH-<cohortId>.md` INTO artifactDir containing, in this order:
   - Header: cohortId, sample size, rate, threshold, generated_at, Oxylabs cost note
   - Summary metrics: total sampled; with both galleries; of the current Branch-2 silent confirms, how many stay CONFIRMED_MATCH vs become WOULD_ROUTE vs UNCERTAIN; the would-lose-free-confirm rate with Wilson 95% CI
   - dHash min-distance histogram table (bins × count, gallery-pairs only)
   - Redistribution table: provisional × current_verdict × proposed_verdict (counts)
   - Vision $ projection table: Routing A/B/C with as-sampled count, weekly-scaled count, and $ cost
   - Per-pair detail table (one row per pair): pair_id, county, street_address, provisional, priceAgrees, minDist, sharedCount, distinct, hasFloorplanBooli, isMultiUnit, current_verdict, proposed_verdict — so the operator can eyeball WHY each verdict happened (the trust deliverable)
   - A "Routing options for operator" closing section restating A/B/C plainly.
   Also write a machine-readable `PROBE-DHASH-<cohortId>.json` (same per-pair records + summary) alongside it for downstream reference.
10. End with a pass/fail summary line and process.exit(0) on success; wrap the whole thing in the `(async () => {...})().catch(e => { log('UNEXPECTED', ...); process.exit(1); })` pattern. Best-effort `await client.end()`.

All SQL (if any) uses $1 parameterized placeholders. No secrets in logs. No shell strings in execFileSync.
  </action>
  <verify>
    <automated>node -e "require('./scripts/probe-dhash-sizing.js')" 2>&1 | Select-String -Pattern "Error|SyntaxError" -NotMatch | Out-Null; node --check scripts/probe-dhash-sizing.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/probe-dhash-sizing.js` exits 0 (valid syntax)
    - `grep -n "require('../lib/spotcheck-dhash')" scripts/probe-dhash-sizing.js` matches AND `grep -c "sharedPhotoPairs\|filterDiscriminatingFiles" scripts/probe-dhash-sizing.js` returns >= 2
    - `grep -n "adjudicatePair" scripts/probe-dhash-sizing.js` matches (current_verdict computed from the real adjudicator)
    - `grep -n "wilson95" scripts/probe-dhash-sizing.js` matches (CI on the would-route rate)
    - `grep -n "PROBE-DHASH-" scripts/probe-dhash-sizing.js` matches (writes the md report)
    - `grep -vE '^\s*//' scripts/probe-dhash-sizing.js | grep -c "0.042\|VISION_COST"` returns >= 1 (vision cost projection present in code)
    - `grep -c "execFileSync" scripts/probe-dhash-sizing.js` — if the fetch-fallback path exists, every execFileSync uses an array argument (grep shows `execFileSync(` followed by `process.execPath` array form, never a single shell string)
    - No `child_process` call passes a string command (grep for `exec(` without File should return 0 matches: `grep -c "[^e]exec(" scripts/probe-dhash-sizing.js` returns 0)
  </acceptance_criteria>
  <done>Probe script is syntactically valid, imports the new dHash primitives + real adjudicator, computes current-vs-proposed per pair, and writes the operator report. (A live data run happens at the operator checkpoint, on the droplet where DB + Oxylabs are reachable.)</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Operator routing decision (D-01/D-03 gate)</name>
  <decision>How should price-agree + likely-match pairs that find NO dHash shared photo be routed once Branch 2 stops free-confirming them (D-03)?</decision>
  <context>
The probe (Task 2) produces PROBE-DHASH-&lt;cohortId&gt;.md with the real redistribution and vision $ projections. This decision parameterizes Wave 2 (plans 14-02 + 14-04): the exact onward routing for dHash-fail pairs must NOT be hard-coded before the operator sees the numbers (D-03 LOCKED). Wave 2 cannot proceed until this is chosen.

Run the probe on the droplet (DB + Oxylabs reachable there): commit+push this plan's code, operator pulls, then `node scripts/probe-dhash-sizing.js --cohort &lt;latest&gt; --rate 0.20` (or `--dir` against an existing artifact). Read the generated PROBE-DHASH md.
  </context>
  <how-to-verify>
1. Deploy: from the repo, commit + push this plan's two files (operator pulls on droplet — no Claude SSH per project convention).
2. On droplet: `node scripts/probe-dhash-sizing.js --cohort <latest-cohort> --rate 0.20 --conc 5` (~3-5 min, ~$4.31 Oxylabs). Confirm it writes `verf-spotcheck-<cohortId>-*/PROBE-DHASH-<cohortId>.md`.
3. Read the md: confirm the per-pair detail table renders, the histogram has counts, and the three routing $ figures are present.
4. Sanity-check the headline: of the current Branch-2 silent confirms, what fraction would lose their free-confirm (with Wilson CI)?
  </how-to-verify>
  <options>
    <option id="routing-a">
      <name>Routing A — all would-route pairs → vision, then UNCERTAIN→human</name>
      <pros>Maximum automated recovery of true matches that used different photo sets across platforms; smallest human queue.</pros>
      <cons>Highest vision $ (see probe Routing A figure); depends on ANTHROPIC_API_KEY being set on the droplet.</cons>
    </option>
    <option id="routing-b">
      <name>Routing B — narrow-price-gap would-route → vision, wider gap → human</name>
      <pros>Spends vision $ only where price strongly corroborates; bounded cost (probe Routing B figure).</pros>
      <cons>Some true matches with a 2-5% price gap go to the human queue instead of auto-recovering.</cons>
    </option>
    <option id="routing-c">
      <name>Routing C — all would-route → human queue only (no vision)</name>
      <pros>$0 vision cost; simplest; every lost free-confirm is human-adjudicated.</pros>
      <cons>Largest human queue (probe Routing C count); slowest throughput. Viable only if the queue size is sustainable.</cons>
    </option>
  </options>
  <resume-signal>State the chosen routing (routing-a / routing-b / routing-c, or a documented variant with explicit thresholds), AND confirm the dHash threshold to use in Wave 2 (default 6, or a recalibrated value justified by the probe histogram per Claude's Discretion). Also confirm whether the ≥2-distinct-shared-photos rule needs the tiny-gallery relaxation (Pitfall 2) based on how many 1-image-gallery pairs the probe found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → probe | Reads artifact JSON + downloaded gallery images (jimp decode); untrusted byte content crosses here |
| DB → probe | Cohort resolution query result |
| child process invocation | probe spawns cohort-spotcheck.js / spotcheck-photos.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | jimp decode of gallery images (sharedPhotoPairs/filterDiscriminatingFiles) | mitigate | hashAll already wraps each hashImage in try/catch and skips unreadable/corrupt files (console.warn, never throws); new functions reuse hashAll — no raw byte handling added |
| T-14-02 | Injection | child-process spawn of cohort-spotcheck.js / spotcheck-photos.js | mitigate | execFileSync with argv ARRAYS only (no shell strings), per 12-02 decision; cohortId comes from own DB, not user input |
| T-14-03 | Injection | DB cohort-resolution query | mitigate | parameterized $1 placeholders only (T-13-02 rule); probe does no string-interpolated SQL |
| T-14-04 | Information disclosure | PROBE-DHASH md/json artifacts + cron logs | accept | artifacts contain listing addresses + verdicts (already in existing verf-spotcheck artifacts); no secrets/keys written; ANTHROPIC_API_KEY never read by the probe (vision cost is projected, not called) |
</threat_model>

<verification>
- `node lib/spotcheck-dhash.js --smoke` → 0 fail (new + existing cases)
- `node --check scripts/probe-dhash-sizing.js` → exit 0
- On droplet: probe run produces PROBE-DHASH-<cohortId>.md with histogram, redistribution table, and 3 routing $ projections
- Operator routing decision recorded (resume-signal) — gates Wave 2
</verification>

<success_criteria>
- sharedPhotoPairs + filterDiscriminatingFiles exported and --smoke green
- Probe runs N=200+ (≥0.20 of latest cohort) and writes the operator-readable report doubling as the verdict-trust dataset
- Vision $ projected (not spent) at ≥3 routings with Wilson CI on the would-route rate
- Operator has chosen the routing + confirmed threshold + tiny-gallery relaxation decision
</success_criteria>

<output>
After completion, create `.planning/phases/14-spot-check-verdict-quality/14-01-SUMMARY.md`. Record: the chosen routing (verbatim), the confirmed dHash threshold, the tiny-gallery relaxation decision, and the probe's headline would-lose-free-confirm rate + CI — Wave 2 plans depend on all four.
</output>
