---
phase: 21-consistent-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/price-scraper-droplet-runbook.md
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03]
must_haves:
  truths:
    - "A fresh SSH session authenticates to root@170.64.181.89 with the persisted droplet_ed25519 key — no DO-console paste needed (ACCESS-01)"
    - "The droplet_ed25519 public key (comment julian-droplet) is present in /root/.ssh/authorized_keys, which lives on the persistent ext4 root disk (/dev/vda1) → survives reboot by construction (ACCESS-01)"
    - "sshd has PubkeyAuthentication enabled on the droplet (ACCESS-01)"
    - "The 'Julian Droplet' key (id 55446611) is registered at the DigitalOcean account level, visible via doctl compute ssh-key list (ACCESS-02)"
    - "A committed runbook documents host/region/user/key, the exact ssh command incl. the IdentitiesOnly/MaxAuthTries gotcha, add + revoke procedures, the 3-key access inventory, and the 'DO cannot inject keys into existing droplets' caveat (ACCESS-03)"
  artifacts:
    - path: "docs/price-scraper-droplet-runbook.md"
      provides: "The access runbook for the price-scraper droplet 170.64.181.89"
      contains: "IdentitiesOnly"
      min_lines: 40
  key_links:
    - from: "docs/price-scraper-droplet-runbook.md"
      to: "/root/.ssh/authorized_keys"
      via: "documented revoke one-liner"
      pattern: "sed -i .*julian-droplet.*authorized_keys"
    - from: "docs/price-scraper-droplet-runbook.md"
      to: "root@170.64.181.89"
      via: "documented ssh command with the IdentitiesOnly gotcha"
      pattern: "IdentitiesOnly=yes.*IdentityAgent=none.*droplet_ed25519"
---

<objective>
Make operator/Claude access to the price-scraper droplet `170.64.181.89`
(`ubuntu-s-1vcpu-2gb-syd1-01`, syd1, user `root`) durable and documented —
replacing the fragile one-off DO-console key paste used on 2026-06-29.

ACCESS-01 and ACCESS-02 are already TRUE by construction (the `droplet_ed25519`
public key with comment `julian-droplet` is in `/root/.ssh/authorized_keys` on
the persistent ext4 disk, and the account key "Julian Droplet" id `55446611`
exists at the DO account level). This plan **verifies** them by construction
(Task 1) and then writes the **access runbook** that satisfies ACCESS-03
(Task 2).

Purpose: After this plan, a reboot of the live scraper cannot lock the operator
out (key is on persistent disk + sshd pubkey on), a future rebuild made with the
account key auto-trusts us, and a committed runbook tells anyone exactly how to
connect, who has access, and how to add/revoke keys.
Output: `docs/price-scraper-droplet-runbook.md` (the only committed file). The
SSH/doctl steps in Task 1 are **read-only verification** — no droplet state is
changed and the live scraper is **NOT rebooted** (the actual reboot test is
operator-gated and out of scope per 21-CONTEXT.md > Deferred Ideas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-consistent-access/21-CONTEXT.md

<facts>
<!-- Verified live 2026-06-29. These are GIVENS — Task 1 re-confirms them read-only;
     do NOT generate a new key, do NOT add a login user, do NOT reboot. -->
Host / IP        : 170.64.181.89
Droplet name     : ubuntu-s-1vcpu-2gb-syd1-01
Region           : syd1
Login user       : root  (team containers + scripts run as root — keep this)
Operator key     : ~/.ssh/droplet_ed25519  (ed25519; public comment `julian-droplet`)
Account key      : "Julian Droplet", id 55446611  (public half of droplet_ed25519)
authorized_keys  : /root/.ssh/authorized_keys holds 3 keys —
                   (1) ours (ed25519, comment julian-droplet)
                   (2) a team ed25519 key
                   (3) a team rsa key
                   File is on /dev/vda1 ext4 (persistent) → durable across reboot.
sshd             : PubkeyAuthentication default-on; root key login works today.

Exact connect command (the documented gotcha — WITHOUT these flags the local
agent offers other keys first, trips MaxAuthTries, and returns a FALSE
"Permission denied (publickey)"):
  ssh -o IdentitiesOnly=yes -o IdentityAgent=none -i ~/.ssh/droplet_ed25519 root@170.64.181.89

Add a key (must be pasted via the DO web console for THIS existing droplet —
DigitalOcean CANNOT inject keys into an existing droplet via API):
  echo 'ssh-ed25519 AAAA... <comment>' >> /root/.ssh/authorized_keys

Revoke a key (by its comment):
  sed -i '/julian-droplet/d' /root/.ssh/authorized_keys

Other DO account keys that may explain the 2 non-ours entries:
  "cohort-key", "Hemnet", "Tom Laptop", "MonitorDoTom"
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify durable access by construction (ACCESS-01, ACCESS-02) and capture the key inventory</name>
  <read_first>
    - .planning/phases/21-consistent-access/21-CONTEXT.md (the <facts>/<decisions> — exact host, key, account-key id, and the IdentitiesOnly gotcha; the safety decision: NO reboot, verify by construction only)
  </read_first>
  <action>
    Run the following **read-only** checks. They change NO droplet state and do
    NOT reboot the live scraper. Use Git Bash for ssh on Windows. Every ssh call
    MUST use the gotcha flags so it does not false-fail on MaxAuthTries:

    SSH_PREFIX="ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -i ~/.ssh/droplet_ed25519 root@170.64.181.89"
    (ControlMaster=no/ControlPath=none force a FRESH connection — no reused
    socket — so the auth is genuinely re-exercised, satisfying the
    "fresh SSH session" requirement of ACCESS-01.)

    1. Fresh-session auth (ACCESS-01 truth #1):
         $SSH_PREFIX "echo SSH_OK"
       → must print SSH_OK.

    2. Key present + comment (ACCESS-01 truth #2):
         $SSH_PREFIX "grep -c julian-droplet /root/.ssh/authorized_keys"
       → must be >= 1.

    3. authorized_keys holds exactly the 3 known keys:
         $SSH_PREFIX "grep -c '^ssh-' /root/.ssh/authorized_keys"
       → must be 3.

    4. File is on the persistent disk (ACCESS-01 truth #2 — survives reboot):
         $SSH_PREFIX "df --output=source /root/.ssh/authorized_keys | tail -1"
       → must be /dev/vda1 (ext4 persistent root volume).

    5. sshd pubkey auth on (ACCESS-01 truth #3):
         $SSH_PREFIX "sshd -T 2>/dev/null | grep -i '^pubkeyauthentication'"
       → must print "pubkeyauthentication yes".

    6. Account-level key registered (ACCESS-02):
         doctl compute ssh-key list --format ID,Name,FingerPrint --no-header
       → output must contain a row with ID 55446611 / Name "Julian Droplet".

    7. Identify all 3 authorized_keys entries (for the runbook inventory). Get
       each key's fingerprint on the droplet, then cross-reference against the DO
       account keys by fingerprint:
         $SSH_PREFIX "ssh-keygen -lf /root/.ssh/authorized_keys"
         doctl compute ssh-key list --format Name,FingerPrint --no-header
       Match each on-droplet fingerprint to an account-key Name where possible.
       For ours, the comment julian-droplet ⇄ "Julian Droplet" 55446611. For the
       other two (1 ed25519 + 1 rsa), record the comment from the line and the
       best-match account-key name (e.g. "Tom Laptop", "MonitorDoTom"); if a
       fingerprint has no account-key match, label it "team key — owner per DO
       console, not at account level".

    Capture the results of steps 1–7 verbatim into a scratchpad evidence file at:
      "$TMPDIR/21-access-evidence.txt" (or, if $TMPDIR is unset, the session
      scratchpad dir) — Task 2 reads this to populate the runbook inventory and
      fingerprint table. This file is intermediate (NOT committed).

    If any ssh step false-fails with "Permission denied (publickey)", re-confirm
    the gotcha flags are present before treating it as a real failure (that is
    the exact symptom the gotcha causes). Do NOT reboot the droplet to
    investigate — the live scraper stays up.
  </action>
  <verify>
    <automated>ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -i ~/.ssh/droplet_ed25519 root@170.64.181.89 "grep -c julian-droplet /root/.ssh/authorized_keys"</automated>
  </verify>
  <acceptance_criteria>
    - The fresh-session command (`$SSH_PREFIX "echo SSH_OK"`) prints `SSH_OK` (ACCESS-01: key auth works with no console paste).
    - `grep -c julian-droplet /root/.ssh/authorized_keys` on the droplet returns >= 1.
    - `grep -c '^ssh-' /root/.ssh/authorized_keys` on the droplet returns 3.
    - `df --output=source /root/.ssh/authorized_keys | tail -1` returns `/dev/vda1` (persistent → survives reboot by construction).
    - `sshd -T | grep -i '^pubkeyauthentication'` returns `pubkeyauthentication yes`.
    - `doctl compute ssh-key list` output contains ID `55446611` / Name "Julian Droplet" (ACCESS-02).
    - The scratchpad evidence file exists and contains the `ssh-keygen -lf` fingerprint listing for all 3 authorized_keys entries plus the doctl account-key fingerprints used to identify them.
    - No droplet state was modified (only grep/df/sshd -T/ssh-keygen -lf/echo were run; NO reboot, NO writes to authorized_keys).
  </acceptance_criteria>
  <done>
    Durable access is proven by construction: a fresh keyed SSH session works,
    our key is on the persistent disk among the 3 known keys, sshd pubkey auth is
    on, the account key 55446611 is confirmed at the DO level, and all 3
    authorized_keys entries are identified by fingerprint — with the evidence
    captured to scratchpad for the runbook.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the access runbook docs/price-scraper-droplet-runbook.md (ACCESS-03)</name>
  <read_first>
    - .planning/phases/21-consistent-access/21-CONTEXT.md (the <facts> block: host/region/user/key/account-key-id, exact ssh command, add/revoke one-liners, the "DO can't inject keys into existing droplets" caveat — these are the required runbook contents)
    - the scratchpad evidence file from Task 1 ($TMPDIR/21-access-evidence.txt) — supplies the 3-key fingerprint inventory and the identified owner names
  </read_first>
  <action>
    Create `docs/price-scraper-droplet-runbook.md` (create the `docs/` dir if it
    does not exist). This is the ACCESS-03 deliverable. Section ordering and
    wording are at your discretion, but the file MUST contain ALL of the
    following (use the verified values from 21-CONTEXT.md > <facts> and the Task 1
    evidence — do NOT paste any private key material):

    1. **Host / identity** — IP `170.64.181.89`, droplet name
       `ubuntu-s-1vcpu-2gb-syd1-01`, region `syd1`, login user `root`
       (state explicitly: root is intentional — team containers/scripts run as
       root; no separate login user this phase).

    2. **Key** — operator key `~/.ssh/droplet_ed25519` (ed25519, public comment
       `julian-droplet`); account-level key "Julian Droplet" id `55446611`
       (the public half of droplet_ed25519). Optionally record the public
       fingerprint (from Task 1) — never the private key.

    3. **How to connect (with the gotcha)** — the EXACT command in a fenced code
       block:
         ssh -o IdentitiesOnly=yes -o IdentityAgent=none -i ~/.ssh/droplet_ed25519 root@170.64.181.89
       followed by a short explanation: without `IdentitiesOnly=yes` /
       `IdentityAgent=none` the local agent offers other keys first, trips
       `MaxAuthTries`, and you get a FALSE `Permission denied (publickey)`.

    4. **Durability** — `/root/.ssh/authorized_keys` lives on `/dev/vda1` ext4
       (persistent root disk) → access survives reboots by construction. Note the
       reboot test itself is operator-gated (live scraper) and was not performed.

    5. **Add a key** — the DO web-console paste one-liner (run on the droplet via
       the DigitalOcean console, since DO cannot inject keys into an existing
       droplet via API — see caveat):
         echo 'ssh-ed25519 AAAA... <comment>' >> /root/.ssh/authorized_keys

    6. **Revoke a key** — by comment:
         sed -i '/julian-droplet/d' /root/.ssh/authorized_keys
       (show the literal `julian-droplet` example and note: substitute the
       target key's comment).

    7. **Access inventory** — a table of the 3 `authorized_keys` entries from
       Task 1: for each, the key type (ed25519 / rsa), the comment, the
       fingerprint, and who it belongs to (ours = julian-droplet / "Julian
       Droplet" 55446611; the other two identified against the DO account keys,
       or labelled "team key — owner per DO console" if unmatched).

    8. **Caveat — account key vs existing droplet** — explicitly: "DigitalOcean
       cannot inject an SSH key into an *existing* droplet via API. For THIS
       droplet the durable mechanism is the on-disk `/root/.ssh/authorized_keys`
       entry. The account-level key (55446611) only auto-trusts on *future*
       droplets created/rebuilt with it."

    Do NOT include any private key contents, passwords, or Oxylabs/DB secrets in
    this file (public fingerprints and the account-key id are fine).
  </action>
  <verify>
    <automated>test -f docs/price-scraper-droplet-runbook.md && grep -q "IdentitiesOnly=yes" docs/price-scraper-droplet-runbook.md && grep -q "55446611" docs/price-scraper-droplet-runbook.md && echo RUNBOOK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/price-scraper-droplet-runbook.md` exists.
    - `grep -q "170.64.181.89" docs/price-scraper-droplet-runbook.md` (host/IP).
    - `grep -q "syd1" docs/price-scraper-droplet-runbook.md` (region).
    - `grep -q -i "user.*root\|root user\|root@170.64.181.89" docs/price-scraper-droplet-runbook.md` (login user).
    - `grep -q "55446611" docs/price-scraper-droplet-runbook.md` (account-key id, ACCESS-02 evidence).
    - `grep -q "IdentitiesOnly=yes" docs/price-scraper-droplet-runbook.md` AND `grep -q "IdentityAgent=none" docs/price-scraper-droplet-runbook.md` AND `grep -q "droplet_ed25519" docs/price-scraper-droplet-runbook.md` (exact connect command).
    - `grep -q -i "MaxAuthTries" docs/price-scraper-droplet-runbook.md` (the gotcha is explained).
    - `grep -q "sed -i" docs/price-scraper-droplet-runbook.md` AND `grep -q "julian-droplet" docs/price-scraper-droplet-runbook.md` (revoke one-liner).
    - `grep -q -i "authorized_keys" docs/price-scraper-droplet-runbook.md` AND the add-key one-liner (`>> /root/.ssh/authorized_keys`) is present.
    - The runbook contains an access inventory listing all 3 keys (`grep -c -i "ed25519\|rsa" docs/price-scraper-droplet-runbook.md` returns >= 3, accounting for the inventory rows).
    - `grep -q -i "cannot inject\|can't inject\|cannot push\|can't push" docs/price-scraper-droplet-runbook.md` (the existing-droplet caveat).
    - The file contains NO private key block: `grep -c "PRIVATE KEY" docs/price-scraper-droplet-runbook.md` returns 0.
  </acceptance_criteria>
  <done>
    A committed runbook fully documents the access model — host/region/user/key +
    account-key id, the exact ssh command and the IdentitiesOnly/MaxAuthTries
    gotcha, add and revoke procedures, the 3-key access inventory, and the
    "DO cannot inject keys into existing droplets" caveat — with no secret
    material. ACCESS-03 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → droplet sshd (public internet) | The operator authenticates to root over SSH from the open internet; anyone holding a key in `authorized_keys` gets root. |
| repo (committed runbook) → readers | The runbook is committed to this repo; anything written into it is visible to all repo readers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Spoofing / Repudiation | `/root/.ssh/authorized_keys` (3 keys, root access) | mitigate | Task 1 enumerates and identifies all 3 entries by fingerprint; the runbook records the access inventory (who holds each key) and a documented `sed -i` revoke one-liner, so stale/unknown keys are visible and removable. |
| T-21-02 | Information disclosure | committed `docs/price-scraper-droplet-runbook.md` | mitigate | Runbook records only PUBLIC fingerprints + the account-key id (55446611) — never private keys, passwords, or DB/Oxylabs secrets. Acceptance criterion greps `PRIVATE KEY` count == 0. |
| T-21-03 | Elevation of privilege | root SSH login | accept | Per 21-CONTEXT.md decision, the model stays user=root (team containers/scripts run as root); no new login user this phase. Exposure is bounded: pubkey-only auth (no password), keys enumerated/revocable. Deferred: dedicated per-droplet key / non-root deploy user. |
| T-21-04 | Denial of service | live production scraper during verification | mitigate | All Task 1 checks are read-only (grep/df/sshd -T/ssh-keygen -lf/echo); the droplet is NOT rebooted. The real reboot test is operator-gated and explicitly out of scope. |
</threat_model>

<verification>
- Task 1: fresh keyed SSH session returns SSH_OK; on-droplet greps confirm our key present, 3 keys total, file on /dev/vda1, sshd pubkeyauthentication yes; `doctl compute ssh-key list` shows 55446611; all 3 keys fingerprinted/identified into scratchpad.
- Task 2: all `grep` gates in the acceptance criteria pass against `docs/price-scraper-droplet-runbook.md`, and `grep -c "PRIVATE KEY"` returns 0.
- No droplet state changed; no reboot performed (the actual reboot test is operator-gated, deferred per CONTEXT).
</verification>

<success_criteria>
1. (ACCESS-01) Durable access proven by construction: a fresh keyed SSH session authenticates with no DO-console intervention, our key is on the persistent `/dev/vda1` disk, and sshd pubkey auth is on.
2. (ACCESS-02) The "Julian Droplet" key id 55446611 is confirmed at the DO account level via `doctl compute ssh-key list` and documented.
3. (ACCESS-03) `docs/price-scraper-droplet-runbook.md` is committed and documents host/region/user/key + account-key id, the exact ssh command with the IdentitiesOnly/MaxAuthTries gotcha, add/revoke procedures, the 3-key access inventory, and the "DO cannot inject keys into existing droplets" caveat — with no secret material.
</success_criteria>

<output>
After completion, create `.planning/phases/21-consistent-access/21-01-SUMMARY.md`
</output>
