---
phase: 24-cleanup
plan: 05
type: execute
wave: 5
depends_on: ["24-04"]
files_modified:
  - .planning/phases/24-cleanup/24-05-HARDENING.md
  - docs/price-scraper-droplet-remediation.md
  # On-droplet / scraper-repo effects (NOT this-repo files — edited over SSH on a feature branch in tt7676/hem-bol-scrapers, mirroring Phase 23):
  #   /etc/docker/daemon.json (host-level), /var/www/apps/hemnet/docker-compose.yml, settings/base.py, /var/www/apps/hemnet/.env, the rebuilt `hemnet` image
autonomous: false   # every step is a destructive/redeploy/secret action on the live box — operator-gated
requirements: [CLEAN-04]

must_haves:
  truths:
    - "The diagnosed live entry vector (from 24-01 R3 / 24-02 R3) is closed AT THE SOURCE in repo/host config on a feature branch — bind Docker daemon to the unix socket only via /etc/docker/daemon.json, OR put Redis on the docker-internal network + requirepass, OR unpublish/scope the offending port in docker-compose.yml — with the DO Cloud Firewall rule (24-02) kept as defense-in-depth; OR, if the source fix is too risky pre-resize, the port is explicitly recorded as kept-firewalled-interim with a TODO (per-fix escape hatch)"
    - "django runserver + DEBUG=True is replaced with a production server (gunicorn/uvicorn) + DEBUG=False on the feature branch — OR :8000 is recorded as kept-firewalled-interim with a TODO"
    - "Metabase v0.47.1 is upgraded to a current pinned image (CVE-2023-38646 pre-auth RCE class) — OR :3000 is recorded as kept-firewalled-interim with a TODO; any Metabase-store migration caution (shared managed Postgres) is noted"
    - "The exposed .env secrets are rotated reversibly: DJANGO_SECRET_KEY is rotated, and a DEDICATED rotatable Oxylabs sub-user is ported onto the box ONLY now that the host is verified clean (24-04) — never the shared cohort-tracker creds; DATABASE_URL and MB_DB_* (shared managed defaultdb) are NOT blind-rotated (coordinate-or-defer, or a separate scraper DB user) so the cohort-tracker's own jobs are not broken; NO secret VALUE is written into any doc/plan/log/commit"
    - "After any rebuild the Hemnet scraper is re-verified GREEN via the Phase-23 verification-crawl pattern (bounded, operator-pre-approved, ~0 403s through Oxylabs) and the 6 hemnet containers are Up (hemnet-crawler-playwright still gated off per Phase 23)"
    - "Every change is reversible: a feature branch in tt7676/hem-bol-scrapers (mirror Phase 23 feat/…), the prior `hemnet` image tag kept for rollback, only the affected image rebuilt + only the affected container(s) recreated, and an exact revert recipe documented; the whole 24-05 wave is independently skippable to its firewall-interim controls (24-02 containment + secret rotation hold the line)"
  artifacts:
    - path: ".planning/phases/24-cleanup/24-05-HARDENING.md"
      provides: "Durable hardening evidence: feature branch name, per-fix source-edit-OR-firewall-interim decision (vector / runserver / Metabase), secret-rotation record (key names only), rebuild + re-verify-crawl result (~0 403s), and the exact revert recipe"
      contains: "revert recipe"
    - path: "docs/price-scraper-droplet-remediation.md"
      provides: "The 24-04 'team actions recommended' items (entry-vector source fix, runserver/DEBUG, Metabase upgrade, .env rotation) marked DONE-in-24-05 (or kept-firewalled-interim), pointing to 24-05-HARDENING.md"
      contains: "24-05"
  key_links:
    - from: ".planning/phases/24-cleanup/24-05-HARDENING.md"
      to: "the rebuilt hemnet image / re-verify crawl"
      via: "bounded Oxylabs crawl proving ~0 403s after the source hardening + rebuild"
      pattern: "403"
    - from: "docs/price-scraper-droplet-remediation.md"
      to: ".planning/phases/24-cleanup/24-05-HARDENING.md"
      via: "the durable-hardening items are marked closed in 24-05 and reference the hardening evidence"
      pattern: "24-05"
---

<objective>
Durable root-cause hardening (D-08), sequenced LAST and gated on the host being VERIFIED CLEAN by 24-04. The DO Cloud Firewall containment from 24-02 stays the immediate control; 24-05 fixes the source so the firewall is not the only thing holding the line. On a feature branch in the operator-owned scraper repo (`tt7676/hem-bol-scrapers`, mirroring Phase 23's `feat/hemnet-oxylabs-fetch` posture): (1) close the diagnosed entry vector at the source — BRANCHING on the live vector identified in 24-01 R3 / 24-02 R3, not assuming one; (2) replace `django runserver`+`DEBUG=True` with a production server + `DEBUG=False`; (3) upgrade Metabase v0.47.1 to a current pinned image; (4) rotate the exposed `.env` secrets — with the SHARED managed `defaultdb` `DATABASE_URL`/`MB_DB_*` treated as coordinate-or-defer (NOT a blind rotate), and a dedicated rotatable Oxylabs sub-user ported back only now that the box is verified clean; (5) rebuild only the affected image + recreate only the affected container(s) reversibly (prior tag kept), then prove the Hemnet scraper still fetches green via the Phase-23 verification-crawl pattern (~0 403s). Each sub-fix is independently skippable to its firewall-interim control; the whole wave may split to a follow-up phase if the rebuild/redeploy risks destabilizing the scraper right before the Phase-25 resize.

Purpose: CLEAN-04's durable closure — fix the entry vector at the source (not only the firewall) and rotate the secrets a long compromise may have exposed, so the box is durably trusted, with the scraper re-verified green and a documented rollback.
Output: `.planning/phases/24-cleanup/24-05-HARDENING.md` (branch + per-fix decisions + secret-rotation record + re-verify crawl + revert recipe); `docs/price-scraper-droplet-remediation.md` updated to mark the durable-hardening items closed in 24-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cleanup/24-CONTEXT.md
@.planning/phases/24-cleanup/24-RESEARCH.md
@.planning/phases/24-cleanup/24-VERIFICATION.md
@docs/price-scraper-droplet-remediation.md
@docs/price-scraper-droplet-runbook.md
@docs/price-scraper-droplet-audit.md
@.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-01-PLAN.md
@.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-02-PLAN.md

**Target droplet (NOT this repo):** `170.64.181.89` (`s-8vcpu-16gb`, syd1, root). Repo `github.com/tt7676/hem-bol-scrapers`, app at `/var/www/apps/hemnet`; compose services bind-mount the whole repo (`volumes: - .:/app`, 22-EVIDENCE §1). The on-droplet/scraper-repo edits below are made over SSH on a feature branch — they are NOT files in this cohort-tracker repo. The `.env` lives at `/var/www/apps/hemnet/.env` (`OXYLABS_*`, `DJANGO_SECRET_KEY`, `DATABASE_URL`, `MB_DB_*`).

**Phase-23 analog (MIRROR THIS EXACT PATTERN):** 23-01 edited the same scraper repo on the droplet on a `feat/…` branch (never team `main`), 23-02 rebuilt ONLY the `hemnet` image (`docker compose build <svc>`) + recreated ONLY the affected container (`docker compose up -d --no-deps <svc>`) and ran a bounded ~200-page Oxylabs verification crawl proving ~0 403s, 23-03 gated Playwright off reversibly. 24-05 reuses that build → recreate → verify-crawl → document-revert shape for the hardening edits.
</context>

<guardrails>
## HARD GUARDRAILS — destructive / redeploy / secret-rotation, OPERATOR-GATED (D-06 / D-08)

`autonomous: false` — pause for operator go-ahead before EACH destructive/redeploy/secret step; proceed only on go-ahead. Reversible-first, audit-before-change, in place.

**PRECONDITION GATE (D-07):** Do NOT start this plan unless `24-VERIFICATION.md ## Verification table` records the host VERIFIED CLEAN (all-PASS from 24-04). If the box is not verified clean, STOP — 24-05 runs only on a trusted box; the firewall containment (24-02) holds meanwhile.

**NON-NEGOTIABLE:**
- **Reversibility (mirror Phase 23):** all repo/config edits on a feature branch in `tt7676/hem-bol-scrapers` (e.g. `feat/p24-durable-hardening`); NEVER edit/commit/force-push team `main`; do NOT trigger the team deploy pipeline. Keep the PRIOR `hemnet` image tag (e.g. `docker tag hemnet:current hemnet:pre-24-05`) before any rebuild so rollback is one command. Rebuild ONLY the affected image; recreate ONLY the affected container(s) with `--no-deps`. Document the exact revert recipe.
- **Per-fix escape hatch:** each source fix (vector closure / runserver / Metabase) is independently skippable to its firewall-interim control (the 24-02 DO Cloud Firewall deny + a recorded TODO) if the source fix risks destabilizing the scraper pre-resize. If the rebuild/redeploy itself looks risky right before Phase 25, the whole wave may be SPLIT to a follow-up phase — record that decision; the 24-02 containment + the secret rotation still hold the security line.
- **BRANCH ON THE DIAGNOSED VECTOR — do NOT assume one.** Read the 24-01 R3 / 24-02 R3 finding (which port/service was the live vector, or "local foothold only") and fix THAT at the source. Keep the DO Cloud Firewall rule as defense-in-depth regardless.
- **SHARED managed Postgres `defaultdb` caveat (CRITICAL):** `DATABASE_URL` (and `MB_DB_*` if it points at the same managed cluster) is SHARED with THIS cohort-tracker repo. DB-credential rotation is NOT unilateral — a blind rotate breaks the cohort-tracker's own jobs. Treat DB creds as **coordinate-or-defer**: either (a) coordinate a lockstep rotation that also updates this repo's `.env`/droplet config, or (b) provision a SEPARATE scraper DB user and point `DATABASE_URL` at it (non-breaking), or (c) DEFER with a recorded note. NEVER blind-rotate the shared DB password/user.
- **Oxylabs creds:** rotate, but PORT working creds onto the box ONLY now that it is verified clean (already true at Wave 5), preferring a DEDICATED, rotatable Oxylabs sub-user over the shared cohort-tracker creds (STATE 2026-06-30). The droplet's own creds are currently dead (401, Phase 23) — porting a fresh dedicated sub-user is what re-enables prod.
- **NO secret VALUES anywhere.** Record key NAMES + "rotated"/"ported" facts only; paste `<REDACTED>` for values. Never write `OXYLABS_*`, `DJANGO_SECRET_KEY`, `DATABASE_URL`, `MB_DB_*` values into any doc/plan/log/commit.
- **No DB table drops; no live-container collateral.** Never prune the live `hemnet` image; never stop/rm a live hemnet container beyond the targeted recreate; Playwright stays gated off (Phase 23).
- **Paid Oxylabs run:** the bounded re-verify crawl is the ONE paid run for this plan and needs explicit operator go-ahead (standing no-Oxylabs-without-approval rule). Hard-cap it; report calls + cost.

**Lock-out guard (SSH):** keep a SECOND live SSH session open before/while changing daemon/firewall/redis config; if a fresh login fails, rule out the `IdentitiesOnly` gotcha first (runbook) before assuming lock-out. DO web Console is the fallback.

## Connection (exact — IdentitiesOnly gotcha is mandatory)
```bash
DROPLET='ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89'
```
Off-box probes (`nc`, `doctl`) run from the operator workstation, not via `$DROPLET`.

## Evidence file
Create `.planning/phases/24-cleanup/24-05-HARDENING.md` with sections: `## Precondition gate (verified-clean)`, `## Feature branch + prior-image tag`, `## Fix 1 — entry-vector source closure`, `## Fix 2 — runserver/DEBUG`, `## Fix 3 — Metabase upgrade`, `## Fix 4 — secret rotation`, `## Rebuild + re-verify crawl`, `## Revert recipe`. Under each, record the exact command/decision + result. No secret values.
</guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot + feature branch + author the durable source-hardening edits (vector / runserver / Metabase)</name>
  <read_first>
    - `24-VERIFICATION.md` `## R3 Entry vector` (24-01) + `## R3` closure subsection (24-02) — the EXACT diagnosed vector (port/service, or "local foothold only") this task must fix at the source, and what firewall closure 24-02 already applied
    - `24-RESEARCH.md` §"Entry Vector: Check Commands + Decision Tree" (the per-candidate source fixes: daemon.json unix-socket-only; Redis docker-internal + requirepass; compose port scope), §"Security Domain / STRIDE" (mitigations per threat), §"Threat Model"
    - `24-CONTEXT.md` D-08 (the four hardening sub-fixes + the escape hatch) and the revised D-06 (repo edits permitted, reversible)
    - `23-01-PLAN.md` (the feature-branch-edit posture to mirror) + `docs/price-scraper-droplet-audit.md` (exposed services: `django runserver`:8000+DEBUG, Metabase v0.47.1:3000, Redis broker, Docker daemon; the compose layout)
  </read_first>
  <files>.planning/phases/24-cleanup/24-05-HARDENING.md</files>
  <action>
OPERATOR-GATED. Create `24-05-HARDENING.md` with the section skeleton (see guardrails). First record the precondition gate: quote the 24-04 VERIFIED-CLEAN verdict — if absent, STOP.

Snapshot + branch (mirror 23-01):
```bash
$DROPLET 'cd /var/www/apps/hemnet && git rev-parse HEAD && git status --porcelain && git branch --show-current'
$DROPLET 'cd /var/www/apps/hemnet && git checkout -b feat/p24-durable-hardening && git branch --show-current'   # NEVER edit on main
$DROPLET 'docker images | grep -E "^hemnet| hemnet "'   # record the current hemnet image id/tag (prior-tag rollback baseline)
```
Record the on-droplet HEAD, the new branch name, and the current image id into `## Feature branch + prior-image tag` — this is the rollback baseline (the actual `docker tag …:pre-24-05` is taken in Task 3 right before the rebuild).

**Fix 1 — close the entry vector AT THE SOURCE (BRANCH on the 24-01/24-02 finding — do NOT assume):**
- If the diagnosed vector was the **Docker daemon API on TCP**: confirm/set the daemon to unix-socket-only via the host-level `/etc/docker/daemon.json` (NOT compose) — e.g. ensure no `-H tcp://0.0.0.0` and `hosts` is `unix:///var/run/docker.sock` only. **Back it up first:** `$DROPLET 'cp -a /etc/docker/daemon.json /root/24-backup/daemon.json.bak 2>/dev/null || echo "no daemon.json (defaults)"'`. **BLAST-RADIUS WARNING:** applying a daemon.json change requires `systemctl restart docker`, which bounces **ALL** containers (not the `--no-deps` single-container model the rest of this plan uses) — so for THIS vector, **PREFER the firewalled-interim control** (the 24-02 DO Cloud Firewall deny already contains it, repo-edit-free, no bounce) and record that as the chosen control + a TODO, UNLESS the operator explicitly approves the daemon restart (then schedule it with the rebuild in Task 3, expect all 6 hemnet containers to bounce, and re-verify they all come back Up). Record before/after `daemon.json` (no secrets) either way; the DO firewall deny stays as defense-in-depth regardless.
- If it was **host-published / unauthenticated Redis**: on the feature branch, scope `hemnet-redis` to the docker-internal network only (remove the host `ports:` publish in `docker-compose.yml`) AND set `requirepass` (via compose env / the broker config the app reads), updating the app's broker URL to match — keep the value out of the doc.
- If it was **a specific published port** (e.g. :8000 / :3000): unpublish/scope it in `docker-compose.yml` to localhost/internal only.
- If 24-01/24-02 found **"local foothold only" (no external vector)**: record that no source port change is needed; the firewall default-deny stays as defense-in-depth; note it explicitly.
Record the chosen branch + the exact edit (or "no change needed — local foothold") into `## Fix 1`. **Escape hatch:** if the source change risks the scraper pre-resize, record "kept firewalled-interim (24-02 DO Cloud Firewall deny) + TODO" instead and move on.

**Fix 2 — replace `django runserver` + `DEBUG=True`:** on the feature branch, switch the prod web service to a production server (gunicorn/uvicorn) in `docker-compose.yml` (command:) + `requirements`/Dockerfile if gunicorn is not installed, and set `DEBUG=False` in `settings/base.py` (or via env). **Escape hatch:** if a full swap is too risky pre-resize, record ":8000 kept firewalled-interim + TODO" as the chosen control. Record the decision + edit into `## Fix 2`.

**Fix 3 — upgrade Metabase v0.47.1:** on the feature branch, pin the `metabase` image in `docker-compose.yml` to a current patched tag (CVE-2023-38646 class). **Note the migration caution:** Metabase backs onto the SAME managed Postgres — a major version jump may migrate its app schema; record the caution + a backup/rollback note (and that the image tag swap is revertible). **Escape hatch:** if the upgrade risks the Metabase store, record ":3000 kept firewalled-interim + TODO". Record into `## Fix 3`.

**Commit each fix as its OWN commit** on the feature branch as you author it (NOT one bundled commit), so each is independently revertible with a clean `git revert <sha>` and the per-fix escape hatch has a matching per-fix rollback:
```bash
# after Fix 1: git add -A && git commit -m "harden(p24): close entry vector at source (CLEAN-04)"
# after Fix 2: git add -A && git commit -m "harden(p24): runserver->gunicorn + DEBUG=False (CLEAN-04)"
# after Fix 3: git add -A && git commit -m "harden(p24): upgrade Metabase v0.47.1 -> pinned (CLEAN-04)"
```
Skip the commit for any fix taken to firewalled-interim (nothing to commit). Then validate any edited Python/compose parses (read-only, no restart):
```bash
$DROPLET 'cd /var/www/apps/hemnet && docker compose config >/dev/null && echo COMPOSE_OK'
$DROPLET 'docker exec hemnet-crawler python -c "import ast; ast.parse(open(\"/app/settings/base.py\").read()); print(\"PARSE_OK\")" 2>/dev/null || echo "settings parse: re-check path"'
```
Do NOT rebuild yet (that is Task 3). Record per-fix decisions + the COMPOSE_OK/PARSE_OK results + the per-fix commit SHAs (one per applied fix). No push to main, no deploy script. No secret values anywhere.
  </action>
  <acceptance_criteria>
    - `## Precondition gate` quotes the 24-04 VERIFIED-CLEAN verdict; `## Feature branch + prior-image tag` records the new `feat/…` branch (NOT main) + the current hemnet image id baseline
    - `## Fix 1` states which 24-01/24-02-diagnosed vector it closes at the source (daemon.json / redis docker-internal+requirepass / compose port scope) OR "local foothold — no source change, firewall stays" OR "kept firewalled-interim + TODO" (escape hatch) — it does NOT assume a vector
    - `## Fix 2` records the runserver→gunicorn/uvicorn + DEBUG=False edit OR ":8000 kept firewalled-interim + TODO"
    - `## Fix 3` records the Metabase pinned-image upgrade (with the shared-Postgres migration caution) OR ":3000 kept firewalled-interim + TODO"
    - `docker compose config` (COMPOSE_OK) recorded; each applied fix committed as its OWN commit on the feature branch (one SHA per applied fix recorded, so per-fix `git revert` works); team `main` untouched; NO secret value pasted
  </acceptance_criteria>
  <verify>
    <automated>bash -c "f=.planning/phases/24-cleanup/24-05-HARDENING.md; test -f \"$f\" && grep -qiE 'verified.clean|all-PASS|VERIFIED CLEAN' \"$f\" && grep -qiE 'feat/' \"$f\" && grep -qiE 'daemon.json|requirepass|docker-internal|compose|port scope|local foothold' \"$f\" && grep -qiE 'gunicorn|uvicorn|DEBUG=False|firewalled-interim' \"$f\" && grep -qiE 'metabase' \"$f\" && grep -qiE 'COMPOSE_OK|compose config' \"$f\" && ! grep -niE '(OXYLABS_[A-Z]+|DJANGO_SECRET_KEY|MB_DB_[A-Z]+|DATABASE_URL)[=: ]+[^<[:space:]]' \"$f\""</automated>
  </verify>
  <done>On a feature branch in the operator-owned scraper repo, the diagnosed entry vector is closed at the source (or recorded as firewalled-interim), runserver/DEBUG and Metabase are hardened (or recorded as firewalled-interim), the compose/settings parse, and the change is committed to the branch only — with the prior-image rollback baseline recorded and zero secret values.</done>
</task>

<task type="auto">
  <name>Task 2: Rotate the exposed .env secrets reversibly (shared-defaultdb = coordinate-or-defer)</name>
  <read_first>
    - `24-RESEARCH.md` §"Security Domain" ("Assume secret exposure"), §"Runtime State Inventory" → "Secrets/env vars" (the `.env` key inventory + the no-creds-until-verified rule)
    - `24-CONTEXT.md` D-08 secret-rotation bullet (Oxylabs dedicated rotatable sub-user, port only AFTER verified-clean) + the SHARED `defaultdb` caveat; `STATE.md` (2026-06-30 dedicated-sub-user decision; droplet Oxylabs creds dead/401)
    - `docs/price-scraper-droplet-remediation.md` (24-04's `.env`-rotation recommendation this task now executes)
    - Host: `/var/www/apps/hemnet/.env` (keys: `OXYLABS_*`, `DJANGO_SECRET_KEY`, `DATABASE_URL`, `MB_DB_*`)
  </read_first>
  <files>.planning/phases/24-cleanup/24-05-HARDENING.md</files>
  <action>
OPERATOR-GATED. Rotate ONLY the freely-rotatable secrets; treat the shared-DB creds as coordinate-or-defer. Back up `.env` first (additive, no secret leaves the box): `$DROPLET 'cp -a /var/www/apps/hemnet/.env /root/24-backup/env.pre-24-05.bak'`.

**DJANGO_SECRET_KEY — rotate freely** (app-internal signing key; rotating it only invalidates existing signed cookies/sessions, harmless on a scraper box). Generate a fresh value ON the box, write it into `.env` directly, never echo/commit the value:
```bash
$DROPLET 'python3 -c "import secrets; print(secrets.token_urlsafe(64))" > /root/.django_secret.tmp'   # value stays on box, never printed to the doc
# update DJANGO_SECRET_KEY in /var/www/apps/hemnet/.env from that file, then shred it:
$DROPLET 'shred -u /root/.django_secret.tmp 2>/dev/null || rm -f /root/.django_secret.tmp'
```
Record "DJANGO_SECRET_KEY: rotated (value never recorded)" — names only.

**Oxylabs — dedicated rotatable sub-user, ported only now (verified clean):** in the Oxylabs dashboard create/rotate a DEDICATED sub-user for this droplet (NOT the shared cohort-tracker creds), and write its `OXYLABS_*` values into the box's `.env`. This is the cred refresh that re-enables prod (droplet's own creds are 401, Phase 23). Record "OXYLABS_*: rotated to a dedicated droplet sub-user, ported post-verified-clean (values never recorded)". Do NOT paste the values.

**DATABASE_URL + MB_DB_* — SHARED managed `defaultdb`: DO NOT blind-rotate.** Choose and record ONE:
  (a) **defer** — record a coordinated-rotation TODO (rotate in lockstep with updating THIS cohort-tracker repo's `.env`/droplet config so its jobs don't break); OR
  (b) **separate scraper DB user** — provision a new managed-DB user scoped for the scraper and point `DATABASE_URL` at it (non-breaking for cohort-tracker), value written on-box only.
Record the chosen path + the explicit reason "shared defaultdb — blind rotate would break cohort-tracker jobs". If MB_DB_* targets the same cluster, apply the same caveat.

Restart of the affected containers to pick up the new env happens in Task 3 (the rebuild/recreate), so the new secrets take effect with the redeploy. Record into `## Fix 4 — secret rotation`: which keys were rotated, which were coordinate-or-deferred (and why), the `.env` backup path, and that NO value was written anywhere.
  </action>
  <acceptance_criteria>
    - `## Fix 4` records: DJANGO_SECRET_KEY rotated (value never recorded); OXYLABS_* rotated to a DEDICATED droplet sub-user ported post-verified-clean (never the shared cohort-tracker creds)
    - DATABASE_URL (and MB_DB_* if same cluster) explicitly recorded as coordinate-or-defer / separate-scraper-user — NOT blind-rotated — with the shared-`defaultdb` reason stated
    - `.env` backed up to `/root/24-backup/env.pre-24-05.bak` (revert path) BEFORE any change
    - NO secret VALUE appears anywhere in the doc (key names + "rotated/deferred" facts only)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "f=.planning/phases/24-cleanup/24-05-HARDENING.md; grep -qiE 'DJANGO_SECRET_KEY' \"$f\" && grep -qiE 'oxylabs.*(sub-user|dedicated|rotat)' \"$f\" && grep -qiE 'DATABASE_URL' \"$f\" && grep -qiE 'shared|defaultdb|coordinate|defer|separate.*user' \"$f\" && grep -qiE 'verified.clean|post-verified' \"$f\" && grep -qiE 'env.pre-24-05.bak|\\.env.*backup' \"$f\" && ! grep -niE '(OXYLABS_[A-Z]+|DJANGO_SECRET_KEY|MB_DB_[A-Z]+|DATABASE_URL)[=: ]+[^<[:space:]]' \"$f\""</automated>
  </verify>
  <done>The freely-rotatable secrets (DJANGO_SECRET_KEY + a dedicated Oxylabs sub-user) are rotated and ported onto the now-verified-clean box, the shared-`defaultdb` DB creds are recorded as coordinate-or-defer (never blind-rotated, protecting the cohort-tracker's jobs), `.env` is backed up, and no secret value is written anywhere.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Rebuild + redeploy reversibly + re-verify the scraper green + document revert + update remediation doc</name>
  <read_first>
    - `23-02-PLAN.md` (the EXACT rebuild → recreate → bounded verification-crawl pattern to mirror: `docker compose build <svc>` + `docker compose up -d --no-deps <svc>`, ~200-page crawl, ~0 403s, report calls+cost, disturb-nothing check)
    - `24-05-HARDENING.md` `## Feature branch + prior-image tag` (Task 1) + `## Fix 4` (Task 2 — new secrets now on box)
    - `24-RESEARCH.md` §"Verification" (CLEAN-03 running-set: 6 hemnet containers Up, Playwright gated off) ; `docs/price-scraper-droplet-remediation.md` (24-04's "team actions recommended" list this task marks closed)
  </read_first>
  <what-built>
    Only the affected `hemnet` image is rebuilt from the `feat/p24-durable-hardening` branch (prior tag kept for one-command rollback) and only the affected container(s) recreated, the new secrets are in effect, the Hemnet scraper is re-verified GREEN via a bounded operator-pre-approved Oxylabs crawl (~0 403s), the 6 hemnet containers are Up (Playwright still gated off), and the remediation doc records the durable-hardening items closed in 24-05 with an exact revert recipe.
  </what-built>
  <action>
OPERATOR-GATED (rebuild a prod image + recreate containers + ONE pre-approved PAID Oxylabs crawl).

1. **Tag the prior image for rollback BEFORE rebuilding:**
```bash
$DROPLET 'docker tag $(docker images -q hemnet:latest 2>/dev/null || docker images --format "{{.Repository}}:{{.Tag}}" | grep -m1 hemnet) hemnet:pre-24-05 && docker images | grep hemnet'
```
2. **Rebuild ONLY the affected image + recreate ONLY the affected container(s)** (mirror 23-02; capture pre-state so the disturb-nothing check is provable):
```bash
$DROPLET 'docker ps --format "{{.Names}}\t{{.Status}}\t{{.CreatedAt}}"'   # pre-state
$DROPLET 'cd /var/www/apps/hemnet && git branch --show-current'           # == feat/p24-durable-hardening
$DROPLET 'cd /var/www/apps/hemnet && docker compose build <affected-svc>'  # ONLY the image touched by the hardening edits
$DROPLET 'cd /var/www/apps/hemnet && docker compose up -d --no-deps <affected-svc(s)>'   # --no-deps: do NOT recreate redis/booli/etc.
$DROPLET 'docker logs --since 3m <affected-container> 2>&1 | tail -30'     # clean start, no Traceback/ImportError
```
For the Metabase upgrade (if done in Task 1), recreate `metabase` only; watch its logs for a clean store migration; if it fails, roll back the image tag (revert recipe) and fall to the firewalled-interim control.
3. **Confirm nothing else disturbed:** redis/beat/django/writer/booli + the stopped `hemnet-crawler-playwright` retain their original `CreatedAt` (only the affected container recreated); 6 hemnet containers `Up`, Playwright still Exited.
4. **Re-verify the Hemnet scraper is GREEN (bounded, operator-pre-approved, hard-capped — the ONE paid run):** trigger a bounded Hemnet pricing-page crawl through the Oxylabs path (manual trigger; do NOT re-enable the beat schedule), count pages / 403s / 200s, and capture the Oxylabs call count + cost. Expect ~0 403s (<=~1%). Keep `OXYLABS_*` values out of the report. If the 403 rate is NOT ~0, STOP — roll back to `hemnet:pre-24-05` and record the failure (the hardening regressed the fetch; fall to firewalled-interim or split to a follow-up phase).
5. **Document the exact revert recipe** into `## Revert recipe`: e.g. `git checkout main` (or `git revert <per-fix-sha>` to roll back ONE fix, since each fix is its own commit — W2; or `git checkout -- <files>`) on the droplet checkout; `docker tag hemnet:pre-24-05 hemnet:latest && docker compose up -d --no-deps <svc>` to roll the image back; `cp -a /root/24-backup/env.pre-24-05.bak /var/www/apps/hemnet/.env` to restore secrets; `cp -a /root/24-backup/daemon.json.bak /etc/docker/daemon.json && systemctl restart docker` (full bounce) if daemon.json was changed. Plus the per-fix firewall-interim fallbacks and the "split-to-follow-up-phase" option.
6. **Update `docs/price-scraper-droplet-remediation.md`:** mark the durable-hardening items (entry-vector source fix, runserver/DEBUG, Metabase upgrade, `.env` rotation) as **DONE in Phase 24-05** (or "kept firewalled-interim + TODO" where the escape hatch was taken), pointing to `24-05-HARDENING.md`. Note the DB-cred coordinate-or-defer status. No secret values.

Record into `## Rebuild + re-verify crawl`: the prior-tag, the build+recreate commands, the disturb-nothing `docker ps`, the crawl headline (N pages, 403 rate, Oxylabs calls + cost), and the rollback availability.
  </action>
  <how-to-verify>
    1. SSH in. `docker images | grep hemnet` shows both the rebuilt image and `hemnet:pre-24-05` (rollback tag present).
    2. `docker ps --format '{{.Names}}\t{{.Status}}\t{{.CreatedAt}}'` shows the affected container with a fresh CreatedAt and `Up`; redis/beat/django/writer retain ORIGINAL CreatedAt; `hemnet-crawler-playwright` still Exited.
    3. `docker exec <affected-container> ...` / `docker logs --since 5m <affected-container>` shows a clean start (no Traceback).
    4. Open `24-05-HARDENING.md` `## Rebuild + re-verify crawl`: N pages crawled, 403 rate ~0, Oxylabs calls + cost stated; `## Revert recipe` present; no secret values.
    5. `docs/price-scraper-droplet-remediation.md` marks the hardening items closed in 24-05.
  </how-to-verify>
  <acceptance_criteria>
    - `hemnet:pre-24-05` rollback tag exists BEFORE the rebuild; only the affected image rebuilt; only the affected container(s) recreated (`--no-deps`); redis/beat/django/writer + stopped Playwright retain original CreatedAt; 6 hemnet containers `Up`, Playwright still Exited
    - `## Rebuild + re-verify crawl` records a bounded Oxylabs crawl with N pages, a ~0 403 rate (<=~1%), and the Oxylabs call count + estimated cost; if 403 rate was not ~0, a rollback to `hemnet:pre-24-05` is recorded instead
    - `## Revert recipe` documents the exact rollback (branch checkout + `hemnet:pre-24-05` retag + `.env` restore from `env.pre-24-05.bak` + daemon.json restore) AND the per-fix firewall-interim / split-to-follow-up escape hatches
    - `docs/price-scraper-droplet-remediation.md` marks the entry-vector source fix, runserver/DEBUG, Metabase, and `.env` rotation as DONE-in-24-05 (or firewalled-interim) referencing `24-05-HARDENING.md`; DB-cred coordinate-or-defer status noted
    - NO secret value in either doc; team `main` untouched
  </acceptance_criteria>
  <verify>
    <automated>bash -c "f=.planning/phases/24-cleanup/24-05-HARDENING.md; r=docs/price-scraper-droplet-remediation.md; grep -qiE 'pre-24-05' \"$f\" && grep -qiE '403' \"$f\" && grep -qiE 'oxylabs|cost|call' \"$f\" && grep -qiE 'hemnet.*Up|6 hemnet|containers Up' \"$f\" && grep -qiE 'playwright.*(exited|stopped|gated)' \"$f\" && grep -q '## Revert recipe' \"$f\" && grep -qiE 'env.pre-24-05.bak|rollback|git checkout' \"$f\" && grep -qiE '24-05' \"$r\" && grep -qiE 'DONE|firewalled-interim|hardening' \"$r\" && ! grep -niE '(OXYLABS_[A-Z]+|DJANGO_SECRET_KEY|MB_DB_[A-Z]+|DATABASE_URL)[=: ]+[^<[:space:]]' \"$f\" \"$r\""</automated>
  </verify>
  <resume-signal>Type "approved" once `docker images` shows the rebuilt image + the `hemnet:pre-24-05` rollback tag, the bounded re-verify crawl shows ~0 403s with Oxylabs cost stated, the 6 hemnet containers are Up (Playwright still Exited), and the revert recipe + remediation-doc update are recorded — or describe the 403 result / any deviation.</resume-signal>
  <done>The affected image is rebuilt from the feature branch (prior tag kept), only the affected container(s) recreated with the new secrets, the Hemnet scraper is re-verified green (~0 403s through Oxylabs) with the 6 hemnet containers Up and Playwright still gated off, an exact revert recipe is documented, and the remediation doc records the durable-hardening items closed in 24-05 — all reversibly, with zero secret values and team main untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → droplet listening services | The diagnosed entry vector is now closed AT THE SOURCE (not only the firewall) |
| operator workstation → live prod droplet + scraper repo (SSH) | Rebuild/redeploy + secret rotation on a live shared box; a branch error could hit team `main` |
| `.env` / shared managed `defaultdb` → cohort-tracker | The DB creds are shared — a blind rotate breaks THIS repo's jobs |
| evidence/docs/commit → secrets | `OXYLABS_*` / `DJANGO_SECRET_KEY` / DB creds must never be written down |

## STRIDE Threat Register (extends 24-RESEARCH §"Security Domain"; IDs continue from 24-04 T-24-19)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-20 | Elevation of Privilege / Tampering | exposed Docker daemon API (if that was the vector) | mitigate | Bind daemon to unix socket only via host `/etc/docker/daemon.json` (source fix); DO Cloud Firewall deny kept as defense-in-depth |
| T-24-21 | Tampering / EoP | host-published unauthenticated Redis (if that was the vector) | mitigate | Scope `hemnet-redis` to the docker-internal network + `requirepass` in compose (source fix); firewall-deny 6379 stays |
| T-24-22 | Information Disclosure / EoP | `django runserver` + `DEBUG=True` on :8000 | mitigate | Replace with gunicorn/uvicorn + `DEBUG=False`; OR :8000 kept firewalled-interim with a TODO (escape hatch) |
| T-24-23 | Elevation of Privilege | old Metabase v0.47.1 (CVE-2023-38646 pre-auth RCE class) on :3000 | mitigate | Upgrade to a current pinned image (with shared-Postgres migration caution); OR :3000 kept firewalled-interim |
| T-24-24 | Information Disclosure | `.env` secrets potentially exfiltrated during the long compromise | mitigate | Rotate DJANGO_SECRET_KEY + a dedicated rotatable Oxylabs sub-user (ported only post-verified-clean) |
| T-24-25 | Denial of Service (self-inflicted) | rebuild/redeploy destabilizes the scraper right before the Phase-25 resize | mitigate | Prior `hemnet:pre-24-05` tag for one-command rollback; rebuild only the affected image; `--no-deps` recreate; re-verify crawl gate; per-fix firewalled-interim + split-to-follow-up escape hatch |
| T-24-26 | Information Disclosure | secret VALUES leaking into committed docs / the branch | mitigate | Names-only + `<REDACTED>`; redaction grep gate on every task; creds written on-box only, never echoed/committed |
| T-24-27 | Tampering | blind rotation of the shared `defaultdb` DB creds breaks the cohort-tracker's own jobs | mitigate | `DATABASE_URL`/`MB_DB_*` treated as coordinate-or-defer (or a separate scraper DB user); NEVER blind-rotated |
</threat_model>

<verification>
- `24-05-HARDENING.md` records the verified-clean precondition, a `feat/…` branch (main untouched), and per-fix decisions (source fix OR firewalled-interim) for the diagnosed vector, runserver/DEBUG, and Metabase — branching on the 24-01/24-02 finding, not assuming.
- Secret rotation: DJANGO_SECRET_KEY + a dedicated Oxylabs sub-user rotated/ported post-verified-clean; shared-`defaultdb` DB creds coordinate-or-deferred; `.env` backed up; no secret value anywhere.
- Rebuild reversible (`hemnet:pre-24-05` tag), only the affected image/container touched, 6 hemnet containers Up + Playwright gated off, re-verify crawl ~0 403s with cost reported, exact revert recipe documented.
- `docs/price-scraper-droplet-remediation.md` marks the durable-hardening items closed in 24-05 (or firewalled-interim).
- Grep gates green: no secret values, no DROP TABLE, no live-image prune.
</verification>

<success_criteria>
CLEAN-04's durable closure is complete: the diagnosed entry vector is fixed at the source (not only the firewall), the dev-server and Metabase exposures are hardened (or recorded as firewalled-interim), the exposed `.env` secrets are rotated (with the shared-`defaultdb` DB creds protected via coordinate-or-defer and a dedicated rotatable Oxylabs sub-user ported only now that the box is verified clean), and the Hemnet scraper is re-verified green through Oxylabs after a reversible rebuild — with a prior-image rollback tag, an exact revert recipe, and per-fix firewalled-interim / split-to-follow-up escape hatches, all on a feature branch with team `main` untouched and zero secret values written down.
</success_criteria>

<output>
After completion, create `.planning/phases/24-cleanup/24-05-SUMMARY.md` — include the feature branch + commit SHA, the per-fix source-vs-firewalled-interim decisions, the secret-rotation record (names only + the DB coordinate-or-defer status), the re-verify crawl headline (N pages, 403 rate, Oxylabs calls + cost), and the exact revert recipe.
</output>
