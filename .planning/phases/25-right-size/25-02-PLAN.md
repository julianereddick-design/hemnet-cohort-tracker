---
phase: 25-right-size
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - .planning/phases/25-right-size/25-VERIFICATION.md
autonomous: false
requirements: [SIZE-01]
user_setup: []

must_haves:
  truths:
    - "A real Oxylabs crawl (N>=200, Metabase gated) was run on the current box and PEAK RAM under load was captured, not just idle steady-state"
    - "The target slug is chosen from measured evidence: peak < ~1.5 GiB -> s-1vcpu-2gb (~$12/mo); otherwise fallback s-2vcpu-4gb (~$24/mo)"
    - "No Oxylabs crawl ran without explicit per-run operator approval"
  artifacts:
    - path: ".planning/phases/25-right-size/25-VERIFICATION.md"
      provides: "Peak-RAM profile (docker stats / free sampling) + recorded slug decision with the peak figure that justifies it"
      contains: "peak"
  key_links:
    - from: "Oxylabs crawl via fetch_via_webscraper (crawler container)"
      to: "peak RAM measurement (docker stats --no-stream sampling)"
      via: "RAM sampled repeatedly across the crawl, max recorded"
      pattern: "peak.*GiB"
    - from: "measured peak RAM"
      to: "chosen slug (s-1vcpu-2gb | s-2vcpu-4gb)"
      via: "D-03 guardrail threshold ~1.5 GiB"
      pattern: "s-(1vcpu-2gb|2vcpu-4gb)"
---

<objective>
Run the D-03 guardrail: ONE bounded operator-approved Oxylabs crawl (N>=200 pages) with Metabase gated, sampling RAM throughout to capture the **peak** demand under load (not just idle steady-state). Then make the slug decision from that measured peak — proceed to `s-1vcpu-2gb` only if peak fits comfortably with headroom (peak < ~1.5 GiB); otherwise fall back to `s-2vcpu-4gb`.

Purpose: a monthly crawler's RAM ceiling is set by the crawl burst (concurrent `fetch_via_webscraper` workers + writer + django), not by idle. Measuring peak on the still-large 16 GB box de-risks committing to a 2 GB target before the irreversible-feeling power-cycle.
Output: Peak-RAM profile + the recorded slug decision appended to `25-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-right-size/25-CONTEXT.md
@.planning/phases/24-cleanup/24-05-HARDENING.md

<facts>
- SSH: `ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89`
- Verification/crawl harness: the real `fetch_via_webscraper()` path in `apps/core/webscraper.py` on recent `ListingV2` Hemnet URLs, concurrency 5 — the SAME harness Phase 24-05 used (50/50 OK, 0 HTTP-403, ~$0.63 for ~251 calls). The 24-05 verifier scripts were removed from the box after use; re-stage the same bounded harness.
- Success signal per page = HTTP 200 + `__NEXT_DATA__` present. IGNORE the embedded `cdn-cgi/challenge-platform` script (present on normal pages — caused a 24-05 false "100% blocked"); key on `__NEXT_DATA__`.
- Oxylabs creds on the box are the cohort-tracker Web Scraper creds ported in 24-05 (operator-owned, box verified-clean). ~200 pages ≈ ~$0.50-0.63.
- This crawl runs PRE-resize on the 16 GB box — it cannot OOM; it only measures peak RAM demand to predict 2 GB fit.
</facts>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator-approved peak-RAM profiling crawl (D-03)</name>
  <read_first>
    - .planning/phases/24-cleanup/24-05-HARDENING.md ("Rebuild + re-verify crawl" — the fetch_via_webscraper harness, concurrency 5, __NEXT_DATA__ keying, the cdn-cgi false-positive lesson)
    - .planning/phases/25-right-size/25-CONTEXT.md (D-03 guardrail + "No Oxylabs crawl without per-run operator approval; N>=200 default")
    - .planning/phases/25-right-size/25-01-SUMMARY.md (confirms Metabase already gated, the precondition for this profile)
  </read_first>
  <what-built>
    A bounded Oxylabs crawl harness (re-staged from the 24-05 pattern) that drives the real `fetch_via_webscraper()` over N>=200 recent Hemnet `ListingV2` URLs at concurrency 5, while a parallel sampler records peak RAM.
  </what-built>
  <action>
    OPERATOR APPROVAL GATE (D-06 + N>=200 rule): this task issues paid Oxylabs calls and MUST NOT run without explicit per-run go-ahead. Surface this prompt and WAIT:
      "Approve the D-03 peak-RAM profiling crawl? ~200 Hemnet pages via Oxylabs (cohort-tracker Web Scraper creds), ~$0.50-0.63, Metabase gated, on the current 16 GB box. Reply 'approved' to run."
    On approval, SSH in and:
    1. Confirm Metabase is still gated: `docker ps --format '{{.Names}}' | grep -c metabase` = 0.
    2. Start a background RAM sampler for the crawl duration, e.g.:
       `( for i in $(seq 1 120); do date -u +%H:%M:%S; free -m | awk '/Mem:/{print "used_MiB="$3}'; docker stats --no-stream --format '{{.Name}} {{.MemUsage}}'; sleep 3; done ) > /root/25-ramprofile.txt 2>&1 &`
    3. Re-stage and run the bounded `fetch_via_webscraper` harness over N>=200 recent ListingV2 URLs at concurrency 5 (exact page count is planner/operator discretion within the N>=200 floor), keying success on HTTP 200 + `__NEXT_DATA__` (ignore `cdn-cgi/challenge-platform`).
    4. After the crawl completes, stop the sampler and compute PEAK: `sort -t= -k2 -n /root/25-ramprofile.txt | tail` (max `used_MiB`) and note the peak `hemnet-crawler`/`hemnet-django`/`hemnet-writer` MemUsage lines.
    5. Remove the re-staged verifier script from the box afterward (24-05 hygiene). Append the peak figure, the per-container peak lines, crawl OK/403 counts, and Oxylabs cost to `25-VERIFICATION.md` under `## D-03 peak-RAM profile`. Never log credential values.
  </action>
  <acceptance_criteria>
    - Crawl ran only after explicit operator "approved".
    - N>=200 pages attempted; report shows the OK count and **0 HTTP-403** (and 0 auth-401), keyed on `__NEXT_DATA__`.
    - `/root/25-ramprofile.txt` exists with repeated samples spanning the crawl; the recorded PEAK `used_MiB` is captured in `25-VERIFICATION.md`.
    - Re-staged verifier script removed from the droplet after the run.
    - Oxylabs cost (~$0.50-0.63) recorded; no credential value written anywhere.
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize the Oxylabs profiling crawl, or describe concerns.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Slug decision from measured peak (D-01 / D-03 fallback)</name>
  <read_first>
    - .planning/phases/25-right-size/25-VERIFICATION.md (the `## D-03 peak-RAM profile` peak figure just recorded)
    - .planning/phases/25-right-size/25-CONTEXT.md (D-01 target s-1vcpu-2gb, D-03 fallback s-2vcpu-4gb, threshold "peak < ~1.5 GiB with headroom")
  </read_first>
  <decision>Which slug to resize to, given the measured peak RAM under load.</decision>
  <context>
    D-01 targets `s-1vcpu-2gb` (~$12/mo, ~8x cheaper). The workload is a once-a-month crawler storing ~200 data points, so CPU is irrelevant; RAM peak is the only binding constraint. D-03 says proceed to 2 GB only if the measured peak fits comfortably with headroom; otherwise drop to the 4 GB fallback.
  </context>
  <action>
    Read the recorded PEAK `used_MiB` from Task 1. Apply the D-03 rule and RECORD the chosen slug + the peak figure that justifies it in `25-VERIFICATION.md` under `## Slug decision`:
      - PEAK < ~1.5 GiB (≈1536 MiB) with headroom under 2 GiB → choose **`s-1vcpu-2gb`** (D-01).
      - PEAK >= ~1.5 GiB (no comfortable 2 GiB headroom) → choose **`s-2vcpu-4gb`** (D-03 fallback).
    Surface the recommendation + peak evidence to the operator and confirm the slug before plan 25-03 executes the resize.
  </action>
  <acceptance_criteria>
    - `25-VERIFICATION.md` has a `## Slug decision` line naming exactly one of `s-1vcpu-2gb` or `s-2vcpu-4gb` and citing the measured peak `used_MiB`.
    - The decision follows the D-03 threshold (2 GB only if peak < ~1.5 GiB with headroom).
    - Chosen slug is confirmed by the operator (carried into 25-03 as the resize target).
  </acceptance_criteria>
  <resume-signal>Confirm the chosen slug: "s-1vcpu-2gb" or "s-2vcpu-4gb".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| droplet → Oxylabs API | borrowed cohort-tracker Web Scraper creds cross here |
| operator → paid-spend authorization | per-run approval gate on Oxylabs cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Info disclosure | Borrowed Oxylabs creds in crawl harness | mitigate | creds read from droplet `.env` only, never printed/logged/committed; re-staged verifier script removed after the run (24-05 hygiene) |
| T-25-05 | Repudiation/abuse | Unapproved paid Oxylabs spend | mitigate | blocking operator-approval gate before the crawl (D-06); N>=200 bounded; cost recorded |
| T-25-06 | Tampering | Wrong slug chosen → under-provisioned box OOMs the scraper | mitigate | decision is data-driven from measured PEAK (not idle) with the ~1.5 GiB headroom rule; fallback slug path explicit |
</threat_model>

<verification>
- Crawl report: N>=200 attempted, 0 HTTP-403, `__NEXT_DATA__`-keyed OK count.
- `/root/25-ramprofile.txt` peak `used_MiB` recorded in `25-VERIFICATION.md`.
- `## Slug decision` names exactly one slug, justified by the peak figure.
</verification>

<success_criteria>
- Peak RAM under load measured and recorded (D-03 guardrail honored).
- Slug chosen by the D-03 rule and operator-confirmed.
- No unapproved Oxylabs spend.
</success_criteria>

<output>
After completion, create `.planning/phases/25-right-size/25-02-SUMMARY.md`.
</output>
