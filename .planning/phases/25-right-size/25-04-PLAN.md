---
phase: 25-right-size
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - .planning/phases/25-right-size/25-VERIFICATION.md
autonomous: false
requirements: [SIZE-02]
user_setup: []

must_haves:
  truths:
    - "After the power-cycle SSH still works and the 127.0.0.1 bind held — :3000/:8000 are still closed off-box (reboot did NOT re-expose the entry vector)"
    - "On the smaller slug all 6 Hemnet containers are healthy/Up, Metabase stays gated (Exited), and the box does not OOM under a real crawl"
    - "A bounded Oxylabs verification crawl (~200 pages) returns HTTP 200 + __NEXT_DATA__ with 0% HTTP-403 at the smaller size (D-05 green bar)"
    - "If the box is blocked/unhealthy/OOMs, the documented rollback is a reversible re-resize back up"
  artifacts:
    - path: ".planning/phases/25-right-size/25-VERIFICATION.md"
      provides: "Post-resize green-bar evidence: container health, off-box bind probe, crawl 403 rate, RAM-under-load, GREEN/ROLLBACK verdict"
      contains: "0 HTTP-403"
  key_links:
    - from: "fetch_via_webscraper crawl on the resized box"
      to: "HTTP 200 + __NEXT_DATA__, 0% 403"
      via: "bounded ~200-page Oxylabs crawl keyed on __NEXT_DATA__"
      pattern: "0 .*403"
    - from: "post-reboot off-box probe"
      to: ":3000/:8000 still closed"
      via: "loopback bind survived the power-cycle"
      pattern: "(3000|8000).*closed"
---

<objective>
Prove the D-05 green bar at the smaller size (SIZE-02): SSH survived the power-cycle, the `127.0.0.1` bind held (no :3000/:8000 re-exposure post-reboot), all 6 Hemnet containers are healthy with Metabase still gated, and a bounded operator-approved Oxylabs verification crawl (~200 pages) returns HTTP 200 + `__NEXT_DATA__` with 0% HTTP-403 — without the box OOMing. If any of that fails, execute the reversible rollback (re-resize up).

Purpose: closes SIZE-02 — the scraper still runs correctly and reaches Hemnet via Oxylabs at the reduced slug. The rollback path makes the whole resize safe-by-construction.
Output: Green-bar evidence + GREEN/ROLLBACK verdict appended to `25-VERIFICATION.md`; phase-final state recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-right-size/25-CONTEXT.md
@.planning/phases/24-cleanup/24-05-HARDENING.md

<facts>
- SSH: `ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89`
- Verification crawl harness = the SAME `fetch_via_webscraper()` path used in 24-05 / 25-02: recent `ListingV2` Hemnet URLs, concurrency 5, success = HTTP 200 + `__NEXT_DATA__`. IGNORE `cdn-cgi/challenge-platform` (normal-page Cloudflare script; caused the 24-05 false "100% blocked"). ~200 pages ≈ ~$0.50-0.63.
- Expected post-resize containers: 6 hemnet Up (crawler/beat/writer/django/redis + Metabase — but Metabase must stay gated/Exited per D-02, so effectively 5 scraper containers Up + Metabase Exited). `hemnet-crawler-playwright` remains Exited (P23).
- D-05 rollback = reversible re-resize back up (`doctl compute droplet-action resize 357087018 --size s-2vcpu-4gb --resize-disk=false` or `s-8vcpu-16gb`), possible because 25-03 preserved the disk. Same power-off → resize → power-on order, with the write token.
- After GREEN, the operator revokes the write-scoped doctl token (T-25-07 follow-through).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-reboot infra health + bind-survival check</name>
  <read_first>
    - docs/price-scraper-droplet-runbook.md (SSH gotcha; access-survives-reboot rationale)
    - .planning/phases/24-cleanup/24-05-HARDENING.md (the loopback bind is the durable reboot-safe protection; 24-02 iptables DROPs were NOT reboot-persistent)
    - .planning/phases/25-right-size/25-VERIFICATION.md (the 25-03 resize result + chosen slug)
  </read_first>
  <action>
    On the resized box, confirm the reboot left it healthy AND secure:
    1. SSH survived: `ssh ... 'hostname && uptime'` returns with fresh post-reboot uptime.
    2. D-07 POST-reboot bind-survival (HIGH severity): off-box probe `170.64.181.89` ports 22/3000/8000 → **22 OPEN, 3000 + 8000 closed/filtered**. The loopback compose bind (commit ed7192c) is in-config so it must survive; the 24-02 host-iptables DROPs are NOT reboot-persistent, so this probe is the real test that the bind held. On-box cross-check: `docker ps --format '{{.Names}} {{.Ports}}' | grep -E "metabase|django"` shows `127.0.0.1:` prefixes.
    3. Container health: `docker ps --format '{{.Names}} {{.Status}}'` → crawler/beat/writer/django/redis `Up`; `hemnet-metabase` Exited (still gated, D-02); `hemnet-crawler-playwright` Exited (P23). If Metabase auto-started on reboot, re-gate it (`docker stop hemnet-metabase`) and note it.
    4. Headroom snapshot: `free -h` and `df -h /` (disk still 50 G; idle RAM well within the new slug).
    Append all to `25-VERIFICATION.md` under `## Post-resize infra health`. If SSH fails OR 3000/8000 are OPEN OR scraper containers are not Up → flag for the rollback decision in Task 2.
  </action>
  <acceptance_criteria>
    - SSH returns `hostname`/`uptime` with fresh uptime (box rebooted onto the smaller slug).
    - Off-box probe: 22 OPEN; 3000 + 8000 closed/filtered (bind survived the reboot).
    - `docker ps`: crawler/beat/writer/django/redis `Up`; `hemnet-metabase` Exited; `hemnet-crawler-playwright` Exited.
    - `df -h /` still 50 G; `free -h` idle usage within the new slug.
    - Evidence appended under `## Post-resize infra health`.
  </acceptance_criteria>
  <files>.planning/phases/25-right-size/25-VERIFICATION.md</files>
  <verify>
    <automated>ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89 'docker ps --format "{{.Names}} {{.Status}}"'</automated>
  </verify>
  <done>Box is reachable, secure (3000/8000 closed post-reboot), scraper containers healthy, Metabase still gated, disk preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator-approved Oxylabs verification crawl + GREEN/ROLLBACK verdict (D-05)</name>
  <read_first>
    - .planning/phases/24-cleanup/24-05-HARDENING.md ("Rebuild + re-verify crawl": fetch_via_webscraper, concurrency 5, __NEXT_DATA__ keying, cdn-cgi false-positive)
    - .planning/phases/25-right-size/25-CONTEXT.md (D-05 green bar + rollback = reversible re-resize up; "No Oxylabs crawl without per-run approval; N>=200")
    - .planning/phases/25-right-size/25-VERIFICATION.md (Task 1 health result; the chosen slug for the rollback target)
  </read_first>
  <what-built>
    The same bounded `fetch_via_webscraper` crawl harness, now run ON the resized smaller box, with a concurrent RAM watch to confirm it doesn't OOM under load.
  </what-built>
  <action>
    OPERATOR APPROVAL GATE (D-06 + N>=200): paid Oxylabs crawl — surface and WAIT:
      "Approve the post-resize verification crawl? ~200 Hemnet pages via Oxylabs on the resized <slug> box, ~$0.50-0.63. This is the SIZE-02 green bar. Reply 'approved' to run."
    On approval, SSH in and:
    1. Start a RAM watch (`free -m` sampler as in 25-02) so an OOM/near-OOM under load is visible on the smaller slug.
    2. Re-stage and run the bounded harness over N>=200 recent ListingV2 URLs at concurrency 5; success = HTTP 200 + `__NEXT_DATA__` (ignore `cdn-cgi/challenge-platform`); tally OK / HTTP-403 / auth-401.
    3. Stop the sampler; confirm no OOM (no process killed; used RAM stayed within the slug; `dmesg | grep -i oom` empty).
    4. Remove the re-staged verifier script (24-05 hygiene).
    VERDICT:
      - **GREEN** if: 0 HTTP-403 (and 0 auth-401), `__NEXT_DATA__` present across the N>=200 pages, no OOM, and Task 1 health all passed → SIZE-02 met. Record GREEN + crawl stats + Oxylabs cost in `25-VERIFICATION.md` under `## D-05 verification — GREEN`. Note: operator should now revoke the write-scoped doctl token.
      - **ROLLBACK** if blocked (403s), box unhealthy, or OOM: execute the reversible re-resize UP (power-off → `doctl compute droplet-action resize 357087018 --size s-2vcpu-4gb --resize-disk=false --wait` (or `s-8vcpu-16gb`) → power-on, write token), re-verify the crawl green at the larger slug, and record `## D-05 verification — ROLLBACK` with the reason and the slug landed on.
    Never log credential or token values.
  </action>
  <acceptance_criteria>
    - Crawl ran only after explicit operator "approved".
    - N>=200 pages attempted; report shows **0 HTTP-403** and 0 auth-401, keyed on `__NEXT_DATA__` (GREEN path) OR a documented ROLLBACK with the re-resize executed and re-verified.
    - RAM watch shows no OOM on the smaller slug (`dmesg | grep -i oom` empty; no killed scraper process).
    - Re-staged verifier script removed from the droplet.
    - `25-VERIFICATION.md` records a `## D-05 verification` section with a GREEN or ROLLBACK verdict, crawl stats, and Oxylabs cost; GREEN note includes the token-revocation reminder.
  </acceptance_criteria>
  <resume-signal>Type "approved" to run the verification crawl, or describe concerns.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → droplet :3000/:8000 | reboot must not have re-exposed the Kinsing vector |
| droplet → Oxylabs API | borrowed cohort-tracker creds cross here |
| operator → DO write token | revoked after green (least-privilege follow-through) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-11 | Info disclosure | Reboot re-exposed :3000/:8000 | mitigate | Task 1 off-box probe is the explicit post-reboot test that the loopback bind held (the iptables interim was NOT reboot-persistent); HIGH-severity STOP→rollback if OPEN |
| T-25-12 | Info disclosure | Borrowed Oxylabs creds in crawl | mitigate | creds read from droplet `.env` only, never printed/logged; verifier script removed after run |
| T-25-13 | Denial of service | Smaller slug OOMs the scraper under load | mitigate | RAM watch + `dmesg` OOM check during the crawl; ROLLBACK = reversible re-resize up (disk preserved by 25-03) |
| T-25-14 | Elevation | Write-scoped doctl token lingering | mitigate | GREEN verdict records the operator token-revocation step (closes T-25-07) |
</threat_model>

<verification>
- Off-box probe: 22 OPEN, 3000 + 8000 closed post-reboot.
- `docker ps`: scraper containers Up, Metabase Exited.
- Crawl: N>=200, 0 HTTP-403, `__NEXT_DATA__` present; `dmesg | grep -i oom` empty.
- `25-VERIFICATION.md` carries a GREEN (or ROLLBACK) verdict.
</verification>

<success_criteria>
- SIZE-02 met: scraper runs correctly and reaches Hemnet via Oxylabs (0% 403) at the smaller slug, no OOM.
- Loopback bind survived the reboot (no entry-vector re-exposure).
- GREEN recorded, or a documented reversible rollback up + re-verified green.
- Write token revocation noted for the operator.
</success_criteria>

<output>
After completion, create `.planning/phases/25-right-size/25-04-SUMMARY.md`.
</output>
