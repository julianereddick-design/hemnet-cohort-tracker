---
phase: 25-right-size
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-right-size/25-VERIFICATION.md
autonomous: true
requirements: [SIZE-01]
user_setup: []

must_haves:
  truths:
    - "The 127.0.0.1 compose bind (commit ed7192c) is the RUNNING state, so a reboot will NOT re-expose :3000/:8000"
    - "Metabase is gated off (container stopped) — the largest steady-state RAM consumer is removed"
    - "Steady-state RAM with Metabase gated is comfortably under ~1 GiB, evidencing a 2 GB slug can fit"
    - "The Metabase gate is reversible (one-line docker start revert recorded)"
  artifacts:
    - path: ".planning/phases/25-right-size/25-VERIFICATION.md"
      provides: "Pre-flight safety + Metabase gate-off evidence (commands + raw output)"
      contains: "127.0.0.1"
  key_links:
    - from: "droplet docker-compose.yml (commit ed7192c)"
      to: "running container port publishes :8000/:3000"
      via: "docker compose config + off-box port probe"
      pattern: "127.0.0.1:(3000|8000)"
    - from: "docker stop hemnet-metabase"
      to: "free -h steady-state used RAM"
      via: "RAM drops below ~1 GiB"
      pattern: "hemnet-metabase.*Exited"
---

<objective>
Establish the pre-flight safety state and unlock the smaller slug BEFORE any power-cycle: (1) confirm the Phase 24-05 `127.0.0.1` compose bind (commit `ed7192c`) is the running state so the upcoming reboot does not re-expose Metabase :3000 / django :8000 (D-07), and (2) gate Metabase to on-demand by stopping its container (D-02), then measure that steady-state RAM drops comfortably under ~1 GiB.

Purpose: D-07 is a HIGH-severity reboot-exposure guard — the interim 24-02 host-iptables DROPs are NOT reboot-persistent, so the loopback bind is the only durable protection across the resize power-cycle. D-02 removes the ~1.6 GiB Metabase steady-state consumer so an `s-1vcpu-2gb` target becomes viable.
Output: Appended evidence in `25-VERIFICATION.md` (bind confirmed, Metabase gated, steady-state RAM captured, revert recipe recorded).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-right-size/25-CONTEXT.md
@docs/price-scraper-droplet-runbook.md
@.planning/phases/24-cleanup/24-05-HARDENING.md
@.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-PLAYWRIGHT-GATEOFF.md

<facts>
- Droplet: `170.64.181.89`, DO ID `357087018`, region `syd1`, current slug `s-8vcpu-16gb`.
- SSH (always use the IdentitiesOnly gotcha or it false-fails):
  `ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89`
- Droplet checkout: `/var/www/apps/hemnet` (repo `tt7676/hem-bol-scrapers`). The `127.0.0.1` rebind is commit `ed7192c` on branch `feat/p24-durable-hardening`.
- Metabase container name: `hemnet-metabase` (the gate-off mirrors the Phase-23 `docker stop` Playwright recipe — STOP only, no `rm`/`down`/`prune`).
- Post-cleanup baseline (24-VERIFICATION): 6 hemnet containers Up + `hemnet-crawler-playwright` Exited (gated off); disk 22 G used / 50 G; RAM steady-state ~2.6 GiB used with Metabase ~1.6 GiB the largest remaining consumer.
- Off-box port probe (Windows workstation, `nc` absent) uses the bash `/dev/tcp` TCP-connect form, per 24-VERIFICATION precedent.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 pre-flight — confirm the 127.0.0.1 compose bind is the running state</name>
  <read_first>
    - docs/price-scraper-droplet-runbook.md (SSH IdentitiesOnly gotcha, doctl notes)
    - .planning/phases/24-cleanup/24-05-HARDENING.md (Fix 1: compose `127.0.0.1:8000`/`127.0.0.1:3000` rebind, commit ed7192c)
    - .planning/phases/24-cleanup/24-VERIFICATION.md (24-02 containment note: iptables DROPs NOT reboot-persistent; off-box probe form)
  </read_first>
  <action>
    SSH to the droplet with the IdentitiesOnly form above. Confirm the loopback bind is the RUNNING state, not just committed:
    1. `cd /var/www/apps/hemnet && git log --oneline -5 | grep ed7192c` — confirm commit `ed7192c` ("harden(p24): bind :8000/:3000 to 127.0.0.1") is in the checked-out history.
    2. `cd /var/www/apps/hemnet && docker compose config | grep -nE "127\.0\.0\.1:(3000|8000)"` — confirm BOTH `127.0.0.1:8000:8000` and `127.0.0.1:3000:3000` are the published forms (not `0.0.0.0`).
    3. On-box running state: `docker ps --format '{{.Names}} {{.Ports}}' | grep -E "metabase|django"` — confirm the live port mappings show `127.0.0.1:` prefixes, proving the running containers (not just the file) bind loopback.
    4. Off-box from the operator workstation, TCP-connect probe `170.64.181.89` ports 22 / 3000 / 8000 via the bash `/dev/tcp` form (per 24-VERIFICATION): expect **22 OPEN, 3000 closed/filtered, 8000 closed/filtered**.
    If any check fails (commit absent, ports show 0.0.0.0, or 3000/8000 OPEN off-box) — STOP and flag: the reboot would re-expose the Kinsing entry vector. Do not proceed to the gate-off or any later resize plan until the loopback bind is the running state.
    Append all commands + raw output to `.planning/phases/25-right-size/25-VERIFICATION.md` under a `## D-07 pre-flight — loopback bind` heading (no secret values).
  </action>
  <acceptance_criteria>
    - `git log` on the droplet shows commit `ed7192c` present.
    - `docker compose config` shows `127.0.0.1:8000:8000` AND `127.0.0.1:3000:3000` (zero `0.0.0.0:3000`/`0.0.0.0:8000` matches).
    - `docker ps` live port mappings for metabase + django show the `127.0.0.1:` prefix.
    - Off-box probe: port 22 OPEN; ports 3000 and 8000 both closed/filtered.
    - `25-VERIFICATION.md` contains a `## D-07 pre-flight — loopback bind` section with raw output.
  </acceptance_criteria>
  <files>.planning/phases/25-right-size/25-VERIFICATION.md</files>
  <verify>
    <automated>ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89 'cd /var/www/apps/hemnet && docker compose config | grep -cE "127\.0\.0\.1:(3000|8000)"'</automated>
  </verify>
  <done>Loopback bind confirmed as the running state (compose + live containers + off-box probe all agree); reboot will not re-expose :3000/:8000. Evidence appended.</done>
</task>

<task type="auto">
  <name>Task 2: Gate Metabase off (D-02) and capture steady-state RAM</name>
  <read_first>
    - .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-PLAYWRIGHT-GATEOFF.md (the reversible `docker stop` gate-off recipe + before/after RAM table to mirror)
    - .planning/phases/25-right-size/25-CONTEXT.md (D-02: gate Metabase to on-demand; Metabase ~1.6 GiB is the largest remaining consumer)
  </read_first>
  <action>
    Apply the same reversible gate-off pattern Phase 23 used for Playwright — STOP only, no removal:
    1. Capture BEFORE: `free -h` and `docker stats --no-stream --format 'table {{.Name}}\t{{.MemUsage}}'` (note `hemnet-metabase`'s usage, expected ~1.6 GiB).
    2. Gate off: `docker stop hemnet-metabase` (reversible; do NOT `rm`/`down`/`prune`/edit compose).
    3. Capture AFTER (let it settle ~30 s): `free -h` and `docker stats --no-stream`.
    4. Confirm state: `docker ps -a --format '{{.Names}} {{.Status}}' | grep metabase` → `hemnet-metabase ... Exited`; `docker ps --format '{{.Names}}' | grep -c metabase` → 0 running.
    5. Confirm the other 5 hemnet containers (crawler/beat/writer/django/redis) are still Up and `hemnet-crawler-playwright` remains Exited (gated off in P23) — nothing else disturbed.
    Record the one-line revert recipe: `docker start hemnet-metabase` (reach the UI on-demand via `ssh -L 3000:localhost:3000 …`). Append a BEFORE→AFTER RAM table + the revert recipe to `25-VERIFICATION.md` under `## D-02 Metabase gate-off`.
  </action>
  <acceptance_criteria>
    - `docker ps -a` shows `hemnet-metabase` in `Exited` state; running-metabase count = 0.
    - The 5 other hemnet containers (crawler/beat/writer/django/redis) remain `Up`; `hemnet-crawler-playwright` remains `Exited`.
    - `free -h` AFTER shows steady-state **used RAM under ~1 GiB** (down from ~2.6 GiB; Metabase ~1.6 GiB freed).
    - `25-VERIFICATION.md` records a BEFORE→AFTER RAM table and the `docker start hemnet-metabase` revert recipe.
  </acceptance_criteria>
  <files>.planning/phases/25-right-size/25-VERIFICATION.md</files>
  <verify>
    <automated>ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89 'docker ps --format "{{.Names}}" | grep -c metabase'</automated>
  </verify>
  <done>Metabase gated off (Exited), steady-state RAM under ~1 GiB, the 5 other hemnet containers undisturbed, revert recipe recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → droplet :3000/:8000 | Kinsing re-infection vector if Metabase/django are published on 0.0.0.0 |
| operator workstation → droplet (SSH) | privileged root admin channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Elevation/Info-disclosure | Metabase :3000 / django :8000 reboot re-exposure | mitigate | Task 1 verifies commit `ed7192c` loopback bind is the RUNNING state (compose config + live `docker ps` ports + off-box probe) BEFORE any later power-cycle; HIGH-severity gate — STOP if any check fails |
| T-25-02 | Denial of service | Metabase gate-off breaking the scraper | accept (reversible) | `docker stop` only (no rm/down); 5 scraper containers verified still Up; one-line `docker start` revert recorded — Metabase is not on the scrape path |
| T-25-03 | Spoofing | SSH key offering / false Permission-denied | mitigate | mandatory `IdentitiesOnly=yes -o IdentityAgent=none -i ~/.ssh/droplet_ed25519` per runbook |
</threat_model>

<verification>
- Off-box: `170.64.181.89` port 22 OPEN, 3000 + 8000 closed/filtered.
- On-box: `docker compose config` shows both `127.0.0.1:` publishes; `hemnet-metabase` Exited; 5 scraper containers Up.
- `free -h` steady-state used RAM under ~1 GiB.
</verification>

<success_criteria>
- D-07 loopback bind confirmed as running state (reboot-safe).
- Metabase gated off reversibly; steady-state RAM under ~1 GiB.
- Evidence appended to `25-VERIFICATION.md`; no later plan proceeds if the bind check failed.
</success_criteria>

<output>
After completion, create `.planning/phases/25-right-size/25-01-SUMMARY.md`.
</output>
