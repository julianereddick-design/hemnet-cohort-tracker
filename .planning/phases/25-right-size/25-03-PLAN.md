---
phase: 25-right-size
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - .planning/phases/25-right-size/25-VERIFICATION.md
autonomous: false
requirements: [SIZE-01]
user_setup:
  - service: digitalocean
    why: "Driving the resize requires a WRITE-scoped doctl token — the current token is read-only and 403s on droplet-action writes (D-06)"
    env_vars:
      - name: DIGITALOCEAN_ACCESS_TOKEN
        source: "DigitalOcean → API → Tokens/Keys → Generate New Token (write scope); used for this resize then revoked"
    dashboard_config:
      - task: "Generate a write-scoped Personal Access Token, provide it to the operator workstation for the resize, and revoke it after 25-04 verifies green"
        location: "DigitalOcean Control Panel → API → Personal access tokens"

must_haves:
  truths:
    - "A WRITE-scoped doctl token is available and proven (a write probe succeeds, not 403) before any power action"
    - "The 127.0.0.1 loopback bind is re-confirmed as the running state immediately before power-off (D-07 second checkpoint)"
    - "The droplet is power-cycled in the exact D-06 order: power-off -> reversible resize -> power-on"
    - "The resize is CPU/RAM-only and REVERSIBLE — the 50 G disk is preserved (no one-way disk-grow)"
    - "doctl confirms the new slug (Memory/VCPUs reduced, Disk still 50) and the box comes back reachable via SSH"
  artifacts:
    - path: ".planning/phases/25-right-size/25-VERIFICATION.md"
      provides: "Resize evidence: pre/post doctl droplet get, power-cycle log, SSH-back-up confirmation"
      contains: "doctl compute droplet get 357087018"
  key_links:
    - from: "doctl droplet-action resize 357087018 --size <slug>"
      to: "droplet Memory/VCPUs reduced, Disk unchanged at 50"
      via: "doctl compute droplet get 357087018 --format Memory,VCPUs,Disk"
      pattern: "(2048|4096)\\s+(1|2)\\s+50"
    - from: "power-on"
      to: "SSH reachable on smaller box"
      via: "ssh ... 'hostname && uptime'"
      pattern: "ubuntu-s-1vcpu-2gb-syd1-01"
---

<objective>
Execute the reversible right-size (D-06): with a write-scoped `doctl` token and explicit operator approval, power-off → resize droplet `357087018` to the slug chosen in 25-02 → power-on, keeping the existing 50 G disk (CPU/RAM-only, reversible — NOT the one-way disk-grow). Confirm the new slug via `doctl` and that the box returns reachable over SSH.

Purpose: this is the cost-cut action (SIZE-01) — ~$96/mo → ~$12/mo (or ~$24/mo fallback). Reversibility (keep disk) preserves the option to re-resize up if 25-04's verification fails.
Output: Pre/post `doctl` droplet state + power-cycle log appended to `25-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-right-size/25-CONTEXT.md
@docs/price-scraper-droplet-runbook.md

<facts>
- Droplet DO ID `357087018`, IP `170.64.181.89`, region `syd1`, current slug `s-8vcpu-16gb` (Memory 16384, VCPUs 8, Disk 50).
- Chosen target slug = the one recorded in `25-VERIFICATION.md` `## Slug decision` (25-02): `s-1vcpu-2gb` (default) or `s-2vcpu-4gb` (fallback).
- DigitalOcean **cannot resize a powered-on droplet** — power-off is required first. D-06 order: power-off → resize → power-on.
- **Reversible (CPU/RAM-only) resize = do NOT grow the disk.** `doctl compute droplet-action resize` defaults `--resize-disk` to false; pass it explicitly (`--resize-disk=false`) to be unambiguous. The one-way "full" resize that grows disk is FORBIDDEN (D-04).
- doctl auth: the operator provisions a write-scoped token for this plan (current read-only token 403s on writes, D-06). SSH gotcha per runbook still applies for the post-resize reachability check.
- Rollback (used by 25-04 if verification fails): re-run the same resize action back up to `s-2vcpu-4gb` or `s-8vcpu-16gb` — possible BECAUSE the disk was preserved.
</facts>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-power-cycle gate — write token proven + D-07 re-confirm + power-off approval</name>
  <read_first>
    - docs/price-scraper-droplet-runbook.md (doctl read-only-token limitation; resize/power notes; SSH gotcha)
    - .planning/phases/25-right-size/25-CONTEXT.md (D-06 write-token sequence + explicit approval before power-off; D-07 pre-flight)
    - .planning/phases/25-right-size/25-VERIFICATION.md (the chosen slug; the 25-01 D-07 bind confirmation to re-verify)
  </read_first>
  <what-built>
    Operator-provisioned write-scoped doctl token, a proof that it has write scope (not 403), and a final D-07 re-confirmation immediately before the power-cycle.
  </what-built>
  <action>
    OPERATOR APPROVAL GATE (D-06): power-off interrupts the live box. Do not power-off without explicit go-ahead. First verify readiness, then surface the prompt and WAIT.
    1. Confirm a write-scoped token is configured (`DIGITALOCEAN_ACCESS_TOKEN` from operator, or `doctl auth init` with the write token). Prove write scope WITHOUT mutating state yet — a read still works and the token is the new one: `doctl account get` succeeds. (Do not run a write probe that changes the droplet; the power-off itself in Task 2 is the first write — if the token were read-only it 403s there and Task 2 stops cleanly.)
    2. D-07 SECOND CHECKPOINT — re-confirm the loopback bind is STILL the running state right before the reboot: SSH in, `cd /var/www/apps/hemnet && docker compose config | grep -cE "127\.0\.0\.1:(3000|8000)"` = 2, and off-box probe `170.64.181.89` 3000/8000 still closed. If not closed → STOP (reboot would re-expose the entry vector).
    3. Capture PRE state: `doctl compute droplet get 357087018 --format ID,Name,Memory,VCPUs,Disk,Status` (expect `16384 8 50 active`).
    4. Surface and WAIT:
       "Approve the resize power-cycle of droplet 357087018 (170.64.181.89)? Sequence: power-off → reversible resize to <chosen slug> (keep 50 G disk) → power-on. Live containers will briefly stop. Reply 'approved' to proceed."
    Append the token-readiness + D-07 re-confirm + PRE doctl state to `25-VERIFICATION.md` under `## Pre-resize gate`. Never write the token value.
  </action>
  <acceptance_criteria>
    - `doctl account get` succeeds with the operator-provided token (token present and authenticating).
    - D-07 re-confirm: `docker compose config` loopback match count = 2; off-box 3000/8000 closed.
    - PRE `doctl ... get` recorded showing `16384 8 50 active`.
    - Chosen slug from 25-02 is restated in `## Pre-resize gate`.
    - Explicit operator "approved" captured before Task 2 runs.
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize the power-off → resize → power-on, or "hold".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Power-off → reversible resize → power-on → confirm new slug</name>
  <read_first>
    - docs/price-scraper-droplet-runbook.md (SSH gotcha for the post-resize reachability check)
    - .planning/phases/25-right-size/25-CONTEXT.md (D-04 reversible CPU/RAM-only; D-06 sequence)
  </read_first>
  <action>
    Execute the D-06 sequence against DO ID `357087018` with the write-scoped token. Let `SLUG` = the slug confirmed in Task 1 (`s-1vcpu-2gb` default, else `s-2vcpu-4gb`):
    1. Power off and WAIT for completion: `doctl compute droplet-action power-off 357087018 --wait`. (If this 403s, the token is still read-only — STOP, do not retry; report back for a write token.)
    2. Confirm off: `doctl compute droplet get 357087018 --format Status` → `off`.
    3. Reversible resize (CPU/RAM only, keep disk): `doctl compute droplet-action resize 357087018 --size "$SLUG" --resize-disk=false --wait`.
    4. Power on and WAIT: `doctl compute droplet-action power-on 357087018 --wait`.
    5. Confirm new slug: `doctl compute droplet get 357087018 --format ID,Name,Memory,VCPUs,Disk,Status` → Memory/VCPUs reduced to the chosen slug, **Disk still 50**, Status `active`.
    6. Confirm reachability: SSH in (IdentitiesOnly form) and run `hostname && uptime && free -h && df -h /` — box answers, uptime is fresh (just rebooted), Disk still 50 G.
    Append the power-cycle log + PRE/POST `doctl` state + the SSH-back-up output to `25-VERIFICATION.md` under `## Resize executed`. Do NOT take any disk-growing/full resize. Token revocation is handled by the operator after 25-04 verifies green (note this in the summary).
  </action>
  <acceptance_criteria>
    - `doctl compute droplet get 357087018 --format Memory,VCPUs,Disk` shows the chosen slug's Memory + VCPUs and **Disk = 50** (e.g. `2048  1  50` for s-1vcpu-2gb, or `4096  2  50` for the fallback).
    - Resize used `--resize-disk=false` (reversible); no disk-grow/full resize was issued.
    - Status `active` after power-on; SSH returns `hostname`/`uptime` with a fresh post-reboot uptime.
    - `df -h /` still shows a 50 G root disk.
    - `25-VERIFICATION.md` records the full power-cycle log + PRE/POST doctl state.
  </acceptance_criteria>
  <files>.planning/phases/25-right-size/25-VERIFICATION.md</files>
  <verify>
    <automated>doctl compute droplet get 357087018 --format Memory,VCPUs,Disk,Status</automated>
  </verify>
  <done>Droplet resized to the chosen smaller slug, disk preserved at 50 G, box back active and SSH-reachable; cost cut from ~$96/mo. Reversible (re-resize up still possible).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DO API (write token) | privileged infra-mutation credential crosses here |
| public internet → droplet :3000/:8000 | reboot must not re-expose the Kinsing vector |
| operator workstation → droplet (SSH) | must survive the power-cycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Elevation/Spoofing | Write-scoped doctl token | mitigate | least-privilege single-purpose token, operator-provisioned, never printed/committed, REVOKED after 25-04 green (user_setup records revocation) |
| T-25-08 | Info disclosure | Reboot re-exposing :3000/:8000 | mitigate | D-07 SECOND checkpoint in Task 1 re-confirms the loopback bind is the running state immediately before power-off; STOP if 3000/8000 not closed |
| T-25-09 | Denial of service | Irreversible disk-grow resize traps the box on a bigger slug | mitigate | `--resize-disk=false` (D-04) — CPU/RAM only; disk preserved so re-resize up remains possible |
| T-25-10 | Denial of service | SSH lockout after power-cycle | accept (low) | runbook: `authorized_keys` on persistent `/dev/vda1`, access survives reboot by construction; Task 2 verifies SSH returns post-power-on |
</threat_model>

<verification>
- `doctl compute droplet get 357087018` → chosen slug Memory/VCPUs, Disk 50, Status active.
- SSH `hostname && uptime` → box answers with fresh uptime.
- `df -h /` → 50 G root preserved.
</verification>

<success_criteria>
- Droplet resized down reversibly to the chosen slug (SIZE-01 cost cut).
- 50 G disk preserved; re-resize-up path intact.
- D-07 re-confirmed pre-reboot; box returns reachable.
</success_criteria>

<output>
After completion, create `.planning/phases/25-right-size/25-03-SUMMARY.md`.
</output>
