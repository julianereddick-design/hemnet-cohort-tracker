---
phase: 19-scheduled-batch-orchestrator-sold-match-batch
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - deploy-instructions.md
autonomous: true
requirements: [SCHED-03]
must_haves:
  truths:
    - "deploy-instructions.md contains an installable weekly crontab line that invokes `node sold-match-batch.js` directly (Mon 07:30 UTC; fortnightly via the orchestrator's even-week gate), logging to /var/log/hemnet/sold-match-batch.log, in the existing crontab block format"
    - "deploy-instructions.md documents the required env vars for the batch (DATABASE_URL, Oxylabs creds, SOLD_MATCH_BRIDGE default-on, MAX_OXY_CALLS ~8000 batch ceiling, RECHECK_BRIDGE_FINAL_ONLY default-off cost lever, SLACK_WEBHOOK_URL)"
    - "deploy-instructions.md has a Runbook entry for sold-match-batch (Detect via Slack/cron_job_log/log file; Diagnose via result_summary.batchStoppedBy/sample stats/recordsMatched; Re-run via `node sold-match-batch.js`) modeled on the cohort-spotcheck-gate entry, noting the config/sold-panel.json panel + ._backfill_pending and the MAX_OXY_CALLS / RECHECK_BRIDGE_FINAL_ONLY cost levers"
  artifacts:
    - path: "deploy-instructions.md"
      provides: "Sold match batch crontab line + env-var list + operator runbook entry"
      contains: "node sold-match-batch.js"
  key_links:
    - from: "deploy-instructions.md crontab block"
      to: "sold-match-batch.js"
      via: "direct node invocation per the runJob-direct-require contract"
      pattern: "node sold-match-batch.js"
    - from: "deploy-instructions.md runbook"
      to: "config/sold-panel.json"
      via: "documented panel + backfill cost lever (D-13/D-17)"
      pattern: "sold-panel.json"
---

<objective>
Document SCHED-03: add the installable weekly crontab line for `sold-match-batch.js` (fortnightly
via the even-week gate), the required env-var list (incl MAX_OXY_CALLS ~8000 batch ceiling,
SOLD_MATCH_BRIDGE, RECHECK_BRIDGE_FINAL_ONLY cost lever), and an operator runbook entry to
`deploy-instructions.md`, modeled on the existing `cohort-spotcheck-gate` cron line + runbook entry.

Purpose: SCHED-03 — the cron schedule, env vars, and a detect/diagnose/re-run runbook entry (with
the panel + backfill + cost-lever notes) are documented and the crontab line is installable on the
droplet (installation itself is an operator-gated go-live step, NOT part of acceptance — consistent
with Phases 15-18).
Output: edits to `deploy-instructions.md` (no code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/19-scheduled-batch-orchestrator-sold-match-batch/19-CONTEXT.md
@.planning/phases/19-scheduled-batch-orchestrator-sold-match-batch/19-02-PLAN.md
@config/sold-panel.json

<interfaces>
<!-- Anchors in deploy-instructions.md to model the new content on (line numbers from this session; re-grep before editing). -->

Crontab block — the Phase 12 cohort-spotcheck-gate cron line (~line 53-58) to mirror exactly:
```cron
# === Phase 12 — Weekly cohort spot-check QA gate (Mon 06:30 UTC, immediately after cohort-create) ===
30 6 * * 1  cd /opt/hemnet-cohort-tracker && node cohort-spotcheck-gate.js     >> /var/log/hemnet/spotcheck-gate.log 2>&1
```
Live Monday-UTC slots already taken (the new line MUST NOT collide):
- cohort-create          0 6  * * 1  (06:00)
- cohort-spotcheck-gate 30 6 * * 1  (06:30)
- hemnet-targeted-match (Job B)  0 3  * * 1  (03:00)
- market-totals-daily  30 8 * * *  (08:30 daily)
→ chosen slot: **Mon 07:30 UTC = `30 7 * * 1`** (clear of all the above; D-14 fortnightly via the
  orchestrator's even-week gate — the cron line itself fires weekly).

Env-var section anchor (~line 22-33): "Required vars" / "Optional vars" / "Phase 13 vars".
Runbook entry to model on (the cohort-spotcheck-gate entry, ~line 296-319): a bold header line,
then **Detect:** / **Diagnose:** / **Re-run:** sub-sections. `verify-cron-job-log.js` is the
existing cron_job_log inspector to reuse in Detect.

result_summary fields the runbook should reference for diagnosis (from sold-match-batch.js, Plan 19-02):
sample stats { allocated, fetched, deedsExcluded, dupsExcluded, perMuni, window }, batch totals
{ matched, booli_only, uncertain, error, matchRate }, the re-check block { enrolled, rechecked,
lateMatched, stillPending, uncertain, settled }, `oxylabsSpent`, `batchStoppedBy`, `fetchFailures`,
`recordsMatched`/`recordsTotal`, and `skipped`/`reason:'off-week'` on an off-week.

Panel + cost levers (D-13/D-16/D-17):
- `config/sold-panel.json` — the national panel (11 munis v1); `target_sample_size` (~1000) and
  `lookback_days` (14) live here; `._backfill_pending` lists munis needing Hemnet/both IDs (a morning
  append task, NOT a blocker — panel is pure config).
- `MAX_OXY_CALLS` — batch ceiling; cost model ~3-6k/run → default ~8000 (D-17).
- `RECHECK_BRIDGE_FINAL_ONLY` — default-off; on = skip the SERP bridge on intermediate re-checks
  (~mid 9k→~6k calls/month). Operator cost lever, document but leave OFF (D-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the weekly crontab line + env-var documentation for sold-match-batch</name>
  <read_first>
    - deploy-instructions.md (the Crontab block ~lines 41-110 and the env-var section ~lines 20-40 — re-grep for exact current line numbers before editing)
    - sold-match-batch.js (Plan 19-02 — confirm the env vars it actually reads: SCRAPE_FORCE_OXYLABS, SOLD_MATCH_BRIDGE, MAX_OXY_CALLS, SLACK_WEBHOOK_URL, SOLD_BATCH_FETCH_FAIL_THRESHOLD)
    - lib/sold-config.js (Plan 19-02 — RECHECK_BRIDGE_FINAL_ONLY default false)
    - lib/sold-transport.js (MAX_OXY_CALLS default 4000, line 42 — the batch sizes it higher via env)
  </read_first>
  <action>
    In `deploy-instructions.md`:
    1. Add a new crontab stanza in the existing ```cron block, just after the cohort-spotcheck-gate
    Phase 12 stanza so the Monday-morning jobs stay grouped. EXACT format of the surrounding lines:
    ```cron
    # === Phase 19 (v3.1) — Sold match batch (weekly cron, FORTNIGHTLY effect, Mon 07:30 UTC) ===
    # sold-match-batch.js runs the whole sold-match pipeline in ONE process: calls the national
    # population-weighted sampler (config/sold-panel.json) for a de-duped ~1000-record 14-day sample,
    # matches each record against Hemnet, then runs the Phase-18 re-check drain, all under ONE
    # batch-wide Oxylabs ceiling (MAX_OXY_CALLS). The line fires WEEKLY but the orchestrator no-ops
    # on ODD ISO weeks (even-week gate) → effective FORTNIGHTLY cadence. Fails safe: validate()
    # escalates to Slack on ceiling/fatal/incomplete rather than logging a partial run as success.
    # Slot 07:30 UTC clears cohort-create (06:00), cohort-spotcheck-gate (06:30), Job B (03:00),
    # market-totals (08:30). Logs to cron_job_log + the log file below.
    30 7 * * 1  cd /opt/hemnet-cohort-tracker && node sold-match-batch.js        >> /var/log/hemnet/sold-match-batch.log 2>&1
    ```
    2. In the env-var section, add a "Phase 19 (v3.1) — Sold match batch vars" subsection documenting:
       - `DATABASE_URL` (already required; the batch reuses the cron-wrapper client).
       - `OXYLABS_USERNAME` / `OXYLABS_PASSWORD` (already required for the forced-Oxylabs transport).
       - `MAX_OXY_CALLS` — the batch-wide Oxylabs ceiling. Cost model ~3-6k calls/run (sampler ~1000
         records + re-check drain ~50% reaching the bridge → ~7-13k/month, mid ~9k). Set **~8000** —
         high enough to complete a full fortnight, low enough to be a real hard cap (D-17). Note the
         lib/sold-transport.js DEFAULT is 4000 (too low for the full batch) → set it explicitly in env.
       - `SOLD_MATCH_BRIDGE` — default-on; the orchestrator sets it to `'1'` itself (D-05). Document
         `SOLD_MATCH_BRIDGE=0` as the opt-out.
       - `RECHECK_BRIDGE_FINAL_ONLY` — **default OFF** cost lever (D-16). When `=1`, the re-check drain
         skips the SERP bridge on intermediate re-attempts and runs it only on the final attempt before
         settle (~mid 9k→~6k calls/month). Leave OFF for full-fidelity; flip to `1` only as a cost lever.
       - `SLACK_WEBHOOK_URL` — already documented; the batch's validate() escalations post here (same
         webhook as Phase 12, NOT the bot token).
    Make the additions self-consistent with the existing doc voice (terse, operator-facing). Commit as
    `docs(19-03): sold-match-batch weekly crontab line + env vars (SCHED-03)`.
  </action>
  <verify>
    <automated>grep -n "node sold-match-batch.js" deploy-instructions.md && grep -n "30 7 \* \* 1" deploy-instructions.md && grep -n "MAX_OXY_CALLS" deploy-instructions.md && grep -n "RECHECK_BRIDGE_FINAL_ONLY" deploy-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "30 7 \* \* 1  cd /opt/hemnet-cohort-tracker && node sold-match-batch.js" deploy-instructions.md` returns 1 (the installable line, exact slot, direct node invocation).
    - `grep -c "/var/log/hemnet/sold-match-batch.log" deploy-instructions.md` returns at least 1.
    - `grep -c "MAX_OXY_CALLS" deploy-instructions.md` returns at least 1, documented as the ~8000 batch ceiling.
    - `grep -c "RECHECK_BRIDGE_FINAL_ONLY" deploy-instructions.md` returns at least 1, documented as default-OFF.
    - `grep -c "SOLD_MATCH_BRIDGE" deploy-instructions.md` returns at least 1.
    - `grep -ciE "fortnight|even.?week|odd.?week" deploy-instructions.md` returns at least 1 (the weekly-line/fortnightly-effect note).
    - The new crontab line does NOT duplicate an existing Monday slot: `grep -c "^30 7 \* \* 1" deploy-instructions.md` returns 1.
  </acceptance_criteria>
  <done>deploy-instructions.md contains the installable Mon 07:30 UTC `node sold-match-batch.js` crontab line (weekly cron / fortnightly even-week effect, dedicated log file) and a Phase 19 env-var subsection covering MAX_OXY_CALLS (~8000 ceiling), SOLD_MATCH_BRIDGE (default-on), RECHECK_BRIDGE_FINAL_ONLY (default-off cost lever), and SLACK_WEBHOOK_URL.</done>
</task>

<task type="auto">
  <name>Task 2: Add the sold-match-batch operator runbook entry (Detect / Diagnose / Re-run + panel & cost levers)</name>
  <read_first>
    - deploy-instructions.md (the cohort-spotcheck-gate runbook entry ~lines 296-319 — model the new entry on it; re-grep for current line numbers)
    - sold-match-batch.js (Plan 19-02 — the result_summary fields: sample stats, batch totals, batchStoppedBy, fetchFailures, recordsMatched/recordsTotal, the re-check block, oxylabsSpent, skipped/off-week)
    - config/sold-panel.json (the panel + ._backfill_pending the runbook references as the coverage/cost lever)
  </read_first>
  <action>
    Add a runbook entry under the `## Runbook` section of `deploy-instructions.md`, modeled on the
    cohort-spotcheck-gate entry, with these parts:
    - **Header:** `**sold-match-batch.js (Phase 19 Sold match batch) status=warning** — validate() escalated.`
      Briefly state the escalation triggers (batch stopped on ceiling before completing; a fatal
      sampler/match error; fetchFailures over threshold; an incomplete match pass). Note methodology is
      Slutpris-only and unchanged (D-01), and that an OFF-WEEK skip (odd ISO week) is NORMAL and does
      NOT escalate (result_summary.skipped=true, reason='off-week').
    - **Detect:** Slack channel — `[WARNING] sold-match-batch: ...` (and `[FAILURE] ...`). Also
      `cron_job_log` (`node scripts/verify-cron-job-log.js`, filter `script_name = 'sold-match-batch'`)
      and `/var/log/hemnet/sold-match-batch.log`.
    - **Diagnose:** read the last cron_job_log row's `result_summary` — if `skipped:true` it was an
      off-week no-op (expected fortnightly). Else inspect `batchStoppedBy` (ceiling vs null), the sample
      stats (`allocated`/`fetched`/`deedsExcluded`/`dupsExcluded`/`perMuni`/`window`),
      `recordsMatched` vs `recordsTotal`, `fetchFailures`, `oxylabsSpent` vs `MAX_OXY_CALLS`, and the
      re-check block (`enrolled/rechecked/lateMatched/stillPending/uncertain/settled`). A `ceiling`
      stop means MAX_OXY_CALLS was too low this fortnight — raise it OR set RECHECK_BRIDGE_FINAL_ONLY=1.
    - **Re-run:**
      ```bash
      # Re-run the whole batch (idempotent upserts per DB-03; same as cron):
      node sold-match-batch.js
      ```
      Note the re-run is idempotent (ON CONFLICT booli_id upserts + de-dup against booli_sold) and
      re-enrolls only un-enrolled booli_only rows, so a re-run after a ceiling stop resumes safely. To
      force a run regardless of week parity for a manual catch-up, document that the operator can run it
      on an even week (or that the even-week gate is the only cadence control — there is no override
      flag by default).
    - **Panel + cost levers (D-13/D-16/D-17):** state that `config/sold-panel.json` is the coverage
      lever — the v1 11-muni panel is metro/south-heavy (no Norrland); appending munis from
      `._backfill_pending` (8 need Hemnet IDs, the rest need both — a morning task) widens national
      coverage with a one-line config edit, NOT a code change. `target_sample_size` (~1000) and
      `lookback_days` (14) also live in the panel. The Oxylabs cost levers are `MAX_OXY_CALLS` (the hard
      ceiling) and `RECHECK_BRIDGE_FINAL_ONLY` (skip the bridge on intermediate re-checks, ~9k→~6k/mo).
    - **Go-live note:** state explicitly that the live DDL migration (Phase 18), the first Oxylabs wet
      run, and installing this crontab line on the droplet are operator-gated go-live steps — NOT part of
      phase acceptance (offline `--smoke` is). No Oxylabs run without explicit per-run operator go-ahead.
    Commit as `docs(19-03): sold-match-batch runbook entry — detect/diagnose/re-run + panel & cost levers (SCHED-03)`.
  </action>
  <verify>
    <automated>grep -n "sold-match-batch.js (Phase 19" deploy-instructions.md && grep -n "batchStoppedBy" deploy-instructions.md && grep -n "sold-panel.json" deploy-instructions.md && grep -n "off-week" deploy-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sold-match-batch" deploy-instructions.md` returns at least 3 (crontab line from Task 1 + runbook header + re-run command).
    - `grep -c "batchStoppedBy" deploy-instructions.md` returns at least 1 (Diagnose references the result_summary field).
    - `grep -c "verify-cron-job-log.js" deploy-instructions.md` still resolves (Detect reuses the existing tool).
    - `grep -c "sold-panel.json" deploy-instructions.md` returns at least 1 (D-13 panel cost/coverage lever documented).
    - `grep -c "_backfill_pending\|backfill" deploy-instructions.md` returns at least 1 (the morning append task).
    - `grep -ciE "off-week|skipped" deploy-instructions.md` returns at least 1 (off-week no-op is documented as normal, not an escalation).
    - `grep -ci "node sold-match-batch.js" deploy-instructions.md` returns at least 2 (crontab line + re-run command).
  </acceptance_criteria>
  <done>The Runbook has a sold-match-batch entry mirroring the cohort-spotcheck-gate one: Detect (Slack/cron_job_log/log file), Diagnose (result_summary.batchStoppedBy / sample stats / recordsMatched / re-check block; off-week skip = normal), Re-run (`node sold-match-batch.js`, idempotent), the config/sold-panel.json panel + ._backfill_pending coverage lever and the MAX_OXY_CALLS / RECHECK_BRIDGE_FINAL_ONLY cost levers (D-13/D-16/D-17), and the operator-gated go-live note.</done>
</task>

</tasks>

<verification>
- `grep "node sold-match-batch.js" deploy-instructions.md` → the crontab line + the re-run command exist.
- `grep "30 7 \* \* 1" deploy-instructions.md` → the chosen non-colliding slot.
- `grep "MAX_OXY_CALLS\|SOLD_MATCH_BRIDGE\|RECHECK_BRIDGE_FINAL_ONLY\|SLACK_WEBHOOK_URL" deploy-instructions.md` → env vars documented.
- `grep "batchStoppedBy\|sold-panel.json\|off-week\|backfill" deploy-instructions.md` → runbook diagnosis + panel/cost levers present.
- This plan touches NO code; the orchestrator behavior it documents is verified by Plan 19-02's `--smoke`.
</verification>

<success_criteria>
SCHED-03: the cron schedule (installable Mon 07:30 UTC line, direct `node sold-match-batch.js`,
weekly cron / fortnightly even-week effect, dedicated log file), required env vars (DATABASE_URL,
Oxylabs creds, MAX_OXY_CALLS ~8000 batch ceiling, SOLD_MATCH_BRIDGE default-on,
RECHECK_BRIDGE_FINAL_ONLY default-off cost lever, SLACK_WEBHOOK_URL), and a detect/diagnose/re-run
operator runbook entry (with the config/sold-panel.json panel + backfill coverage lever and the
Oxylabs cost levers) are documented in deploy-instructions.md. Installing the line on the droplet is
an operator-gated go-live step, not phase acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/19-scheduled-batch-orchestrator-sold-match-batch/19-03-SUMMARY.md`
</output>
