---
phase: 19-scheduled-batch-orchestrator-sold-match-batch
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - deploy-instructions.md
autonomous: true
requirements: [SCHED-03]
must_haves:
  truths:
    - "deploy-instructions.md contains an installable weekly crontab line that invokes `node sold-match-batch.js` directly (Mon 07:30 UTC), logging to /var/log/hemnet/sold-match-batch.log, in the existing crontab block format"
    - "deploy-instructions.md documents the required env vars for the batch (DB_*/DATABASE_URL, Oxylabs creds, SOLD_MATCH_BRIDGE default-on, MAX_OXY_CALLS batch ceiling, SLACK_WEBHOOK_URL)"
    - "deploy-instructions.md has a Runbook entry for sold-match-batch (Detect via Slack/cron_job_log/log file; Diagnose via result_summary.batchStoppedBy/perSegment; Re-run via `node sold-match-batch.js`) modeled on the cohort-spotcheck-gate entry, noting the config/sold-segments.json cost lever"
  artifacts:
    - path: "deploy-instructions.md"
      provides: "Sold match batch crontab line + env-var list + operator runbook entry"
      contains: "node sold-match-batch.js"
  key_links:
    - from: "deploy-instructions.md crontab block"
      to: "sold-match-batch.js"
      via: "direct node invocation per the runJob-direct-require contract"
      pattern: "node sold-match-batch.js"
    - from: "deploy-instructions.md runbook"
      to: "config/sold-segments.json"
      via: "documented segment-list cost lever (D-12)"
      pattern: "sold-segments.json"
---

<objective>
Document SCHED-03: add the installable weekly crontab line for `sold-match-batch.js`, the
required env-var list, and an operator runbook entry to `deploy-instructions.md`, modeled on
the existing `cohort-spotcheck-gate` cron line + runbook entry.

Purpose: SCHED-03 — the cron schedule, env vars, and a detect/diagnose/re-run runbook entry
are documented and the crontab line is installable on the droplet (installation itself is an
operator-gated go-live step, NOT part of acceptance — consistent with Phases 15-18).
Output: edits to `deploy-instructions.md` (no code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/19-scheduled-batch-orchestrator-sold-match-batch/19-CONTEXT.md
@.planning/phases/19-scheduled-batch-orchestrator-sold-match-batch/19-01-PLAN.md

<interfaces>
<!-- Anchors in deploy-instructions.md to model the new content on (line numbers from this session; re-grep before editing). -->

Crontab block (the Phase 12 cohort-spotcheck-gate cron line, ~lines 53-58) to mirror exactly:
```cron
# === Phase 12 — Weekly cohort spot-check QA gate (Mon 06:30 UTC, immediately after cohort-create) ===
# ...comment block...
30 6 * * 1  cd /opt/hemnet-cohort-tracker && node cohort-spotcheck-gate.js     >> /var/log/hemnet/spotcheck-gate.log 2>&1
```
Live Monday-UTC slots already taken (D-11 — the new line MUST NOT collide):
- cohort-create        0 6  * * 1  (06:00)
- cohort-spotcheck-gate 30 6 * * 1  (06:30)
- hemnet-targeted-match (Job B)  0 3  * * 1  (03:00)
- market-totals-daily  30 8 * * *  (08:30 daily)
→ chosen slot: **Mon 07:30 UTC = `30 7 * * 1`** (clear of all the above).

Env-var section anchor (~lines 22-33): "Required vars" / "Optional vars" / "Phase 13 vars".
Runbook entry to model on (the cohort-spotcheck-gate entry, ~lines 296-319): a bold header line,
then **Detect:** / **Artifacts:** / **Re-run manually:** sub-sections.

result_summary fields the runbook should reference for diagnosis (from sold-match-batch.js, Plan 19-01, D-09):
`perSegment` (per-segment {matched,booli_only,uncertain,error,matchRate,stoppedBy}), batch totals,
the re-check block {enrolled,rechecked,lateMatched,stillPending,uncertain,settled},
`oxylabsSpent`, `batchStoppedBy`, `fetchFailures`, `segmentsCompleted`/`segmentsTotal`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the weekly crontab line + env-var documentation for sold-match-batch</name>
  <read_first>
    - deploy-instructions.md (the Crontab block ~lines 41-95 and the env-var section ~lines 20-40 — re-grep for exact current line numbers before editing)
    - sold-match-batch.js (Plan 19-01 — confirm the env vars it actually reads: SCRAPE_FORCE_OXYLABS, SOLD_MATCH_BRIDGE, MAX_OXY_CALLS, SLACK_WEBHOOK_URL)
    - lib/sold-transport.js (MAX_OXY_CALLS default 4000, line 42)
  </read_first>
  <action>
    In `deploy-instructions.md`:
    1. Add a new crontab stanza in the existing ```cron block (place it just after the cohort-spotcheck-gate Phase 12 stanza so the Monday-morning jobs stay grouped). Use the EXACT format of the surrounding lines:
    ```cron
    # === Phase 19 (v3.1) — Weekly Sold match batch (Mon 07:30 UTC) ===
    # sold-match-batch.js runs the whole sold-match pipeline in ONE process: drives
    # scripts/sold-match-run.js across every config/sold-segments.json segment over the
    # rolling sold-date window (~90-120d ago), then runs the Phase-18 re-check drain, all
    # under ONE batch-wide Oxylabs ceiling (MAX_OXY_CALLS). Fails safe: validate() escalates
    # to Slack on ceiling/partial/fatal/incomplete rather than logging a partial run as success.
    # Slot: 07:30 UTC clears cohort-create (06:00), cohort-spotcheck-gate (06:30), Job B (03:00),
    # and market-totals (08:30). Logs to cron_job_log + the log file below.
    30 7 * * 1  cd /opt/hemnet-cohort-tracker && node sold-match-batch.js        >> /var/log/hemnet/sold-match-batch.log 2>&1
    ```
    2. In the env-var section, add a "Phase 19 (v3.1) — Sold match batch vars" subsection documenting:
       - `DATABASE_URL` (already required; note the batch reuses it — the cron-wrapper client)
       - `OXYLABS_USERNAME` / `OXYLABS_PASSWORD` (already required for the forced-Oxylabs transport)
       - `MAX_OXY_CALLS` — the batch-wide Oxylabs ceiling (default 4000 per lib/sold-transport.js); SIZE it as cadence × segment-count × ≤4 bridge calls per booli_only (D-12). Note this is the SINGLE knob that bounds a whole batch run.
       - `SOLD_MATCH_BRIDGE` — default-on; the orchestrator sets it to `'1'` itself (D-05). Document `SOLD_MATCH_BRIDGE=0` as the opt-out.
       - `SLACK_WEBHOOK_URL` — already documented; note the batch's validate() escalations post here (same webhook as Phase 12, NOT the bot token).
    Make the additions self-consistent with the existing doc voice (terse, operator-facing). Commit as `docs(19-02): sold-match-batch weekly crontab line + env vars (SCHED-03)`.
  </action>
  <verify>
    <automated>grep -n "node sold-match-batch.js" deploy-instructions.md && grep -n "30 7 \* \* 1" deploy-instructions.md && grep -n "MAX_OXY_CALLS" deploy-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "30 7 \* \* 1  cd /opt/hemnet-cohort-tracker && node sold-match-batch.js" deploy-instructions.md` returns 1 (the installable line, exact slot, direct node invocation per the runJob-direct-require contract).
    - `grep -c "/var/log/hemnet/sold-match-batch.log" deploy-instructions.md` returns at least 1.
    - `grep -c "MAX_OXY_CALLS" deploy-instructions.md` returns at least 1, and the line documents it as the batch-wide ceiling.
    - `grep -c "SOLD_MATCH_BRIDGE" deploy-instructions.md` returns at least 1.
    - The new crontab line does NOT duplicate an existing Monday slot: `grep -c "^30 7 \* \* 1" deploy-instructions.md` returns 1.
  </acceptance_criteria>
  <done>deploy-instructions.md contains the installable Mon 07:30 UTC `node sold-match-batch.js` crontab line (logging to the dedicated log file) and a Phase 19 env-var subsection covering MAX_OXY_CALLS (batch ceiling), SOLD_MATCH_BRIDGE (default-on), and SLACK_WEBHOOK_URL.</done>
</task>

<task type="auto">
  <name>Task 2: Add the sold-match-batch operator runbook entry (Detect / Diagnose / Re-run)</name>
  <read_first>
    - deploy-instructions.md (the cohort-spotcheck-gate runbook entry ~lines 296-319 — model the new entry on it; re-grep for current line numbers)
    - sold-match-batch.js (Plan 19-01 — the result_summary fields: perSegment, batchStoppedBy, fetchFailures, segmentsCompleted/segmentsTotal, the re-check block, oxylabsSpent)
  </read_first>
  <action>
    Add a runbook entry under the `## Runbook` section of `deploy-instructions.md`, modeled on the cohort-spotcheck-gate entry, with these parts:
    - **Header:** `**sold-match-batch.js (Phase 19 Sold match batch) status=warning** — validate() escalated.` Briefly state the four escalation triggers (batch stopped on ceiling/ceiling-floor before all segments completed; a non-ceiling fatal segment error; fetchFailures over threshold; not all configured segments completed). Note methodology is Slutpris-only and unchanged (D-01).
    - **Detect:** Slack channel — `[WARNING] sold-match-batch: ...` (and `[FAILURE] ...`). Also `cron_job_log` (`node scripts/verify-cron-job-log.js`, filter `script_name = 'sold-match-batch'`) and `/var/log/hemnet/sold-match-batch.log`.
    - **Diagnose:** read the last cron_job_log row's `result_summary` — inspect `batchStoppedBy` (ceiling vs ceiling-floor vs null), `perSegment` (which segment threw / which `stoppedBy`), `segmentsCompleted` vs `segmentsTotal`, `fetchFailures`, `oxylabsSpent` vs `MAX_OXY_CALLS`, and the re-check block (`enrolled/rechecked/lateMatched/stillPending/uncertain/settled`). A `ceiling` stop means MAX_OXY_CALLS was too low for the batch this week — raise it OR trim segments.
    - **Re-run:**
      ```bash
      # Re-run the whole batch (idempotent upserts per DB-03; same as cron):
      node sold-match-batch.js
      ```
      Note the re-run is idempotent (ON CONFLICT booli_id upserts) and re-enrolls only un-enrolled booli_only rows, so a re-run after a ceiling stop resumes safely.
    - **Cost lever (D-12):** state that `config/sold-segments.json` is the operator's primary cost lever — adding/removing segments scales the batch's Oxylabs spend; size `MAX_OXY_CALLS` as cadence × segment-count × ≤4 bridge calls per booli_only.
    - **Go-live note:** state explicitly that the live DDL migration (Phase 18), the first Oxylabs wet run, and installing this crontab line on the droplet are operator-gated go-live steps — NOT part of phase acceptance (offline `--smoke` is). No Oxylabs run without explicit per-run operator go-ahead.
    Commit as `docs(19-02): sold-match-batch runbook entry — detect/diagnose/re-run + cost lever (SCHED-03)`.
  </action>
  <verify>
    <automated>grep -n "sold-match-batch.js (Phase 19" deploy-instructions.md && grep -n "batchStoppedBy" deploy-instructions.md && grep -n "sold-segments.json" deploy-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sold-match-batch" deploy-instructions.md` returns at least 3 (crontab line from Task 1 + runbook header + re-run command).
    - `grep -c "batchStoppedBy" deploy-instructions.md` returns at least 1 (Diagnose references the result_summary field).
    - `grep -c "verify-cron-job-log.js" deploy-instructions.md` still resolves (the Detect step reuses the existing tool) — runbook entry references it.
    - `grep -c "config/sold-segments.json\|sold-segments.json" deploy-instructions.md` returns at least 1 (D-12 cost lever documented).
    - `grep -ci "node sold-match-batch.js" deploy-instructions.md` returns at least 2 (crontab line + re-run command).
  </acceptance_criteria>
  <done>The Runbook has a sold-match-batch entry mirroring the cohort-spotcheck-gate one: Detect (Slack/cron_job_log/log file), Diagnose (result_summary.batchStoppedBy/perSegment/segmentsCompleted/fetchFailures/re-check block), Re-run (`node sold-match-batch.js`, idempotent), the config/sold-segments.json cost lever (D-12), and the operator-gated go-live note.</done>
</task>

</tasks>

<verification>
- `grep "node sold-match-batch.js" deploy-instructions.md` → the crontab line + the re-run command exist.
- `grep "30 7 \* \* 1" deploy-instructions.md` → exactly the chosen non-colliding slot.
- `grep "MAX_OXY_CALLS\|SOLD_MATCH_BRIDGE\|SLACK_WEBHOOK_URL" deploy-instructions.md` → env vars documented.
- `grep "batchStoppedBy\|perSegment\|sold-segments.json" deploy-instructions.md` → runbook diagnosis + cost lever present.
- This plan touches NO code; the orchestrator's behavior it documents is verified by Plan 19-01's `--smoke`.
</verification>

<success_criteria>
SCHED-03: the cron schedule (installable Mon 07:30 UTC line, direct `node sold-match-batch.js`,
dedicated log file), required env vars (DATABASE_URL, Oxylabs creds, MAX_OXY_CALLS batch ceiling,
SOLD_MATCH_BRIDGE default-on, SLACK_WEBHOOK_URL), and a detect/diagnose/re-run operator runbook
entry (with the config/sold-segments.json cost lever) are documented in deploy-instructions.md.
Installing the line on the droplet is an operator-gated go-live step, not phase acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/19-scheduled-batch-orchestrator-sold-match-batch/19-02-SUMMARY.md`
</output>
