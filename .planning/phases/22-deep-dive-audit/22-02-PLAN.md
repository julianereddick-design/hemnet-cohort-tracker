---
phase: 22-deep-dive-audit
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - docs/price-scraper-droplet-audit.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04, AUDIT-05]

must_haves:
  truths:
    - "The audit doc inventories all 6 apps with purpose, owner, and last-active evidence (AUDIT-01)"
    - "The audit doc maps Postgres DB(s)/tables, Metabase, Redis, Docker volumes, and the large logs incl. the 4.6 GB kill.log with a size figure (AUDIT-02)"
    - "The audit doc enumerates every scheduled/triggered job with cadence (AUDIT-03)"
    - "The audit doc states the real CPU/mem/disk baseline vs the s-8vcpu-16gb allocation (AUDIT-04)"
    - "Every non-Hemnet-price app (incl. Booli) has a keep/kill verdict backed by dependency evidence (AUDIT-05)"
    - "Every claim cites the command/path it came from; no secret values appear in the doc"
  artifacts:
    - path: "docs/price-scraper-droplet-audit.md"
      provides: "The evidence-based 5-section droplet audit with keep/kill recommendations"
      min_lines: 80
      contains: "## App inventory"
  key_links:
    - from: "docs/price-scraper-droplet-audit.md"
      to: ".planning/phases/22-deep-dive-audit/22-EVIDENCE.md"
      via: "each claim cites a captured command output"
      pattern: "22-EVIDENCE"
    - from: "docs/price-scraper-droplet-audit.md"
      to: "Phase 24 cleanup"
      via: "per-app keep/kill verdict + dependency evidence gates removal"
      pattern: "keep/kill|Keep|Kill"
---

<objective>
Synthesize the read-only evidence captured in Plan 22-01 (`22-EVIDENCE.md`) into the single evidence-based ops deliverable `docs/price-scraper-droplet-audit.md`, alongside the Phase-21 runbook. The doc's five sections map 1:1 to the phase success criteria (AUDIT-01..05) and end in a keep/kill recommendation per non-Hemnet-price app, backed by dependency evidence — so Phase 24 removes nothing blind.

Purpose: turn raw evidence into a decision-grade audit that gates Phase 24 (cleanup) and informs Phase 25 (right-size).
Output: `docs/price-scraper-droplet-audit.md` with real numbers, every claim cited to its evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-deep-dive-audit/22-CONTEXT.md
@.planning/phases/22-deep-dive-audit/22-EVIDENCE.md
@docs/price-scraper-droplet-runbook.md
</context>

<guardrails>
This plan writes ONLY in this repo (`docs/price-scraper-droplet-audit.md`). It does NOT connect to the droplet — all facts come from `22-EVIDENCE.md`. If the evidence file is missing a fact needed for a section, you MAY run the SAME read-only inspection commands from Plan 22-01's guardrails (identical forbidden list applies: no writes, no `rm`, no docker/service mutations, no reboot, bounded I/O) and append to the evidence file first — never assert without evidence.

**SECRETS:** the audit doc MUST NOT contain any secret/token/password/key value. Note their existence and location only (e.g. "DB password set in `<file>` — value redacted"). The dangling-RSA-key-blob hygiene finding is recorded as a note with remediation deferred to Phase 24.
</guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Write audit sections 1-4 (AUDIT-01..04) from the evidence</name>
  <read_first>
    - .planning/phases/22-deep-dive-audit/22-EVIDENCE.md (the raw evidence — source of every number)
    - .planning/phases/22-deep-dive-audit/22-CONTEXT.md (the exact 5-section structure to mirror)
    - docs/price-scraper-droplet-runbook.md (host facts, slug, IdentitiesOnly note for the doc preamble)
    - docs/price-scraper-droplet-audit.md (the file you are creating)
  </read_first>
  <files>docs/price-scraper-droplet-audit.md</files>
  <action>
Create `docs/price-scraper-droplet-audit.md` with a short preamble (host `170.64.181.89`, region syd1, root login, repo `github.com/tt7676/hem-bol-scrapers`, audit date, "read-only audit — no droplet changes", and a line stating evidence is captured in `.planning/phases/22-deep-dive-audit/22-EVIDENCE.md`). Then write the first four mandated sections, each citing the specific command/path from the evidence file and quoting real numbers.

1. `## App inventory` (AUDIT-01) — a markdown table with one row per app: `hemnet`, `booli`, `spotify`, `procore`, `block_inc`, `core`. Columns: App | Container/image | Purpose | Owner | Last-active evidence (last log write / newest mtime / last container start). Use "unknown — team" where owner can't be derived. Cite the `docker ps`, `find ... -printf %T+`, and `docker inspect` outputs from evidence Section 1.

2. `## Data + storage map` (AUDIT-02) — subsections:
   - Postgres: list each database with size, and a table of the largest tables (name, row count, size) from `pg_stat_user_tables`. Cite the `psql \l` / `\dt` / `pg_size_pretty` outputs.
   - Redis: keyspace + memory summary (DBSIZE, used_memory) from `redis-cli INFO`.
   - Metabase: presence + backing store (no secrets).
   - Docker volumes: name + size table from `docker volume ls` / `docker system df -v`.
   - Large on-disk logs: a table of the biggest logs with path + size, explicitly including the **4.6 GB `kill.log`** and the **121 MB blocking log** with their real sizes from `ls -la`/`du -sh`.

3. `## Scheduled / triggered work` (AUDIT-03) — a table of every job: Source (crontab / Celery beat / restart script) | What it runs | Cadence. Quote the cron lines and `beat_schedule` cadence strings from evidence Section 3.

4. `## Resource + cost baseline` (AUDIT-04) — state the confirmed slug (`s-8vcpu-16gb`, ~$100/mo, from `doctl` — note the legacy `1vcpu-2gb` name is misleading), then actual usage: vCPU count, RAM used/total (`free -h`), disk used/avail (`df -h`), and per-container CPU%/mem (`docker stats`). Add one sentence framing the right-sizing headroom for Phase 25. Cite evidence Section 4.

Every numeric claim must reference where it came from (e.g. "(evidence §2, `du -sh`)"). Do not invent numbers; if a value is absent from evidence, gather it read-only first (see guardrails).
  </action>
  <acceptance_criteria>
    - `docs/price-scraper-droplet-audit.md` exists and contains the headings `## App inventory`, `## Data + storage map`, `## Scheduled / triggered work`, `## Resource + cost baseline`
    - The app-inventory section names all six apps: grep finds `hemnet`, `booli`, `spotify`, `procore`, `block_inc`, `core`
    - Grep finds `kill.log` with a size figure (e.g. `GB`) and the storage section references `Postgres` and `Redis`
    - Grep finds `s-8vcpu-16gb` in the resource/cost section
    - The doc cites the evidence file: grep finds `22-EVIDENCE`
    - No secret value present: `grep -niE '(password|secret|token|api_key)[=: ]+[^<[:space:]]'` returns nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c "f=docs/price-scraper-droplet-audit.md; grep -q '## App inventory' \"$f\" && grep -q '## Data + storage map' \"$f\" && grep -q '## Scheduled / triggered work' \"$f\" && grep -q '## Resource + cost baseline' \"$f\" && for a in hemnet booli spotify procore block_inc core; do grep -q \"$a\" \"$f\" || { echo MISSING $a; exit 1; }; done && grep -q 'kill.log' \"$f\" && grep -q 's-8vcpu-16gb' \"$f\" && grep -q '22-EVIDENCE' \"$f\" && ! grep -niE '(password|secret|token|api_key)[=: ]+[^<[:space:]]' \"$f\""</automated>
  </verify>
  <done>Sections 1-4 of the audit doc are written from cited evidence with real numbers for all 6 apps, storage (incl. kill.log size), schedules with cadence, and the confirmed slug + actual resource baseline — no secret values.</done>
</task>

<task type="auto">
  <name>Task 2: Write keep/kill recommendations + hygiene notes (AUDIT-05)</name>
  <read_first>
    - .planning/phases/22-deep-dive-audit/22-EVIDENCE.md (Section 5 dependency evidence + Section 1 last-active)
    - .planning/phases/22-deep-dive-audit/22-CONTEXT.md (AUDIT-05 contract + carry-forward findings)
    - docs/price-scraper-droplet-audit.md (the file you are appending to)
  </read_first>
  <files>docs/price-scraper-droplet-audit.md</files>
  <action>
Append the final mandated section and a hygiene note to `docs/price-scraper-droplet-audit.md`.

5. `## Keep/kill recommendations` (AUDIT-05) — a table with one row per **non-Hemnet-price** app (`booli`, `spotify`, `procore`, `block_inc`, `core`): App | Verdict (Keep / Kill / Investigate) | What breaks if removed (dependency evidence) | Evidence cite. Drive each verdict from evidence Section 5:
   - State whether the app shares the Postgres DB, shares Redis, is referenced by any cron/restart script, or is imported by another app (`core` is the likely shared dependency — if `hemnet`/`booli` import `core`, `core` is Keep-by-coupling).
   - The Hemnet price scraper is the **keep-by-default primary workload** — state this explicitly; do not put it up for kill.
   - `booli` gets an explicit keep/kill call with its what-breaks evidence (it is in scope per the contract; if it shares the DB/Redis with hemnet or feeds the price product, say so).
   - Where last-active evidence shows an app is idle AND has no shared-dependency coupling (candidate `spotify`/`procore`/`block_inc`), recommend Kill and cite the idle + zero-dependency evidence; where evidence is ambiguous, recommend Investigate with the specific check Phase 24 must run first — never recommend Kill without dependency evidence.

Then append `## Hygiene & deferred findings`:
   - The **dangling RSA key blob** on Tom's `authorized_keys` line 1 (inert, one-key-per-line bug) — note only; remediation deferred to Phase 24.
   - Any secrets/credentials discovered: list their **location only**, value redacted; recommend they are not exposed in logs/repo.
   - A one-line pointer that this audit gates Phase 24 (cleanup) and feeds Phase 25 (right-size).
  </action>
  <acceptance_criteria>
    - Grep finds `## Keep/kill recommendations` and `## Hygiene & deferred findings`
    - A verdict exists for each non-Hemnet app: grep finds `booli`, `spotify`, `procore`, `block_inc`, `core` within the keep/kill section, and the doc contains at least one `Keep` and the words `what breaks` or `breaks if removed`
    - The doc states the Hemnet scraper is the primary/keep-by-default workload: grep finds `primary workload` or `keep-by-default`
    - The dangling RSA key hygiene finding is present: grep finds `RSA` and `Phase 24`
    - No secret value present (same redaction gate as Task 1)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "f=docs/price-scraper-droplet-audit.md; grep -q '## Keep/kill recommendations' \"$f\" && grep -q '## Hygiene & deferred findings' \"$f\" && grep -qiE 'Keep' \"$f\" && grep -qiE 'breaks if removed|what breaks' \"$f\" && grep -qiE 'primary workload|keep-by-default' \"$f\" && grep -qi 'RSA' \"$f\" && grep -q 'Phase 24' \"$f\" && for a in booli spotify procore block_inc core; do grep -q \"$a\" \"$f\" || { echo MISSING $a; exit 1; }; done && ! grep -niE '(password|secret|token|api_key)[=: ]+[^<[:space:]]' \"$f\""</automated>
  </verify>
  <done>Section 5 gives a dependency-evidence-backed keep/kill verdict for every non-Hemnet app (incl. Booli), names Hemnet as the keep-by-default primary workload, and the hygiene section records the dangling-RSA finding with remediation deferred to Phase 24 — no secret values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence file → published audit doc | Raw command output (possibly containing credentials) is transcribed into a committed repo doc |
| operator workstation → live prod droplet (SSH) | Only if a gap forces a top-up read; same live-prod surface as Plan 22-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Information disclosure | secrets transcribed from evidence into the audit doc | mitigate | Doc records existence/location only, values `<REDACTED>`; both tasks' verify greps for `password|secret|token|api_key` + value and fail if found |
| T-22-06 | Tampering / DoS | live droplet, only if a gap-fill read is needed | mitigate | Same read-only whitelist + forbidden list as Plan 22-01 applies; default path touches no droplet at all (synthesis from the evidence file) |
| T-22-03 | Repudiation / hygiene | dangling RSA key blob | accept (note) | Recorded as a finding; remediation explicitly deferred to Phase 24 |
</threat_model>

<verification>
- `docs/price-scraper-droplet-audit.md` exists with all five mandated sections plus a hygiene section.
- All 6 apps inventoried; storage map includes kill.log size; schedules have cadence; slug + real baseline stated; keep/kill verdict per non-Hemnet app with dependency evidence.
- Every claim cites evidence; no secret value anywhere (grep gates green).
</verification>

<success_criteria>
The five phase success criteria (AUDIT-01..05) are each satisfied by a cited, numbers-backed section of `docs/price-scraper-droplet-audit.md`, ending in dependency-evidence-backed keep/kill calls — the deliverable that gates Phase 24.
</success_criteria>

<output>
After completion, create `.planning/phases/22-deep-dive-audit/22-02-SUMMARY.md`.
</output>
