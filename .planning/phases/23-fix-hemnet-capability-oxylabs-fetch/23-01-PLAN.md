---
phase: 23-fix-hemnet-capability-oxylabs-fetch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "/var/www/apps/hemnet/apps/hemnet/tasks.py"   # ON-DROPLET (170.64.181.89), NOT this repo
  - ".planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-SNAPSHOT.md"  # local rollback record
autonomous: false   # SSHes into and mutates a team-owned live prod droplet (edits source + commits a branch)
requirements: [FETCH-01, FETCH-03]
user_setup: []

must_haves:
  truths:
    - "An exact pre-edit snapshot of tasks.py routing + crawler-playwright service/container state exists, enabling rollback"
    - "The Hemnet listing/search fetch (search_listings_2 + its sub-tasks + search_ad_cost_2) calls the Oxylabs path in apps/core/webscraper.py"
    - "The Hemnet fetch path no longer enqueues to playwright_queue / drives local headless Chromium"
    - "The change lives on a feature branch in tt7676/hem-bol-scrapers; team main is untouched and not force-pushed"
    - "No OXYLABS_* / DATABASE_URL / secret value is committed to the branch or written into the snapshot"
  artifacts:
    - path: "/var/www/apps/hemnet/apps/hemnet/tasks.py"
      provides: "Hemnet fetch re-wired to Oxylabs webscraper"
      contains: "webscraper"
    - path: ".planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-SNAPSHOT.md"
      provides: "Pre-edit rollback record (original routing + service/container state + git ref)"
      min_lines: 20
  key_links:
    - from: "apps/hemnet/tasks.py: search_listings_2"
      to: "apps/core/webscraper.py (Oxylabs)"
      via: "direct import/call on the fetch path"
      pattern: "webscraper"
    - from: "apps/hemnet/tasks.py: hemnet fetch path"
      to: "playwright_queue (REMOVED)"
      via: "no longer enqueued"
      pattern: "playwright_queue"
---

<objective>
Re-wire the Hemnet listing/search fetch on the team-owned price-scraper droplet so the page fetch goes through the Oxylabs path already present in the repo (`apps/core/webscraper.py`, added by team PR #22 "feat/webscraper-api") instead of enqueuing to `playwright_queue` / driving the local headless-Chromium `hemnet-crawler-playwright` worker. Do it reversibly: snapshot exact current state first, then commit the edit to a FEATURE BRANCH in the team repo (`tt7676/hem-bol-scrapers`) — never team `main`.

This plan produces the code change only. Plan 23-02 rebuilds the image, deploys, and runs the verification crawl. Plan 23-03 stops the Playwright container after the Oxylabs path is proven.

Purpose: FETCH-01 (route Hemnet fetch through Oxylabs) + the routing half of FETCH-03 (stop sending Hemnet work to playwright_queue). This is the prerequisite enabler for Phase 25 right-sizing — removing the 6.2 GB Playwright RAM driver.
Output: Modified on-droplet `apps/hemnet/tasks.py` committed to a feature branch; a local `23-SNAPSHOT.md` rollback record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-CONTEXT.md
@docs/price-scraper-droplet-runbook.md
@docs/price-scraper-droplet-audit.md
@.planning/phases/22-deep-dive-audit/22-EVIDENCE.md

**Target droplet (NOT this repo):** `170.64.181.89` (`s-8vcpu-16gb`, syd1, root). Repo `github.com/tt7676/hem-bol-scrapers`, app at `/var/www/apps/hemnet`. The compose services bind-mount the whole repo (`volumes: - .:/app`) — see 22-EVIDENCE §1.

**SSH (the `IdentitiesOnly` gotcha is mandatory — see runbook):**
```
ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89
```

**Known facts (from Phase 22 audit — read on the box to confirm, do not assume):**
- Entry point: beat task `Scrape hemnet.se` → `apps.hemnet.tasks.search_listings_2`; ad-cost variant `apps.hemnet.tasks.search_ad_cost_2`. Both currently DISABLED in beat (triggered manually).
- Local-Chromium worker: `hemnet-crawler-playwright` = `celery -A config.celery worker --concurrency=8 -Q playwright_queue` (6.228 GiB RAM, the driver to retire).
- Oxylabs path: `apps/core/webscraper.py`. Creds in `/var/www/apps/hemnet/.env` (`OXYLABS_*`). `core` has 0 celery tasks — it is the shared lib hemnet/booli import.
- Shared `hemnet-redis` broker serves queues `default` / `playwright_queue` / `writer_queue` — do not disturb booli/writer queues.
- Precedent: this cohort-tracker repo already flipped Hemnet to Oxylabs successfully (memory `project_hemnet_flipped_to_oxylabs`).

**GUARDRAILS (LOCKED, non-negotiable — from 23-CONTEXT):**
- Reversible-first: snapshot before any edit.
- Feature branch only; NEVER touch/force-push team `main`; do NOT trigger their deploy pipeline.
- Keep `OXYLABS_*` / `DATABASE_URL` / any secret value OUT of logs, the snapshot, and the repo. The host is KNOWN-COMPROMISED (Kinsing/kdevtmpfsi suppressed per-minute by `kill.sh`) — do not introduce new exposed services.
- This plan does NOT rebuild the image, stop any container, or run any paid crawl (those are 23-02 / 23-03).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot current Hemnet fetch routing + Playwright state (reversible-first)</name>
  <read_first>
    SSH into the droplet and READ (do not edit) the actual on-box files — current state, not assumptions:
    - `/var/www/apps/hemnet/apps/hemnet/tasks.py` (the `search_listings_2` + sub-tasks + `search_ad_cost_2` definitions and how they currently dispatch/enqueue the fetch)
    - `/var/www/apps/hemnet/apps/core/webscraper.py` (the Oxylabs entry point you will route through in Task 2 — note its public function/class name and call signature)
    - `/var/www/apps/hemnet/docker-compose.yml` (the `crawler-playwright` service definition)
    Plus `docs/price-scraper-droplet-runbook.md` (SSH) and `docs/price-scraper-droplet-audit.md` (context).
  </read_first>
  <action>
    SSH in with the exact runbook connection string (above). Capture an exact rollback record into the LOCAL file `.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-SNAPSHOT.md` (written on the operator workstation, NOT on the droplet):
    1. The current git ref of the on-droplet checkout: `cd /var/www/apps/hemnet && git rev-parse HEAD && git status --porcelain && git remote -v`.
    2. The verbatim relevant routing lines of `apps/hemnet/tasks.py` — copy the `search_listings_2` / sub-task / `search_ad_cost_2` fetch-dispatch block AS-IS so the original is recoverable. (Redact nothing here — tasks.py holds no secrets — but DO NOT copy `.env`.)
    3. The Oxylabs entry-point signature from `apps/core/webscraper.py` (function/class name + params only).
    4. Current container + service state for the Playwright worker: `docker ps --format '{{.Names}}\t{{.Status}}\t{{.CreatedAt}}' | grep -v '^#'` and the `crawler-playwright` service block from `docker-compose.yml`.
    5. A one-line revert recipe (e.g. "git checkout -- apps/hemnet/tasks.py on the branch; or git checkout main").
    Do NOT modify any droplet file in this task.
  </action>
  <acceptance_criteria>
    - `.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-SNAPSHOT.md` exists and is >= 20 lines.
    - It records the on-droplet `git rev-parse HEAD` value (40-hex), the original tasks.py fetch-dispatch lines, the webscraper.py entry-point signature, and the crawler-playwright container Status + service block.
    - Snapshot grep for secret patterns returns ZERO: `grep -Eic 'OXYLABS_|DATABASE_URL|doadmin|DJANGO_SECRET' .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-SNAPSHOT.md` prints `0`.
    - No droplet file was changed: on the box, `cd /var/www/apps/hemnet && git status --porcelain` output is unchanged from what the snapshot recorded (clean or same as before).
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-SNAPSHOT.md" && test "$(grep -Eic 'OXYLABS_|DATABASE_URL|doadmin|DJANGO_SECRET' .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-SNAPSHOT.md)" = "0" && test "$(grep -vc '^[[:space:]]*$' .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-SNAPSHOT.md)" -ge 20 && echo SNAPSHOT_OK</automated>
  </verify>
  <done>23-SNAPSHOT.md captures the exact pre-edit routing + git ref + Playwright service/container state with zero secrets; no droplet file mutated.</done>
</task>

<task type="auto">
  <name>Task 2: Re-wire the Hemnet fetch to Oxylabs on a feature branch (FETCH-01 + FETCH-03 routing)</name>
  <read_first>
    On the droplet, re-read `/var/www/apps/hemnet/apps/hemnet/tasks.py` and `/var/www/apps/hemnet/apps/core/webscraper.py` (the current state captured in Task 1). Confirm how `core/booli` already import/use `webscraper` (grep `apps.core.webscraper` across `apps/`) so your re-wire matches the established call pattern, not a hand-rolled HTTP call.
  </read_first>
  <action>
    On the droplet, in `/var/www/apps/hemnet`:
    1. Create and check out a feature branch (NEVER edit on `main`): `git checkout -b feat/hemnet-oxylabs-fetch`. Confirm `git branch --show-current` prints `feat/hemnet-oxylabs-fetch`.
    2. Edit `apps/hemnet/tasks.py` so the page fetch in `search_listings_2` (and every sub-task it dispatches, plus `search_ad_cost_2`) goes through the Oxylabs entry point in `apps/core/webscraper.py` instead of enqueuing to `playwright_queue` / driving local Chromium. Code shape is your discretion (call webscraper inline in the task, or via a thin adapter) PROVIDED the fetch demonstrably routes through Oxylabs and the Hemnet path no longer enqueues to `playwright_queue`. Mirror how `apps/booli`/existing code already calls `webscraper` so signatures match. Do NOT delete the local-Chromium code path or the `crawler-playwright` compose service — leave them as a one-line revert (hard removal is Phase 24/25).
    3. Read Oxylabs creds from the existing `.env` via the established settings/config mechanism — do NOT hardcode `OXYLABS_*` values, and do NOT echo them anywhere.
    4. Validate the file parses: `docker exec hemnet-crawler python -c "import ast,sys; ast.parse(open('/app/apps/hemnet/tasks.py').read()); print('PARSE_OK')"` (read-only; does not restart anything).
    5. Commit ONLY to the feature branch: `git add apps/hemnet/tasks.py && git commit -m "feat(hemnet): route search fetch through Oxylabs webscraper (FETCH-01); stop enqueuing to playwright_queue (FETCH-03)"`. Do NOT push to `main`, do NOT merge, do NOT run `bin/production.sh` or any deploy script.
  </action>
  <acceptance_criteria>
    - `cd /var/www/apps/hemnet && git branch --show-current` prints `feat/hemnet-oxylabs-fetch` (NOT `main`).
    - The Hemnet fetch path routes through webscraper: `grep -vE '^[[:space:]]*#' apps/hemnet/tasks.py | grep -c 'webscraper'` is >= 2 (a single top-of-file import alone is NOT sufficient — both Hemnet entry points must reference it).
    - BOTH Hemnet entry points are independently re-wired (not just `search_listings_2`): extract each function body and confirm it references `webscraper` and no longer dispatches to `playwright_queue`. Verify per-function, e.g. `awk '/def search_listings_2/{f=1} /def search_ad_cost_2/{f=2} f==1' apps/hemnet/tasks.py | grep -c webscraper` is >= 1 AND `awk '/def search_ad_cost_2/{f=1} f' apps/hemnet/tasks.py | grep -c webscraper` is >= 1. Record both function bodies' grep output in the SUMMARY. (If `search_ad_cost_2` genuinely shares the same fetch helper as `search_listings_2`, document that the helper is the single re-wired chokepoint and both call it.)
    - The Hemnet fetch path no longer enqueues to playwright_queue: in the post-edit `apps/hemnet/tasks.py`, no active (non-comment) line dispatches the Hemnet fetch to `playwright_queue` — verify by inspecting `grep -nE 'playwright_queue|apply_async|\.delay\(' apps/hemnet/tasks.py` and confirming any remaining `playwright_queue` reference is commented-out / not on the search_listings_2 / search_ad_cost_2 fetch path. Record the grep output in the SUMMARY.
    - File parses: the `ast.parse` check prints `PARSE_OK`.
    - Exactly one commit on the feature branch; `git log main..feat/hemnet-oxylabs-fetch --oneline | wc -l` >= 1, and `git rev-parse main` equals the HEAD recorded in 23-SNAPSHOT.md (main untouched).
    - No secret committed: `git show feat/hemnet-oxylabs-fetch | grep -Eic 'OXYLABS_[A-Z]+\s*=\s*["'\'']?[A-Za-z0-9]|doadmin:'` prints `0`.
  </acceptance_criteria>
  <verify>
    <automated>ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89 'cd /var/www/apps/hemnet && test "$(git branch --show-current)" = "feat/hemnet-oxylabs-fetch" && test "$(grep -vE "^[[:space:]]*#" apps/hemnet/tasks.py | grep -c webscraper)" -ge 2 && docker exec hemnet-crawler python -c "import ast; ast.parse(open(\"/app/apps/hemnet/tasks.py\").read()); print(\"PARSE_OK\")"'</automated>
  </verify>
  <done>tasks.py on branch `feat/hemnet-oxylabs-fetch` routes the Hemnet fetch through Oxylabs webscraper and no longer enqueues that path to playwright_queue; file parses; committed to the branch only; team main untouched; no secrets committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → team prod droplet (SSH) | We mutate a team-owned, KNOWN-COMPROMISED live prod box |
| droplet → team git remote (tt7676/hem-bol-scrapers) | A branch push could leak secrets or hit `main` |
| `.env` / process env → logs, snapshot, repo, commit | `OXYLABS_*` + `DATABASE_URL` live on a compromised host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information disclosure | `OXYLABS_*` / `DATABASE_URL` leaking into 23-SNAPSHOT.md, the commit, or logs | mitigate | Snapshot/commit grep gates assert zero secret patterns; creds read via existing settings mechanism, never hardcoded or echoed |
| T-23-02 | Tampering | Accidental edit/push to team `main` or triggering their deploy | mitigate | All edits on `feat/hemnet-oxylabs-fetch`; acceptance asserts current branch != main and `git rev-parse main` == pre-edit HEAD; no push/merge/deploy-script run |
| T-23-03 | Denial of service | Disrupting the live shared scraper/DB while editing | mitigate | Task 1 is read-only; Task 2 only edits a file + commits + a read-only `ast.parse` in an existing container — no container restart, no rebuild, no queue change at runtime |
| T-23-04 | Elevation / compromise | Operating on a malware-infected host (Kinsing/kdevtmpfsi) | accept | Phase 23 does not remediate (Phase 24 does); we add no new exposed service and run only scoped commands as the existing root user |
| T-23-05 | Repudiation | Unclear what changed / no rollback path | mitigate | 23-SNAPSHOT.md records exact pre-edit state + git ref + one-line revert recipe before any edit |
</threat_model>

<verification>
- 23-SNAPSHOT.md exists, >= 20 non-blank lines, zero secret patterns.
- On the droplet: current branch is `feat/hemnet-oxylabs-fetch`; `apps/hemnet/tasks.py` non-comment lines reference `webscraper` (>=1) and the Hemnet fetch path no longer routes to `playwright_queue`.
- `ast.parse` of the edited file prints `PARSE_OK`.
- `git rev-parse main` == the HEAD recorded in the snapshot (main untouched); one commit exists on the branch; no secrets in the commit.
</verification>

<success_criteria>
The Hemnet fetch is re-wired to the Oxylabs `webscraper.py` path on an isolated feature branch, with an exact rollback snapshot recorded, no secrets exposed, no container/image/queue changed at runtime, and team `main` untouched. Ready for 23-02 (build + verify).
</success_criteria>

<output>
After completion, create `.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-01-SUMMARY.md` — include the branch name, the commit SHA, the recorded `grep` output proving the playwright_queue path is gone from the Hemnet fetch, and confirmation that main == pre-edit HEAD.
</output>
