---
phase: 23-fix-hemnet-capability-oxylabs-fetch
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - ".planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-PLAYWRIGHT-GATEOFF.md"  # local record of gate-off + one-line revert recipe
  # On-droplet effect (not a repo file): `hemnet-crawler-playwright` container STOPPED (compose service + local-Chromium code left in place)
autonomous: false   # stops a container on a team-owned live prod droplet
requirements: [FETCH-03]
user_setup: []

must_haves:
  truths:
    - "The hemnet-crawler-playwright container is stopped and no longer running"
    - "The ~6.2 GB RAM driver is freed (host used-memory drops materially)"
    - "The crawler-playwright compose service definition AND the local-Chromium code path remain in place (one-line revert)"
    - "booli / redis / metabase / beat / django / writer containers and the default & writer queues are undisturbed"
    - "A documented one-line revert recipe exists to bring Playwright back if the Oxylabs path regresses"
  artifacts:
    - path: ".planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-PLAYWRIGHT-GATEOFF.md"
      provides: "Record of the gate-off (before/after RAM, container state) + the reversibility recipe"
      min_lines: 15
  key_links:
    - from: "hemnet-crawler-playwright container"
      to: "STOPPED (RAM freed)"
      via: "docker stop, service + code left intact"
      pattern: "crawler-playwright"
---

<objective>
Now that the Oxylabs path is proven (23-02: ~0 403s on a ~200-page crawl), gate off the self-hosted Playwright worker REVERSIBLY: stop the `hemnet-crawler-playwright` container so it frees its ~6.2 GB RAM, while LEAVING the compose service definition and the local-Chromium code path in place as a one-line revert. Confirm nothing else is disturbed and document the exact revert recipe.

Purpose: FETCH-03 (retire self-hosted Playwright as a resource driver — gated off, not hard-removed). This frees the RAM that makes Phase 25 right-sizing (4 vCPU / 8 GB slug) plausible.
Output: `hemnet-crawler-playwright` stopped; host RAM freed; a local `23-PLAYWRIGHT-GATEOFF.md` with before/after evidence + revert recipe.

NOTE: Hard removal of the compose service + local-Chromium code + permanent RAM reclaim is DEFERRED to Phase 24/25 (23-CONTEXT Deferred Ideas). Do NOT delete anything here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-CONTEXT.md
@.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-02-SUMMARY.md
@.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-SNAPSHOT.md
@docs/price-scraper-droplet-runbook.md
@docs/price-scraper-droplet-audit.md
@.planning/phases/22-deep-dive-audit/22-EVIDENCE.md

**SSH (mandatory flags):**
```
ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89
```

**Key facts:**
- `hemnet-crawler-playwright` = `celery -A config.celery worker --concurrency=8 -Q playwright_queue`, the single biggest RAM consumer at 6.228 GiB / 15.61 GiB (22-EVIDENCE §4, §1).
- The Hemnet fetch no longer routes to `playwright_queue` (done in 23-01, proven in 23-02), so stopping this worker drops no live Hemnet work.
- Shared `hemnet-redis` serves `default`/`playwright_queue`/`writer_queue` — do NOT touch redis or the booli/writer queues.
- The disabled `restart_crawler_playwright.sh` cron line is already commented out (22-EVIDENCE §3) — so cron will not auto-restart the container.

**LOCKED guardrails (23-CONTEXT):**
- Gate off REVERSIBLY: `docker stop` the container; LEAVE the `crawler-playwright` compose service + local-Chromium code in place. Hard removal = Phase 24/25.
- Reversibility caveat to document: a future `docker compose up -d` / `bin/restart.sh` would restart the container — the durable gate is the routing change (no Hemnet work flows to playwright_queue). Note this in the revert record.
- Do NOT `docker prune`, do NOT edit `docker-compose.yml`, do NOT stop booli/core/redis/metabase/django/writer/beat. Keep secrets out of logs/repo. Host is compromised — add no exposed service.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stop the Playwright container reversibly + confirm RAM freed and nothing else disturbed</name>
  <read_first>
    On the droplet: confirm 23-02 succeeded (open `.planning/phases/23-.../23-02-SUMMARY.md`; the ~200-page crawl was ~0 403s). Read the current state: `docker ps --format '{{.Names}}\t{{.Status}}\t{{.CreatedAt}}' | grep -v '^#'` and `free -h` and `docker stats --no-stream hemnet-crawler-playwright`. Re-read the `crawler-playwright` service block in `/var/www/apps/hemnet/docker-compose.yml` (you will NOT edit it — just confirm it stays). Read `docs/price-scraper-droplet-runbook.md`.
  </read_first>
  <what-built>
    The `hemnet-crawler-playwright` container stopped (freeing ~6.2 GB RAM), with its compose service definition and local-Chromium code path left fully in place as a one-line revert, and every other container/queue undisturbed.
  </what-built>
  <action>
    On the droplet:
    1. Record BEFORE state: `free -h` (note used/available), `docker stats --no-stream --format '{{.Name}}\t{{.MemUsage}}'`, and `docker ps --format '{{.Names}}\t{{.Status}}\t{{.CreatedAt}}'`.
    2. Stop ONLY the Playwright worker: `docker stop hemnet-crawler-playwright` (this STOPS, does not remove — `docker compose stop crawler-playwright` is equivalent; do NOT use `rm`/`down`/`prune`).
    3. Record AFTER state: `free -h` again (expect used-memory to drop by ~6 GB / available to rise), and `docker ps -a --format '{{.Names}}\t{{.Status}}'` (the container should show `Exited`, still present — NOT removed).
    4. Confirm nothing else moved: `hemnet-redis`, `hemnet-metabase`, `hemnet-beat`, `hemnet-django`, `hemnet-writer`, and the booli-relevant containers retain their `Up` status and original `CreatedAt`; the `crawler-playwright` SERVICE still exists in `docker-compose.yml` (grep it).
    5. Write the LOCAL record `.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-PLAYWRIGHT-GATEOFF.md`: before/after `free -h` used+available, the freed RAM delta, the `docker ps -a` line showing the container Exited-but-present, confirmation the compose service + code remain, and the EXACT one-line revert recipe (`docker start hemnet-crawler-playwright` — or `docker compose up -d crawler-playwright`) plus the caveat that a full `bin/restart.sh` / `docker compose up -d` would also restart it (the durable gate is the 23-01 routing change). No secrets in the file.
  </action>
  <how-to-verify>
    1. SSH in. `docker ps -a --format '{{.Names}}\t{{.Status}}'` shows `hemnet-crawler-playwright` as `Exited` and STILL LISTED (not removed).
    2. `docker ps --format '{{.Names}}'` shows all the OTHER containers (`hemnet-redis`, `-metabase`, `-beat`, `-django`, `-writer`, `-crawler`) still `Up`.
    3. `free -h` shows used-memory dropped ~6 GB vs the before snapshot.
    4. `grep -c 'crawler-playwright' /var/www/apps/hemnet/docker-compose.yml` >= 1 (service definition left intact).
    5. Open `23-PLAYWRIGHT-GATEOFF.md` — before/after RAM + revert recipe present, no secrets.
  </how-to-verify>
  <acceptance_criteria>
    - `docker ps -a` shows `hemnet-crawler-playwright` with status `Exited` AND it is still listed (NOT removed): `docker ps -a --format '{{.Names}}' | grep -c '^hemnet-crawler-playwright$'` == 1, and `docker ps --format '{{.Names}}' | grep -c '^hemnet-crawler-playwright$'` == 0.
    - RAM freed: `free -h` used-memory after is materially lower (~6 GB) than before; recorded in the gate-off file.
    - Other containers undisturbed: `docker ps --format '{{.Names}}'` still lists `hemnet-redis`, `hemnet-metabase`, `hemnet-beat`, `hemnet-django`, `hemnet-writer`, `hemnet-crawler` all `Up` with original CreatedAt.
    - Reversibility intact: `grep -c 'crawler-playwright' /var/www/apps/hemnet/docker-compose.yml` >= 1 (compose service NOT deleted); local-Chromium code path not deleted; `docker-compose.yml` not edited (`cd /var/www/apps/hemnet && git status --porcelain docker-compose.yml` empty).
    - `.planning/phases/23-.../23-PLAYWRIGHT-GATEOFF.md` exists, >= 15 non-blank lines, documents before/after RAM + the one-line revert recipe, zero secret patterns (`grep -Eic 'OXYLABS_|DATABASE_URL|doadmin|DJANGO_SECRET'` == 0).
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-PLAYWRIGHT-GATEOFF.md" && test "$(grep -Eic 'OXYLABS_|DATABASE_URL|doadmin|DJANGO_SECRET' .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-PLAYWRIGHT-GATEOFF.md)" = "0" && test "$(grep -vc '^[[:space:]]*$' .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-PLAYWRIGHT-GATEOFF.md)" -ge 15 && ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89 'test "$(docker ps --format "{{.Names}}" | grep -c "^hemnet-crawler-playwright$")" = "0" && test "$(docker ps -a --format "{{.Names}}" | grep -c "^hemnet-crawler-playwright$")" = "1" && test "$(grep -c crawler-playwright /var/www/apps/hemnet/docker-compose.yml)" -ge 1' && echo GATEOFF_OK</automated>
  </verify>
  <resume-signal>Type "approved" once `docker ps -a` shows crawler-playwright Exited-but-present, RAM freed ~6 GB, other containers Up, and the compose service intact — or describe any deviation.</resume-signal>
  <done>hemnet-crawler-playwright is stopped (RAM freed ~6.2 GB), the compose service + local-Chromium code remain in place with a documented one-line revert, and no other container/queue/compose-file was disturbed. FETCH-03 met (reversible gate-off).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → team prod droplet (stop container) | Stopping a container on a live shared box |
| revert record / `.env` → repo, logs | secrets on a compromised host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-11 | Denial of service | Stopping the wrong container, or `down`/`prune` taking out redis/booli/writer | mitigate | Only `docker stop hemnet-crawler-playwright`; acceptance asserts every other container stays `Up` with original CreatedAt and the container is Exited-but-present (not removed) |
| T-23-12 | Tampering / irreversibility | Hard-removing the service or code so revert is impossible | mitigate | No `docker-compose.yml` edit (git status clean for that file), no code deletion; `grep -c crawler-playwright` asserts the service remains; one-line revert recipe documented |
| T-23-13 | Information disclosure | secrets leaking into the gate-off record/logs | mitigate | Gate-off file grep-gated to zero secret patterns; only `free -h` / `docker ps` output recorded |
| T-23-14 | Elevation / compromise | Acting on the malware-infected host | accept | Phase 24 remediates; this plan stops one container and adds no exposed service |
| T-23-15 | Repudiation | No record of what was stopped / how to undo | mitigate | 23-PLAYWRIGHT-GATEOFF.md records before/after RAM + exact revert recipe + the restart-script caveat |
</threat_model>

<verification>
- `hemnet-crawler-playwright` is `Exited` and still listed (not removed); RAM freed ~6 GB.
- All other containers (`redis`, `metabase`, `beat`, `django`, `writer`, `crawler`) still `Up`, original CreatedAt.
- `docker-compose.yml` `crawler-playwright` service intact and file unedited; local-Chromium code not deleted.
- `23-PLAYWRIGHT-GATEOFF.md` documents before/after RAM + one-line revert recipe, zero secrets.
</verification>

<success_criteria>
Self-hosted Playwright is gated off reversibly: the container is stopped (RAM driver removed), the compose service + code remain as a one-line revert, nothing else on the live box is disturbed, and the revert recipe is documented. FETCH-03 complete; the freed ~6.2 GB enables Phase 25 right-sizing.
</success_criteria>

<output>
After completion, create `.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-03-SUMMARY.md` — include the before/after `free -h` RAM, the `docker ps -a` evidence that the container is Exited-but-present, confirmation the compose service is intact, and the documented revert recipe.
</output>
