---
phase: 23-fix-hemnet-capability-oxylabs-fetch
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - ".planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-VERIFICATION-CRAWL.md"  # local crawl report (403 count + Oxylabs cost)
  # On-droplet effects (not repo files): rebuild of the `hemnet` Docker image; restart of the targeted hemnet worker container only
autonomous: false   # rebuilds a prod image, restarts a prod worker, and runs the operator-pre-approved PAID Oxylabs crawl
requirements: [FETCH-01, FETCH-02]
user_setup: []

must_haves:
  truths:
    - "The hemnet Docker image is rebuilt from the feature-branch code and the targeted Hemnet worker runs the new Oxylabs fetch code"
    - "booli / redis / metabase / beat / django and the default & writer queues are undisturbed (untouched containers retain their original uptime)"
    - "A bounded verification crawl of ~200 Hemnet pricing pages (N>=200) completes via the Oxylabs path"
    - "The verification crawl 403 block rate is ~0 (<= ~1% of pages)"
    - "Oxylabs call count + cost for the crawl is reported"
    - "No secret value is written to the crawl log/report or the repo"
  artifacts:
    - path: ".planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-VERIFICATION-CRAWL.md"
      provides: "Verification-crawl result: pages crawled, 403 count/rate, Oxylabs calls + cost"
      min_lines: 15
  key_links:
    - from: "rebuilt hemnet image / restarted worker"
      to: "apps/core/webscraper.py (Oxylabs)"
      via: "the new tasks.py fetch path now running in-container"
      pattern: "webscraper"
    - from: "verification crawl"
      to: "Hemnet pricing pages via Oxylabs"
      via: "~200-page run, 403 count measured"
      pattern: "403"
---

<objective>
Deploy the 23-01 re-wire and prove it works. Rebuild ONLY the `hemnet` Docker image from the feature branch, restart ONLY the Hemnet worker container that executes the fetch (leaving booli/redis/metabase/beat/django and the booli/writer queues untouched), then run the operator-pre-approved bounded verification crawl (~200 Hemnet pricing pages) and confirm the 403 block rate drops to ~0. Report the Oxylabs call count + cost.

Purpose: FETCH-01 (the fetch actually runs through Oxylabs in production) + FETCH-02 (verification crawl returns ~0 403s).
Output: A rebuilt `hemnet` image running the new code on the targeted worker; a local `23-VERIFICATION-CRAWL.md` report (pages, 403s, Oxylabs cost).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-CONTEXT.md
@.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-01-SUMMARY.md
@docs/price-scraper-droplet-runbook.md
@docs/price-scraper-droplet-audit.md
@.planning/phases/22-deep-dive-audit/22-EVIDENCE.md

**SSH (mandatory flags):**
```
ssh -o IdentitiesOnly=yes -o IdentityAgent=none -o ControlMaster=no -o ControlPath=none -o ConnectTimeout=12 -i ~/.ssh/droplet_ed25519 root@170.64.181.89
```

**Key facts:**
- All app containers (`hemnet-django`, `-crawler`, `-crawler-playwright`, `-writer`, `-beat`) are built from the SAME `hemnet` image and bind-mount the repo (`volumes: - .:/app`, 22-EVIDENCE §1). Because of the bind mount, on-disk code is already visible in containers; a worker RESTART is what reloads Python. Rebuilding the `hemnet` image is the LOCKED delivery decision (23-CONTEXT) — honor it, then restart only the targeted worker.
- `search_listings_2` runs on the default queue (`hemnet-crawler`); `hemnet-writer` persists results on `writer_queue`. Confirm on the box which worker executes the re-wired fetch before restarting.
- Hemnet beat task is DISABLED — trigger the crawl MANUALLY (preferred — do not change the prod beat schedule).
- 121 MB `scraper_log_export/HEMNET_http_blocking_signals.log` is the historical 403 signal; the verification crawl proves the post-fix rate.

**LOCKED guardrails (23-CONTEXT):**
- The ~200-page crawl (N>=200) is the ONE paid Oxylabs run pre-authorized for this phase. Anything beyond ~200 pages needs a fresh per-run go-ahead. Report calls + cost after.
- Rebuild ONLY the `hemnet` image. NO `docker prune`. Do NOT stop/recreate booli/core/redis/metabase/django/writer/beat or the `crawler-playwright` container in this plan (stopping Playwright is 23-03).
- Keep `OXYLABS_*` / `DATABASE_URL` out of the crawl log/report and the repo. Host is compromised — add no exposed service.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rebuild the hemnet image + restart only the targeted Hemnet worker</name>
  <read_first>
    On the droplet: `cd /var/www/apps/hemnet && git branch --show-current` (must be `feat/hemnet-oxylabs-fetch` from 23-01). Read `docker-compose.yml` to confirm the image name is `hemnet` and identify which service/container runs `search_listings_2` (the default-queue `crawler`, and `writer` for persistence). Re-read `apps/hemnet/tasks.py` to confirm the re-wire is present (`grep -vE '^[[:space:]]*#' apps/hemnet/tasks.py | grep -c webscraper` >= 1). Read `docs/price-scraper-droplet-runbook.md` and `.planning/phases/23-.../23-01-SUMMARY.md`.
  </read_first>
  <what-built>
    Rebuilt the `hemnet` Docker image from the `feat/hemnet-oxylabs-fetch` branch and restarted ONLY the Hemnet worker(s) that execute the fetch — leaving every other container (booli is a module in the same image, but redis/metabase/beat/django/writer and crawler-playwright containers) at their original uptime, and the booli/writer queues undisturbed.
  </what-built>
  <action>
    On the droplet, ensure the feature branch is checked out (`git branch --show-current` == `feat/hemnet-oxylabs-fetch`).
    1. Capture pre-state for the disturb-nothing check: `docker ps --format '{{.Names}}\t{{.Status}}\t{{.CreatedAt}}' | grep -v '^#'` — save the original Status/CreatedAt of `hemnet-redis`, `hemnet-metabase`, `hemnet-beat`, `hemnet-crawler-playwright`, and the booli-relevant containers.
    2. Rebuild ONLY the hemnet image (LOCKED decision): `docker compose build crawler` (or the equivalent that rebuilds the shared `hemnet` image for the worker service) — do NOT pass `--no-cache` unnecessarily, do NOT `docker prune`, do NOT build metabase/redis.
    3. Recreate ONLY the targeted worker container(s) so the new code loads: `docker compose up -d --no-deps crawler` (add `writer` only if it executes/persists the fetch). The `--no-deps` flag is REQUIRED so dependencies (redis etc.) are not recreated. Do NOT recreate `crawler-playwright`, `metabase`, `redis`, `beat`, or `django` unless one of them is the worker that runs the fetch (confirm from compose).
    4. Confirm the restarted worker runs the new code: `docker exec hemnet-crawler grep -c webscraper /app/apps/hemnet/tasks.py` >= 1, and the worker started cleanly: `docker logs --since 2m hemnet-crawler 2>&1 | tail -30` shows the celery worker ready with no import error.
    5. Confirm nothing else was disturbed: re-run the `docker ps` format command and verify `hemnet-redis`, `hemnet-metabase`, `hemnet-beat`, `hemnet-crawler-playwright` retain their ORIGINAL `CreatedAt` (not recreated), and the writer/booli queues are intact.
  </action>
  <how-to-verify>
    1. SSH in. Run `docker ps --format '{{.Names}}\t{{.Status}}\t{{.CreatedAt}}'`.
    2. Confirm `hemnet-crawler` (the targeted worker) has a recent CreatedAt (just restarted) and is `Up`.
    3. Confirm `hemnet-redis`, `hemnet-metabase`, `hemnet-beat`, `hemnet-crawler-playwright` show their ORIGINAL CreatedAt (2026-04-18, NOT just now) — proving they were not recreated.
    4. Confirm `docker exec hemnet-crawler grep -c webscraper /app/apps/hemnet/tasks.py` prints >= 1.
    5. Confirm `docker logs --since 2m hemnet-crawler` ends with a celery "ready" line and no traceback.
  </how-to-verify>
  <acceptance_criteria>
    - Targeted worker recreated: `hemnet-crawler` (and `hemnet-writer` only if used) has a fresh `CreatedAt` and is `Up`.
    - Untouched containers retain original `CreatedAt`: `hemnet-redis`, `hemnet-metabase`, `hemnet-beat`, `hemnet-crawler-playwright` still show the pre-existing creation timestamp (NOT recreated).
    - `docker exec <targeted-worker> grep -c webscraper /app/apps/hemnet/tasks.py` >= 1.
    - `docker logs --since 5m <targeted-worker>` contains no `Traceback`/`ImportError` and shows the worker ready.
    - No `docker prune`/`down` was run (no container removed): `docker ps -a` still lists all 7 original containers + the stale orphan (8 total, as in 22-EVIDENCE §1).
  </acceptance_criteria>
  <resume-signal>Type "approved" once `docker ps` shows the targeted worker recreated on new code and redis/metabase/beat/crawler-playwright untouched — or describe what looks wrong.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the pre-approved ~200-page verification crawl + report 403 rate and Oxylabs cost (FETCH-02)</name>
  <read_first>
    On the droplet, re-read `apps/hemnet/tasks.py` and `apps/core/webscraper.py` to confirm how to invoke a bounded Hemnet pricing-page fetch through Oxylabs (a management command, a one-off `manage.py shell` call, a small standalone script, or a manually-triggered task run — your discretion per 23-CONTEXT). Identify how a 403 surfaces (HTTP status in the webscraper response / the `HEMNET_http_blocking_signals.log` signature) so you can count them. Read `docs/price-scraper-droplet-runbook.md`.
  </read_first>
  <what-built>
    A bounded, operator-pre-approved verification crawl of ~200 Hemnet pricing pages (N>=200) issued through the new Oxylabs fetch path, with the 403 count and Oxylabs call count + cost measured and recorded.
  </what-built>
  <action>
    1. Invoke a bounded Hemnet pricing-page crawl of N>=200 pages through the re-wired Oxylabs path (manual trigger preferred — do NOT re-enable the beat schedule). Hard-cap the run at ~200 pages; do NOT exceed the pre-authorized bound (anything beyond ~200 needs a fresh go-ahead).
    2. Count outcomes: number of pages fetched, number returning HTTP 403 (or the blocking-signal signature), and the number of successful (200) responses. Capture the per-status tally.
    3. Capture the Oxylabs spend: the number of Oxylabs calls the run made and the resulting cost (derive from the call count at the known per-call rate, or from the Oxylabs dashboard if available). Keep the actual `OXYLABS_*` credential values OUT of the report.
    4. Write the LOCAL report `.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-VERIFICATION-CRAWL.md`: pages crawled (N), 403 count + rate (%), 200 count, Oxylabs calls, estimated cost, and the date/time. Do NOT paste raw page HTML or any secret.
    5. If the 403 rate is NOT ~0 (i.e. > ~1%), STOP and report — do not paper over it; the re-wire is not proven and 23-03 (gating off Playwright) must not proceed.
  </action>
  <how-to-verify>
    1. Open `.planning/phases/23-.../23-VERIFICATION-CRAWL.md`.
    2. Confirm pages crawled N >= 200.
    3. Confirm reported 403 rate <= ~1% (ideally 0).
    4. Confirm Oxylabs call count + cost are stated.
    5. Confirm no `OXYLABS_*` / `DATABASE_URL` value appears in the report.
  </how-to-verify>
  <acceptance_criteria>
    - `23-VERIFICATION-CRAWL.md` exists, >= 15 non-blank lines, states N (pages) >= 200.
    - Reported 403 block rate is ~0 (<= ~1% of pages); the report shows the 403 count and the 200 count explicitly.
    - Report states the Oxylabs call count AND the estimated cost for the run.
    - Zero secrets: `grep -Eic 'OXYLABS_|DATABASE_URL|doadmin|DJANGO_SECRET' .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-VERIFICATION-CRAWL.md` prints `0`.
    - The crawl was bounded to ~200 pages (not an unbounded production run); the beat schedule was NOT re-enabled (confirm the `Scrape hemnet.se` PeriodicTask is still `enabled=False`).
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-VERIFICATION-CRAWL.md" && test "$(grep -Eic 'OXYLABS_|DATABASE_URL|doadmin|DJANGO_SECRET' .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-VERIFICATION-CRAWL.md)" = "0" && grep -Eiq '403' .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-VERIFICATION-CRAWL.md && grep -Eiq 'oxylabs|cost|call' .planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-VERIFICATION-CRAWL.md && echo CRAWL_REPORT_OK</automated>
  </verify>
  <resume-signal>Type "approved" once the crawl report shows N>=200 pages at ~0% 403 with Oxylabs cost stated — or describe the 403 result if it is not ~0.</resume-signal>
  <done>A bounded ~200-page Hemnet crawl through Oxylabs returns ~0 403s, with calls + cost reported and no secrets leaked. FETCH-02 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → team prod droplet (rebuild/restart) | Recreating a container on a live shared box risks collateral disruption |
| verification crawl → Oxylabs (paid) | A paid run that could overspend beyond the authorized bound |
| crawl output / `.env` → report, logs, repo | `OXYLABS_*` + `DATABASE_URL` on a compromised host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Denial of service | Rebuild/restart collaterally recreating redis/metabase/beat/booli or stopping the shared broker | mitigate | `docker compose build` + `up -d --no-deps <worker>` targets only the fetch worker; acceptance asserts redis/metabase/beat/crawler-playwright keep original CreatedAt and all 8 containers still exist; no prune/down |
| T-23-07 | Denial of service / cost | Verification crawl exceeding the ~200-page authorized Oxylabs bound | mitigate | Run hard-capped at ~200 pages; calls + cost reported; anything beyond requires fresh go-ahead (standing no-Oxylabs-without-approval rule) |
| T-23-08 | Information disclosure | `OXYLABS_*` / `DATABASE_URL` leaking into the crawl log or 23-VERIFICATION-CRAWL.md | mitigate | Creds read via existing config; report grep-gated to zero secret patterns; no raw HTML/headers pasted |
| T-23-09 | Tampering | Re-enabling the prod beat schedule as a side effect of triggering the crawl | mitigate | Manual trigger only; acceptance asserts `Scrape hemnet.se` PeriodicTask stays `enabled=False` |
| T-23-10 | Elevation / compromise | Running the paid crawl from a malware-infected host | accept | Phase 24 remediates the malware; this run adds no exposed service and uses only the existing Oxylabs egress |
</threat_model>

<verification>
- Targeted worker recreated on new code (`grep -c webscraper` in-container >= 1, clean logs); redis/metabase/beat/crawler-playwright retain original CreatedAt; all 8 containers still present.
- `23-VERIFICATION-CRAWL.md`: N >= 200 pages, 403 rate ~0 (<= ~1%), Oxylabs calls + cost stated, zero secrets.
- Beat schedule unchanged (`Scrape hemnet.se` still disabled).
</verification>

<success_criteria>
The re-wired Hemnet fetch runs in production on the rebuilt image, a bounded ~200-page crawl confirms ~0 403s through Oxylabs with cost reported, and no other container/queue/schedule/secret was disturbed. FETCH-01 + FETCH-02 proven; ready for 23-03 (gate off Playwright).
</success_criteria>

<output>
After completion, create `.planning/phases/23-fix-hemnet-capability-oxylabs-fetch/23-02-SUMMARY.md` — include the rebuilt-image confirmation, the disturb-nothing `docker ps` evidence, and the crawl headline (N pages, 403 rate, Oxylabs calls + cost).
</output>
