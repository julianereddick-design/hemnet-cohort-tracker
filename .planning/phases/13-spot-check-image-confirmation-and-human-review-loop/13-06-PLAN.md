---
phase: 13-spot-check-image-confirmation-and-human-review-loop
plan: 06
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05]
files_modified:
  - deploy-instructions.md
autonomous: false
requirements: []
covers_decisions: [D-14]
user_setup:
  - service: droplet
    why: "Run the migration + install the two new crontab lines + populate the new env vars"
    env_vars:
      - name: SLACK_BOT_TOKEN
        source: "from SLACK-REVIEW-SETUP.md (Plan 03)"
      - name: SLACK_REVIEW_CHANNEL
        source: "Slack channel id for the review queue"
      - name: SLACK_ALLOWED_REACTORS
        source: "comma-separated Slack user id(s) authorized to confirm removals (operator's own id)"

must_haves:
  truths:
    - "The Phase 13 migration has run on the droplet (spotcheck_review + spotcheck_removed_pairs exist)"
    - "The weekly gate cron fires after cohort-create (Mon 06:30 UTC) and the daily reaction-poller cron fires once daily"
    - "deploy-instructions.md documents the go-live: env vars, both crontab lines, the migration step, and the operator review/removal runbook"
  artifacts:
    - path: "deploy-instructions.md"
      provides: "Phase 13 go-live + operator runbook section"
      contains: "spotcheck-reaction-poller"
  key_links:
    - from: "crontab (droplet)"
      to: "cohort-spotcheck-gate.js + spotcheck-reaction-poller.js"
      via: "weekly gate Mon 06:30 UTC + daily poller"
      pattern: "spotcheck-reaction-poller"
---

<objective>
Take the whole phase live (D-14): the Phase 12 gate ships WITH this phase's image-confirmation +
review loop. Document and stage the go-live — run the migration, install the weekly gate cron
(after cohort-create) + the new daily reaction-poller cron, set the three new env vars, and write
the operator runbook for triaging the review queue and the audited removals. The actual droplet
commands are a human checkpoint (git pull deploy; the operator runs them).

Purpose: Implements D-14 (go-live with this phase: weekly gate cron + daily poller cron).
Output: an updated `deploy-instructions.md` with the go-live steps + operator runbook, and an
operator checkpoint that runs the migration + installs the crons + populates env.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-spot-check-image-confirmation-and-human-review-loop/13-CONTEXT.md
@.planning/phases/13-spot-check-image-confirmation-and-human-review-loop/13-01-SUMMARY.md
@.planning/phases/13-spot-check-image-confirmation-and-human-review-loop/13-03-SUMMARY.md
@.planning/phases/13-spot-check-image-confirmation-and-human-review-loop/13-04-SUMMARY.md
@.planning/phases/13-spot-check-image-confirmation-and-human-review-loop/13-05-SUMMARY.md

<interfaces>
<!-- Existing crontab (deploy-instructions.md line 52): the Phase 12 gate line already documented -->
//   30 6 * * 1  cd /opt/hemnet-cohort-tracker && node cohort-spotcheck-gate.js  >> /var/log/hemnet/spotcheck-gate.log 2>&1
<!-- cohort-create runs Mon 06:00 UTC; the gate at 06:30 UTC; D-13 guard skips+alerts if this week's cohort isn't ready. -->
<!-- Deploy model (memory): git pull on the droplet, NOT file paste. -->
<!-- New scripts: migrate-spotcheck-phase13.js (Plan 01), spotcheck-reaction-poller.js (Plan 05). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the Phase 13 go-live + operator runbook in deploy-instructions.md</name>
  <read_first>
    - deploy-instructions.md (lines 44-55 — the existing Phase 12 gate crontab block + the surrounding crontab format) and (lines 188-217 — the existing spot-check operator runbook section to extend)
    - SLACK-REVIEW-SETUP.md (Plan 03) — referenced for the Slack-app + token + channel setup
    - .planning/phases/13-spot-check-image-confirmation-and-human-review-loop/13-CONTEXT.md (D-14, lines 78-79) — go-live sequencing
  </read_first>
  <action>
    Edit `deploy-instructions.md`. Add a "Phase 13 — image confirmation + review loop (go-live)"
    block that documents, in the existing crontab comment-then-line style:

    1. ENV VARS (in droplet `.env`, never committed):
       - `SLACK_BOT_TOKEN=xoxb-…` (review-queue bot — see SLACK-REVIEW-SETUP.md)
       - `SLACK_REVIEW_CHANNEL=C0……` (the review channel id)
       - `SLACK_ALLOWED_REACTORS=U0……` (comma-separated; the operator's own Slack user id — REQUIRED
         before relying on auto-removal; without it the poller allows ALL reactors as a first-run fallback)
       - optional `DHASH_THRESHOLD=6` (default; leave at 6 — raising it is deferred per CONTEXT)
       - Note `SLACK_WEBHOOK_URL` STAYS (Phase 12 threshold/fetch-failure alerts) — separate from SLACK_BOT_TOKEN.

    2. MIGRATION (one-time, idempotent): `node migrate-spotcheck-phase13.js` → creates
       spotcheck_review + spotcheck_removed_pairs.

    3. CRONTAB — two lines. The weekly gate line ALREADY exists (Mon 06:30 UTC); D-14 means it now
       does useful work (image confirmation + review queue), so it goes live with this phase. ADD
       the new daily poller line (D-10). Document both explicitly:

       ```
       # Weekly spot-check gate — Mon 06:30 UTC, after cohort-create (Mon 06:00 UTC).
       # dHash + advisory vision + review-queue post. D-13 guard skips+alerts on a stale cohort.
       30 6 * * 1  cd /opt/hemnet-cohort-tracker && node cohort-spotcheck-gate.js          >> /var/log/hemnet/spotcheck-gate.log 2>&1

       # Daily reaction poller — reads ✅/❌/❓ on open review messages, applies verdicts,
       # audits+removes confirmed mismatches from cohort_pairs. Runs daily at 12:00 UTC.
       0 12 * * *  cd /opt/hemnet-cohort-tracker && node spotcheck-reaction-poller.js      >> /var/log/hemnet/spotcheck-poller.log 2>&1
       ```

    4. OPERATOR RUNBOOK (extend the existing spot-check runbook section ~line 188). Cover:
       - **Reviewing the queue:** the gate posts a weekly digest of UNCERTAIN pairs + one message
         per CONFIRMED_MISMATCH to `SLACK_REVIEW_CHANNEL`. React: ✅ confirm mismatch (pair is
         audited + removed from cohort_pairs) · ❌ override, valid match (kept + recorded) · ❓
         unsure (left UNCERTAIN, not re-pinged). Only reactions from a SLACK_ALLOWED_REACTORS user count.
       - **What the poller does:** daily it applies those reactions; check
         `/var/log/hemnet/spotcheck-poller.log` or `cron_job_log` (result_summary has
         removed/kept/left/conflicts counts). A "contested" message (both ✅ and ❌) is left for a human.
       - **Recovering a wrongly-removed pair:** every removal is audited in `spotcheck_removed_pairs`
         (pair_id, booli_id, hemnet_id, cohort_id, verdicts, reactor, reason, removed_at). To restore,
         re-insert into cohort_pairs from the audit row (provide the SELECT to find it).
       - **dHash calibration:** the gate logs each pair's `dHash pair <id>: minDist=…` — after a few
         weeks review the distribution before considering raising DHASH_THRESHOLD (deferred).
       - **Detect:** stale-cohort skip fires `[WARNING] cohort-spotcheck-gate skipped: …` to Slack (D-13).
  </action>
  <verify>
    <automated>grep -q "spotcheck-reaction-poller" deploy-instructions.md && grep -q "migrate-spotcheck-phase13" deploy-instructions.md && grep -q "SLACK_ALLOWED_REACTORS" deploy-instructions.md && grep -q "spotcheck_removed_pairs" deploy-instructions.md && echo DEPLOY_DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - deploy-instructions.md contains `spotcheck-reaction-poller` (the daily poller crontab line)
    - Contains `migrate-spotcheck-phase13` (the migration step)
    - Contains `SLACK_BOT_TOKEN`, `SLACK_REVIEW_CHANNEL`, and `SLACK_ALLOWED_REACTORS`
    - Contains `spotcheck_removed_pairs` (the recovery runbook references the audit table)
    - Documents BOTH cron lines (weekly gate + daily poller) and notes SLACK_WEBHOOK_URL stays separate
  </acceptance_criteria>
  <done>deploy-instructions.md documents the go-live: env vars, migration, both crontab lines, and the operator review/removal/recovery/calibration runbook.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    The full Phase 13 pipeline is code-complete and committed: the migration
    (migrate-spotcheck-phase13.js), the two new tables' DDL, the dHash + adjudicate-fix libs, the
    Slack bot lib, the extended gate (dHash + advisory vision + ISO-week guard + review queue), and
    the daily reaction poller. deploy-instructions.md + SLACK-REVIEW-SETUP.md document every step.
    What remains is the operator-only droplet actions (no CLI can create the Slack app or edit the
    live crontab safely on the operator's behalf).
  </what-built>
  <how-to-verify>
    On the droplet, after `git pull`:
    1. Stand up the Slack app per SLACK-REVIEW-SETUP.md; put SLACK_BOT_TOKEN + SLACK_REVIEW_CHANNEL
       in `.env`; invite the bot to the channel.
    2. Set SLACK_ALLOWED_REACTORS to your own Slack user id in `.env`.
    3. Run the migration: `node migrate-spotcheck-phase13.js` → expect
       "Created table: spotcheck_review" / "Created table: spotcheck_removed_pairs".
    4. Smoke the Slack path: post the setup-test digest (command in SLACK-REVIEW-SETUP.md step 7);
       confirm it appears in the channel and you can react.
    5. Install the two crontab lines from deploy-instructions.md (weekly gate already present;
       ADD the daily poller). Confirm with `crontab -l`.
    6. Optional dry once: `node cohort-spotcheck-gate.js --cohort <currentWeek>` (explicit --cohort
       bypasses the D-13 guard) and confirm a review digest posts; then `node spotcheck-reaction-poller.js`
       and confirm it reads your reaction and (if you ✅'d a test) audits+removes.
  </how-to-verify>
  <resume-signal>Type "deployed" once the migration ran, both crons are installed (crontab -l confirms), and the env vars (incl. SLACK_ALLOWED_REACTORS) are set; or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → droplet .env + crontab | Secrets (SLACK_BOT_TOKEN) and the auto-removal authorization (SLACK_ALLOWED_REACTORS) are configured by hand on the droplet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-19 | Information Disclosure | SLACK_BOT_TOKEN in .env | mitigate | Token lives only in the git-ignored droplet `.env`; deploy doc + setup runbook both state "never commit / never log"; deploy is git pull (no secret in the repo). |
| T-13-20 | Elevation of Privilege | unset SLACK_ALLOWED_REACTORS | mitigate | Deploy doc marks SLACK_ALLOWED_REACTORS REQUIRED before relying on auto-removal; the poller's all-reactors fallback is documented as first-run only. Operator sets it to their own id at go-live. |
| T-13-21 | Tampering | recoverability of removals | mitigate | The runbook documents restoring a wrongly-removed pair from spotcheck_removed_pairs, so the destructive path is operationally reversible. |
</threat_model>

<verification>
- deploy-instructions.md documents the migration, both crontab lines, the three env vars, and the operator runbook (grep gate).
- The operator checkpoint confirms the migration ran + both crons installed + env set on the droplet.
</verification>

<success_criteria>
- D-14 go-live documented and staged: weekly gate + daily poller crons, migration, env vars.
- Operator can stand up the review queue and recover a wrongly-removed pair from the audit trail.
</success_criteria>

<output>
After completion, create `.planning/phases/13-spot-check-image-confirmation-and-human-review-loop/13-06-SUMMARY.md`
</output>
