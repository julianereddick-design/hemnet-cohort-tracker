---
phase: 15-sold-data-ingestion-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sold-config.js
  - lib/sold-parse.js
  - lib/sold-addr.js
autonomous: true
requirements: [SOLD-02, SOLD-03, MATCH-02]
must_haves:
  truths:
    - "A lib parser returns snake_case Booli sold-card records with object_type, sold_price, sold_date, sold_price_type, municipality, descriptive_area, living_area, rooms, floor, lat, long"
    - "Each Booli sold record carries is_title_transfer derived from soldPriceType (Lagfart and any non-market type → true)"
    - "A lib parser returns detail-only enriched Booli fields (rent, operating_cost, construction_year, agent_id, agency_id, tenure_form) or null when no SoldProperty node exists"
    - "A lib parser returns snake_case Hemnet SaleCard records (slug, detail_url, street_address, sold_at, final_price, fee, living_area, rooms, ...)"
    - "normAddr v2 normalizes the four spike-recovered false-negative formats"
  artifacts:
    - path: "lib/sold-config.js"
      provides: "SEGMENTS, isTitleTransfer, MARKET_SOLD_TYPES, daysAgoISO, agreement thresholds"
      contains: "SEGMENTS"
    - path: "lib/sold-parse.js"
      provides: "parseBooliSoldCards, parseBooliSoldDetail, parseHemnetSaleCards, parseSweNum, booliSoldMeta, hemnetSalesMeta"
      exports: ["parseBooliSoldCards", "parseBooliSoldDetail", "parseHemnetSaleCards"]
    - path: "lib/sold-addr.js"
      provides: "normAddr v2 (MATCH-02)"
      exports: ["normAddr"]
  key_links:
    - from: "lib/sold-parse.js"
      to: "lib/sold-config.js"
      via: "require('./sold-config') for isTitleTransfer"
      pattern: "require\\(['\"]\\./sold-config['\"]\\)"
    - from: "lib/sold-addr.js"
      to: "lib/spotcheck-evidence.js"
      via: "require('./spotcheck-evidence') for normStreet"
      pattern: "require\\(['\"]\\./spotcheck-evidence['\"]\\)"
---

<objective>
Lift the spike's pure config, parsers, and the `normAddr` v2 address normalizer into reusable `lib/` modules. These have no network or filesystem side effects, so they are the foundation every other Phase 15 module imports.

Purpose: The snake_case parser output shapes become the Phase 16 DB schema contract — they must be stable and lifted without renaming. `normAddr` v2 (MATCH-02) must recover the spike's known false-negative address formats and is unit-tested here so the Hemnet fetcher (Plan 05) can rely on it.
Output: `lib/sold-config.js`, `lib/sold-parse.js`, `lib/sold-addr.js`, each with an inline `--smoke` self-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sold-data-ingestion-library/15-CONTEXT.md
@.planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md

<interfaces>
<!-- Existing exports the executor depends on. Do not re-implement. -->

From lib/spotcheck-evidence.js (already exists):
```javascript
module.exports = { ..., pctDiff, normStreet, computeDeltas, ... };
// normStreet(s): lowercase, trim, collapse internal whitespace; returns null on null input.
```

Parser output shape (snake_case — Phase 16 DB column contract, DO NOT rename):
Booli card: booli_id, residence_url, street_address, object_type, sold_price, sold_date,
  sold_price_type, is_title_transfer, municipality, descriptive_area, living_area, rooms, floor, lat, long
Booli detail: booli_id, residence_id, rent, operating_cost, living_area, additional_area, plot_area,
  rooms, construction_year, agent_id, agency_id, object_type, sold_price, sold_price_type, tenure_form
Hemnet SaleCard: card_id, listing_id, slug, detail_url, street_address, sold_at, sold_at_label,
  asking_price, final_price, living_area, rooms, fee, housing_form, location_description,
  broker_name, broker_agency, lat, long
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift spike-config.js into lib/sold-config.js</name>
  <read_first>
    - scripts/spike-config.js (the verbatim source to lift, lines 1-69)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (sold-config section, lines 159-212)
    - lib/booli-fetch.js (lines 1-25 — module-header + 'use strict' convention to match)
  </read_first>
  <action>
    Create lib/sold-config.js by lifting scripts/spike-config.js VERBATIM (it has no require() dependencies, so no path changes needed). It MUST export, at minimum: SEGMENTS (with the two seeded segments `stockholm-apt` {family: 'APARTMENT', booli: {areaIds: 1, objectType: 'Lägenhet'}, hemnet: {locationId: 18031, itemType: 'bostadsratt'}} and `taby-villa` {family: 'HOUSE', booli: {areaIds: 20, objectType: 'Hus'}, hemnet: {locationId: 17793, itemType: null}}), MARKET_SOLD_TYPES (Set of 'Slutpris','Sista bud'), isTitleTransfer(soldPriceType) (returns false on null — conservative; true for any type not in MARKET_SOLD_TYPES), daysAgoISO(n, fromISO), PRICE_AGREE_PCT, AREA_AGREE_PCT, PRICE_BAND, SOLD_DATE_WINDOW_DAYS, READ_TIME_EXCLUDE_DAYS, DEFAULT_TARGET_PER_SEGMENT. Add `'use strict';` at top. Do NOT delete any exported constant — Plans 03/04/05 import them by name. Append an inline `--smoke` block per the shared `--smoke` pattern (PATTERNS.md lines 656-668) asserting: isTitleTransfer('Lagfart')===true, isTitleTransfer('Slutpris')===false, isTitleTransfer(null)===false, SEGMENTS['stockholm-apt'].family==='APARTMENT', SEGMENTS['taby-villa'].family==='HOUSE', daysAgoISO(0) returns a YYYY-MM-DD string.
  </action>
  <acceptance_criteria>
    - `node -e "const c=require('./lib/sold-config'); process.exit(c.SEGMENTS['stockholm-apt'] && c.SEGMENTS['taby-villa'] && c.isTitleTransfer('Lagfart')===true && c.isTitleTransfer('Slutpris')===false ? 0 : 1)"` exits 0
    - `node lib/sold-config.js --smoke` prints `smoke: N pass, 0 fail` and exits 0
    - `grep -c "Lägenhet" lib/sold-config.js` returns >=1
  </acceptance_criteria>
  <verify>
    <automated>node lib/sold-config.js --smoke</automated>
  </verify>
  <done>lib/sold-config.js exists, exports all named constants/functions, --smoke exits 0 with 0 failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift spike-sold-parse.js into lib/sold-parse.js with snake_case contract assertions</name>
  <read_first>
    - scripts/spike-sold-parse.js (the verbatim source to lift, lines 1-162)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (sold-parse section, lines 25-156)
    - lib/sold-config.js (the file created in Task 1 — the new require target)
    - lib/booli-fetch.js (lines 336-535 — the --smoke self-test block to mirror)
  </read_first>
  <behavior>
    - parseBooliSoldCards(emptyApollo) returns []
    - parseBooliSoldCards on a one-card apollo returns one record with snake_case keys incl. is_title_transfer correctly derived from sold_price_type
    - parseBooliSoldDetail(apolloWithNoSoldProperty) returns null
    - parseBooliSoldDetail extracts rent, operating_cost, construction_year, agent_id, agency_id, tenure_form from a SoldProperty node
    - parseHemnetSaleCards builds detail_url = `https://www.hemnet.se/salda/<slug>` from a SaleCard slug
    - parseSweNum handles Swedish-formatted numbers (spaces as thousands sep, comma decimal) and returns null on non-numeric
  </behavior>
  <action>
    Create lib/sold-parse.js by lifting scripts/spike-sold-parse.js. The ONLY dependency change is `require('./spike-config')` → `require('./sold-config')` (per PATTERNS.md lines 30-38). Keep every function and the snake_case output field names EXACTLY as in the spike — these names are the Phase 16 DB column contract and renaming breaks Phase 16/17. Preserve the `startsWith('searchSold(')` / `startsWith('displayAttributes(')` key-scan idioms verbatim (do NOT convert to exact-key lookups). Export: parseSweNum, parseBooliSoldCards, parseBooliSoldDetail, booliSoldMeta, parseHemnetSaleCards, hemnetSalesMeta. Add an inline `--smoke` block (mirror lib/booli-fetch.js lines 336-535 structure) covering the six behaviors above. Build minimal fixture apollo objects inline (no network).
  </action>
  <acceptance_criteria>
    - `node lib/sold-parse.js --smoke` prints `smoke: N pass, 0 fail` and exits 0
    - `grep -q "require('./sold-config')" lib/sold-parse.js` succeeds (config require rewired)
    - `grep -cv '^[[:space:]]*//' lib/sold-parse.js | head -1` (sanity, file non-empty); AND `grep -q "is_title_transfer" lib/sold-parse.js` succeeds (snake_case contract preserved)
    - `grep -q "sold_price_type" lib/sold-parse.js && grep -q "construction_year" lib/sold-parse.js` succeeds (enriched + classification fields present, SOLD-02/SOLD-03)
    - `node -e "const p=require('./lib/sold-parse'); process.exit(p.parseBooliSoldCards({})!==null && p.parseBooliSoldDetail({})===null ? 0:1)"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node lib/sold-parse.js --smoke</automated>
  </verify>
  <done>lib/sold-parse.js exists, requires ./sold-config, exports all six parsers, snake_case field names preserved, --smoke exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract normAddr v2 into lib/sold-addr.js and unit-test the four recovered formats (MATCH-02)</name>
  <read_first>
    - scripts/spike-hemnet-match.js (lines 31, 64-75 — normAddr v2 source + the normStreet require)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (normAddr section, lines 414-429)
    - lib/spotcheck-evidence.js (lines 49-60, 200-205 — normStreet definition + export to confirm signature)
  </read_first>
  <behavior>
    - normAddr("norrskensvägen 1 c") === "norrskensvägen 1c"  (space-before-unit-letter merge)
    - normAddr("Rindögatan 28, 3 tr") === "rindögatan 28"     (comma split drops floor suffix)
    - normAddr("X 10 / Y 6") === "x 10"                        (slash split, dual-corner address)
    - normAddr("58 och 58A") === "58"                          (" och " split, Booli-truncated number)
    - normAddr(null) === null
  </behavior>
  <action>
    Create lib/sold-addr.js containing the `normAddr` function lifted VERBATIM from scripts/spike-hemnet-match.js lines 64-75. It MUST `const { normStreet } = require('./spotcheck-evidence');` (do NOT inline normStreet — it must stay in sync with the cohort spot-check normalization, per PATTERNS.md lines 630-635). normAddr: split on the first comma, the first slash, and the first ` och ` (case-insensitive `/\s+och\s+/i`); apply normStreet (lowercase/trim/collapse whitespace); then merge a space between a house number and a trailing unit letter with `t.replace(/(\d+)\s+([a-zåäö])(?=\s|$)/g, '$1$2')`; return null on null input. Export `{ normAddr }`. Add an inline `--smoke` block asserting all five behaviors above (these are the exact spike-recovered false-negative formats from MATCH-02).
  </action>
  <acceptance_criteria>
    - `node lib/sold-addr.js --smoke` prints `smoke: 5 pass, 0 fail` (or more) and exits 0
    - `grep -q "require('./spotcheck-evidence')" lib/sold-addr.js` succeeds (normStreet imported, not inlined)
    - `node -e "const {normAddr}=require('./lib/sold-addr'); const t=[['norrskensvägen 1 c','norrskensvägen 1c'],['Rindögatan 28, 3 tr','rindögatan 28'],['X 10 / Y 6','x 10'],['58 och 58A','58']]; process.exit(t.every(([i,o])=>normAddr(i)===o)?0:1)"` exits 0
    - `node -e "const {normAddr}=require('./lib/sold-addr'); process.exit(normAddr(null)===null?0:1)"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node lib/sold-addr.js --smoke</automated>
  </verify>
  <done>lib/sold-addr.js exports normAddr, imports normStreet from spotcheck-evidence, all four MATCH-02 formats pass, --smoke exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped HTML/JSON → parser | Apollo state objects originate from external Booli/Hemnet pages and are untrusted input to the parsers in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | lib/sold-parse.js parsing untrusted Apollo state | mitigate | Parsers use only property access + Array.isArray guards + JSON.parse upstream (in scrape-http extractNextData); NO eval/Function/vm. Lifted code already guards missing nodes (returns []/null). Smoke test asserts null/empty-input paths do not throw. |
| T-15-02 | Denial of Service | lib/sold-parse.js on malformed/huge shapes | accept | Parsers iterate node.result / node.cards arrays only; no recursion over arbitrary depth. Page size is bounded by Booli/Hemnet's own 50-card pagination. Low risk, no mitigation beyond existing array guards. |
| T-15-03 | Information disclosure | lib/sold-addr.js / config | accept | Pure string + constant modules; handle no secrets, perform no I/O. No mitigation needed. |
</threat_model>

<verification>
- `node lib/sold-config.js --smoke` exits 0
- `node lib/sold-parse.js --smoke` exits 0
- `node lib/sold-addr.js --smoke` exits 0
- No parser uses eval/Function/vm: `grep -rn "eval(\|new Function\|require('vm')" lib/sold-parse.js lib/sold-addr.js lib/sold-config.js` returns nothing
</verification>

<success_criteria>
Three side-effect-free lib modules exist with the spike's snake_case contract intact, normAddr v2 recovers all four MATCH-02 formats, and all three `--smoke` self-tests exit 0. Downstream plans (02-05) can import these by name.
</success_criteria>

<output>
After completion, create `.planning/phases/15-sold-data-ingestion-library/15-01-SUMMARY.md`
</output>
