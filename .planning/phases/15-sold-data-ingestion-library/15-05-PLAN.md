---
phase: 15-sold-data-ingestion-library
plan: 05
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lib/sold-fetch-hemnet.js
  - scripts/hemnet-sold.js
autonomous: true
requirements: [SOLD-05, MATCH-02]
must_haves:
  truths:
    - "A lib function fetches Hemnet /salda SaleCard candidates for a given Booli property via per-property search (reusing the cohort search pattern)"
    - "The search builds a price/area/rooms/item-type filtered /salda URL, paginates, and early-stops when the address is found, the page is short, or the sold-date window is exceeded"
    - "No per-card detail fetch occurs — SaleCard fields come from the search-result Apollo only"
    - "House vs apartment search opts differ (houses: wider price/area band, drop rooms + item_type; apartments: tight under the 50-card cap)"
    - "Address matching uses normAddr v2 (MATCH-02) from lib/sold-addr"
    - "A thin CLI wrapper (scripts/hemnet-sold.js) runs the search with SCRAPE_FORCE_OXYLABS set and assertOxyUsed at the end"
  artifacts:
    - path: "lib/sold-fetch-hemnet.js"
      provides: "buildHemnetSoldUrl, searchSoldPaged (per-property paginated search w/ early-stop), within-run search cache"
      exports: ["buildHemnetSoldUrl", "searchSoldPaged"]
    - path: "scripts/hemnet-sold.js"
      provides: "Thin CLI wrapper over lib/sold-fetch-hemnet"
      contains: "SCRAPE_FORCE_OXYLABS"
  key_links:
    - from: "lib/sold-fetch-hemnet.js"
      to: "lib/sold-transport.js"
      via: "require('./sold-transport') for cachedFetch/extractApollo/ceiling"
      pattern: "require\\(['\"]\\./sold-transport['\"]\\)"
    - from: "lib/sold-fetch-hemnet.js"
      to: "lib/sold-parse.js"
      via: "require('./sold-parse') for parseHemnetSaleCards"
      pattern: "require\\(['\"]\\./sold-parse['\"]\\)"
    - from: "lib/sold-fetch-hemnet.js"
      to: "lib/sold-addr.js"
      via: "require('./sold-addr') for normAddr v2 (MATCH-02)"
      pattern: "require\\(['\"]\\./sold-addr['\"]\\)"
---

<objective>
Productionize the per-property Hemnet `/salda` `SaleCard` search as `lib/sold-fetch-hemnet.js` (filtered URL builder, paginated search with address/short-page/window early-stop, within-run search cache, no per-card detail fetch, house-vs-apartment search opts) plus a thin `scripts/hemnet-sold.js` CLI wrapper. This is the Hemnet ingestion half: SOLD-05, consuming `normAddr` v2 (MATCH-02) from Plan 01.

Purpose: This is the second fetch half the Phase 17 runner orchestrates (per Booli property → Hemnet candidates → adjudicate). It must reuse the cohort per-property search pattern and parse SaleCard fields without any detail fetch.
Output: `lib/sold-fetch-hemnet.js` (exports the URL builder + paginated search) and `scripts/hemnet-sold.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sold-data-ingestion-library/15-CONTEXT.md
@.planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md
@.planning/phases/15-sold-data-ingestion-library/15-01-SUMMARY.md
@.planning/phases/15-sold-data-ingestion-library/15-02-SUMMARY.md

<interfaces>
From lib/sold-transport.js (Plan 02):
```javascript
// cachedFetch(url, {logger}) -> { status, html, fromCache, url }   (MAX_OXY_CALLS ceiling, CeilingError)
// extractApollo(html) -> { nextData, apollo }
// remainingCalls(), CeilingError, assertOxyUsed, stdoutLogger, ROOT, appendJsonl, readJsonl
```
From lib/sold-parse.js (Plan 01):
```javascript
// parseHemnetSaleCards(apollo) -> [snake_case SaleCard records: slug, detail_url, street_address, sold_at, final_price, fee, living_area, rooms, ...]
```
From lib/sold-addr.js (Plan 01):
```javascript
// normAddr(s) -> normalized address string | null   (MATCH-02: handles the four recovered false-negative formats)
```
From lib/sold-config.js (Plan 01):
```javascript
// SEGMENTS, PRICE_BAND, SOLD_DATE_WINDOW_DAYS
```
From lib/booli-to-hemnet-mapping.js (existing):
```javascript
// booliObjectTypeToHemnet(objectType) -> hemnet item_type for HOUSE-family per-record item_type
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build lib/sold-fetch-hemnet.js (filtered URL builder + paginated per-property search + within-run cache)</name>
  <read_first>
    - scripts/spike-hemnet-match.js (lines 31 normStreet/deltas require, 78-103 buildHemnetSoldUrl, 107-124 searchSold within-run cache, 132-150 searchSoldPaged, 191-194 house/apt searchOpts)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (sold-fetch-hemnet section, lines 409-509; CeilingError draining lines 637-647)
    - lib/hemnet-fetch.js (lines 1-17 — imports + getWithRetry→extractNextData→parse convention to match)
    - lib/sold-transport.js, lib/sold-parse.js, lib/sold-addr.js, lib/sold-config.js (require targets)
    - lib/booli-to-hemnet-mapping.js (booliObjectTypeToHemnet for HOUSE item_type)
  </read_first>
  <action>
    Create lib/sold-fetch-hemnet.js by productionizing scripts/spike-hemnet-match.js's search logic. Import normAddr from './sold-addr' (do NOT redefine it here — MATCH-02 lives in lib/sold-addr per Plan 01). Implement and export:
    - `buildHemnetSoldUrl(booli, seg, opts = {})`: append `location_ids[]=${seg.hemnet.locationId}`; if booli.sold_price finite, append price_min/price_max banded by opts.priceBand (default PRICE_BAND); add a living_area band (opts.areaBand default 0.07) and a rooms filter UNLESS opts.dropRooms; set item_types[] = APARTMENT→seg.hemnet.itemType, HOUSE→booliObjectTypeToHemnet(booli.object_type) UNLESS opts.dropItemType. Return `https://www.hemnet.se/salda?<params>`.
    - A within-run `searchSold(url)` with searchCache + searchInFlight Maps (lift spike lines 107-124) so concurrent same-URL searches dedupe; it calls cachedFetch → extractApollo → parseHemnetSaleCards, caching [] on non-200.
    - `searchSoldPaged(booli, seg, windowDays, maxPages, opts = {})`: build base URL; bAddr = normAddr(booli.street_address); paginate page=1.. ; per page push cards; early-stop (complete=true) when cards.length===0, OR any card's normAddr(street_address)===bAddr (address found), OR cards.length<50 (last page within filters), OR the oldest sold_at < booliSoldUnix(booli.sold_date) - windowDays*DAY. Return `{ cards, pages, complete }`. NO per-card detail fetch.
    - Provide the house-vs-apartment search opts helper: HOUSE → {priceBand:0.10, areaBand:0.15, dropRooms:true, dropItemType:true}; APARTMENT → {} (tight). Expose it (e.g. searchOptsFor(seg)) or apply it inside searchSoldPaged keyed on seg.family.
    - CeilingError handling: if remainingCalls() <= 40 stop draining (return partial with a stop reason); catch CeilingError and return cleanly (per PATTERNS lines 637-647). No DB/persistence.
    - Append a `--smoke` block that unit-tests buildHemnetSoldUrl OFFLINE: an APARTMENT segment URL contains item_types[] + a rooms param; a HOUSE segment URL (dropRooms+dropItemType) omits rooms and item_types[] and uses the wider price band. No network in smoke.
  </action>
  <acceptance_criteria>
    - `node -e "const f=require('./lib/sold-fetch-hemnet'); process.exit(typeof f.buildHemnetSoldUrl==='function' && typeof f.searchSoldPaged==='function' ? 0:1)"` exits 0
    - `grep -q "require('./sold-transport')" lib/sold-fetch-hemnet.js && grep -q "require('./sold-parse')" lib/sold-fetch-hemnet.js && grep -q "require('./sold-addr')" lib/sold-fetch-hemnet.js` succeeds
    - `grep -q "salda" lib/sold-fetch-hemnet.js && grep -q "location_ids" lib/sold-fetch-hemnet.js` succeeds (SOLD-05 per-property /salda search)
    - `grep -cE "parseBooliSoldDetail|/bostad/" lib/sold-fetch-hemnet.js` returns 0 (no per-card detail fetch — SOLD-05 constraint)
    - `grep -q "function normAddr" lib/sold-fetch-hemnet.js` returns nothing (normAddr imported, not redefined — MATCH-02 single source)
    - `node lib/sold-fetch-hemnet.js --smoke` exits 0 (offline URL-builder checks pass)
  </acceptance_criteria>
  <verify>
    <automated>node lib/sold-fetch-hemnet.js --smoke</automated>
  </verify>
  <done>lib/sold-fetch-hemnet.js exports buildHemnetSoldUrl + searchSoldPaged, does per-property /salda search with address/short-page/window early-stop and no detail fetch, differentiates house vs apartment opts, imports normAddr from sold-addr, and --smoke passes offline.</done>
</task>

<task type="auto">
  <name>Task 2: Build the thin CLI wrapper scripts/hemnet-sold.js</name>
  <read_first>
    - scripts/spike-hemnet-match.js (lines 332-351 — main() + the require.main dual-use guard being thinned)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (CLI wrapper hemnet-sold.js section, lines 564-575; shared patterns 600-682)
    - lib/sold-fetch-hemnet.js (Task 1 output — the lib being wrapped)
    - lib/sold-config.js (SEGMENTS), lib/sold-transport.js (readJsonl, stdoutLogger, assertOxyUsed)
  </read_first>
  <action>
    Create scripts/hemnet-sold.js as a THIN CLI wrapper. First statement MUST be `process.env.SCRAPE_FORCE_OXYLABS = '1';`, then `require('dotenv').config();`, THEN require '../lib/sold-fetch-hemnet', '../lib/sold-config', '../lib/sold-transport'. Parse argv for: --segment <key>, --seed <jsonl path> (Booli seed file to search Hemnet for; default the Plan-04 seed path for the segment under ROOT), --window-days (default SOLD_DATE_WINDOW_DAYS), --max-pages. For each Booli record in the seed, call searchSoldPaged(booli, seg, windowDays, maxPages, searchOptsFor(seg)) and log a per-record result (booli_id → candidate count + complete flag); optionally append candidates to a results JSONL under ROOT. End main() with the transport-assert pattern (`assertOxyUsed()` → exitCode 2 on failure). Wrap entry in `if (require.main === module) { main().catch((e)=>{console.error('FATAL',e);process.exit(1);}); }`. The wrapper holds NO URL-building/search/parse logic — args + delegation only (D-06 cleanup: spike parseArgs scaffolding moves here, out of the lib).
  </action>
  <acceptance_criteria>
    - `node -c scripts/hemnet-sold.js` exits 0 (valid syntax)
    - `grep -n "process.env.SCRAPE_FORCE_OXYLABS = '1'" scripts/hemnet-sold.js` shows it on a line BEFORE the first `require('../lib/` line
    - `grep -q "require('../lib/sold-fetch-hemnet')" scripts/hemnet-sold.js` succeeds
    - `grep -q "assertOxyUsed" scripts/hemnet-sold.js && grep -q "require.main === module" scripts/hemnet-sold.js` succeeds
    - `grep -cE "buildHemnetSoldUrl =|parseHemnetSaleCards|hemnet.se/salda\?" scripts/hemnet-sold.js` returns 0 (no search/build logic in the wrapper — delegation only)
  </acceptance_criteria>
  <verify>
    <automated>node -c scripts/hemnet-sold.js</automated>
  </verify>
  <done>scripts/hemnet-sold.js is a thin wrapper: sets SCRAPE_FORCE_OXYLABS first, parses CLI args, reads a Booli seed, delegates per-record to lib/sold-fetch-hemnet searchSoldPaged, runs assertOxyUsed at the end, and contains no search/build logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hemnet /salda HTML → search parser | external untrusted Apollo JSON drives SaleCard construction + address comparison |
| Oxylabs search calls → cost control | each per-property search page consumes paid credits under MAX_OXY_CALLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | Denial of Service (cost) | unbounded per-property pagination across many seeds | mitigate | searchSoldPaged caps at maxPages and early-stops on address-found/short-page/window; every fetch through cachedFetch (MAX_OXY_CALLS ceiling); drains at remainingCalls()<=40 and returns cleanly on CeilingError. Within-run searchCache dedupes identical URLs. |
| T-15-16 | Tampering | malformed /salda apollo | mitigate | Candidates built only from parseHemnetSaleCards (Plan 01 guarded parser); non-200 caches []; normAddr handles null safely. No eval. |
| T-15-17 | Spoofing | direct-curl substituted for forced Oxylabs | mitigate | CLI wrapper sets SCRAPE_FORCE_OXYLABS before requires; assertOxyUsed at run end throws if any direct-curl success occurred. |
| T-15-18 | Information disclosure | results JSONL / logs | accept | Output holds only public /salda SaleCard fields; no credentials logged or persisted. |
</threat_model>

<verification>
- `node lib/sold-fetch-hemnet.js --smoke` exits 0
- `node -c scripts/hemnet-sold.js` exits 0
- No detail-page fetch in the Hemnet search path (grep shows no /bostad/ or parseBooliSoldDetail)
- normAddr is imported from lib/sold-addr, not redefined (single MATCH-02 source)
- Wrapper sets SCRAPE_FORCE_OXYLABS before requires and runs assertOxyUsed
</verification>

<success_criteria>
The Hemnet ingestion side is a reusable lib (filtered /salda URL builder + paginated per-property search with early-stop and no detail fetch, house-vs-apartment opts) plus a thin CLI wrapper, covering SOLD-05 and consuming normAddr v2 (MATCH-02) — all under the MAX_OXY_CALLS ceiling.
</success_criteria>

<output>
After completion, create `.planning/phases/15-sold-data-ingestion-library/15-05-SUMMARY.md`
</output>
