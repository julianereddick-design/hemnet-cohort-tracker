---
phase: 15-sold-data-ingestion-library
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - lib/sold-fetch-booli.js
  - scripts/booli-sold.js
autonomous: true
requirements: [SOLD-01, SOLD-02, SOLD-03, SOLD-04]
must_haves:
  truths:
    - "A lib function fetches Booli /slutpriser sold records for a configured segment + rolling sold-date window, paginating and early-stopping on sold date"
    - "Each fetched record is classified by soldPriceType with is_title_transfer set (Lagfart excluded from match set but retained in output)"
    - "Cheap card-level enriched attributes are captured for every record regardless of detail policy (D-02)"
    - "Detail-only enriched fields are fetched per the recon-gated D-01 policy (card-only default; apartment-in-fee-window detail; all-records detail ONLY if Plan 03 approved escalation)"
    - "sold_in_advance is set per the Plan 03 recon finding, or left null when no reliable signal exists (D-03 best-effort, never block)"
    - "A thin CLI wrapper (scripts/booli-sold.js) runs the fetch with SCRAPE_FORCE_OXYLABS set and assertOxyUsed at the end"
  artifacts:
    - path: "lib/sold-fetch-booli.js"
      provides: "fetchBooliSold (paginated, idempotent JSONL resume, early-stop) + fetchBooliSoldPage (single-page, returns {cards, meta})"
      exports: ["fetchBooliSold", "fetchBooliSoldPage"]
    - path: "scripts/booli-sold.js"
      provides: "Thin CLI wrapper over lib/sold-fetch-booli"
      contains: "SCRAPE_FORCE_OXYLABS"
  key_links:
    - from: "lib/sold-fetch-booli.js"
      to: "lib/sold-transport.js"
      via: "require('./sold-transport') for cachedFetch/extractApollo/ceiling/JSONL"
      pattern: "require\\(['\"]\\./sold-transport['\"]\\)"
    - from: "lib/sold-fetch-booli.js"
      to: "lib/sold-parse.js"
      via: "require('./sold-parse') for parseBooliSoldCards/booliSoldMeta/parseBooliSoldDetail"
      pattern: "require\\(['\"]\\./sold-parse['\"]\\)"
    - from: "scripts/booli-sold.js"
      to: "lib/sold-fetch-booli.js"
      via: "require('../lib/sold-fetch-booli')"
      pattern: "require\\(['\"]\\.\\./lib/sold-fetch-booli['\"]\\)"
---

<objective>
Productionize the Booli `/slutpriser` seed fetch as `lib/sold-fetch-booli.js` (paginated, sold-date early-stop, idempotent JSONL resume, `soldPriceType` classification, recon-gated detail enrichment, `sold_in_advance` flag) and a thin `scripts/booli-sold.js` CLI wrapper. This covers the entire Booli ingestion side: SOLD-01, SOLD-02, SOLD-03, SOLD-04.

Purpose: This is one of the two fetch halves the Phase 17 runner orchestrates. The detail-fetch policy MUST honor the Plan 03 recon finding (D-01) — card-only default, apartment-in-fee-window detail, all-records detail ONLY if the operator approved escalation in Plan 03's checkpoint.
Output: `lib/sold-fetch-booli.js` (exports both a paginated JSONL fetch and a single-page primitive Phase 16 will call without JSONL) and `scripts/booli-sold.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sold-data-ingestion-library/15-CONTEXT.md
@.planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md
@.planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md
@.planning/phases/15-sold-data-ingestion-library/15-01-SUMMARY.md
@.planning/phases/15-sold-data-ingestion-library/15-02-SUMMARY.md
@.planning/phases/15-sold-data-ingestion-library/15-03-SUMMARY.md

<interfaces>
From lib/sold-transport.js (Plan 02):
```javascript
// cachedFetch(url, {logger}) -> { status, html, fromCache, url }   (enforces MAX_OXY_CALLS ceiling, throws CeilingError)
// extractApollo(html) -> { nextData, apollo }
// appendJsonl(file,obj), readJsonl(file), writeJson, ensureDir, ROOT, stdoutLogger, assertOxyUsed, CeilingError, procStats
```
From lib/sold-parse.js (Plan 01):
```javascript
// parseBooliSoldCards(apollo) -> [snake_case records incl. is_title_transfer]
// booliSoldMeta(apollo) -> meta
// parseBooliSoldDetail(apollo) -> {rent, operating_cost, construction_year, agent_id, agency_id, tenure_form, ...} | null
```
From lib/sold-config.js (Plan 01):
```javascript
// SEGMENTS, DEFAULT_TARGET_PER_SEGMENT, READ_TIME_EXCLUDE_DAYS, daysAgoISO, isTitleTransfer
```
DETAIL-FETCH POLICY: read 15-SOLD-IN-ADVANCE-RECON.md (Plan 03) for the approved D-01 disposition before implementing detail fetch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build lib/sold-fetch-booli.js (paginated seed fetch + single-page primitive + recon-gated detail)</name>
  <read_first>
    - scripts/spike-booli-sold.js (lines 45-137 — scrapeSegment paginated fetch + main; the source to productionize)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (sold-fetch-booli section, lines 340-406)
    - .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md (the approved D-01 detail policy + which field sets sold_in_advance)
    - lib/booli-fetch.js (lines 250-282 fetchBooliSearch — the opts-object + getWithRetry→extractNextData→parse convention to match)
    - lib/sold-transport.js, lib/sold-parse.js, lib/sold-config.js (Plan 01/02 outputs — the require targets)
  </read_first>
  <action>
    Create lib/sold-fetch-booli.js. Lift scripts/spike-booli-sold.js scrapeSegment into an exported `async fetchBooliSold(segKey, seg, opts = {})` taking an OPTS OBJECT (not positional args) with: target, marketTarget, maxPages, maxSoldDate, minSoldDate, logger. Behavior:
    - Idempotent resume: read existing seed JSONL (path under ROOT, e.g. `${ROOT}/seeds/${segKey}.jsonl`), build a `seen` Set of booli_id, resume counts from it.
    - Pagination + early-stop: build `https://www.booli.se/slutpriser?areaIds=${seg.booli.areaIds}&objectType=${encodeURIComponent(seg.booli.objectType)}&maxSoldDate=${maxSoldDate}` (+ `&minSoldDate=` when supplied) `&page=${page}`; loop while !reached() && page<=maxPages; stop on empty-page, status!=200, or target reached. Default maxSoldDate = daysAgoISO(READ_TIME_EXCLUDE_DAYS).
    - Classification (SOLD-02): each card already carries is_title_transfer from parseBooliSoldCards; track marketCollected = records where !is_title_transfer; honor marketTarget against that count. RETAIN title-transfer records in the JSONL output (excluded-from-match, not dropped).
    - Cheap enrichment (D-02/SOLD-03): write the full snake_case card record (object_type, sold_price, sold_date, sold_price_type, municipality, descriptive_area, living_area, rooms, floor, lat, long) for EVERY record — these are free.
    - Detail enrichment (D-01/SOLD-03): per the approved 15-SOLD-IN-ADVANCE-RECON.md disposition. Default: fetch /bostad/<residence_id> detail (via cachedFetch + extractApollo + parseBooliSoldDetail) ONLY for apartment-family records within the fee window; merge rent/operating_cost/construction_year/agent_id/agency_id/tenure_form. Villas stay card-only (detail-only fields null) UNLESS Plan 03's checkpoint approved all-records detail escalation. Make the detail scope a guarded option `opts.detailScope` ('fee-window' default | 'all' | 'none') so escalation is explicit, never silent.
    - sold_in_advance (SOLD-04/D-03): set the flag from the field identified in 15-SOLD-IN-ADVANCE-RECON.md (card or detail per the finding); if recon found no reliable signal, set sold_in_advance: null. NEVER block on this flag.
    - CeilingError: catch and break the page loop cleanly (stop='ceiling'); propagate other errors as logged warnings, continue.
    - ALSO export a lower-level `async fetchBooliSoldPage(segKey, seg, {page, maxSoldDate, minSoldDate, logger})` returning `{ cards, meta }` (no JSONL write) so Phase 16 can call it to persist to the DB directly.
    - Require lib/sold-transport, lib/sold-parse, lib/sold-config (relative './' paths). Append a `--smoke` block that exercises pure logic offline (e.g. reached()/url-building/seen-dedup with a stub) WITHOUT a network call.
  </action>
  <acceptance_criteria>
    - `node -e "const f=require('./lib/sold-fetch-booli'); process.exit(typeof f.fetchBooliSold==='function' && typeof f.fetchBooliSoldPage==='function' ? 0:1)"` exits 0
    - `grep -q "require('./sold-transport')" lib/sold-fetch-booli.js && grep -q "require('./sold-parse')" lib/sold-fetch-booli.js && grep -q "require('./sold-config')" lib/sold-fetch-booli.js` succeeds
    - `grep -q "slutpriser" lib/sold-fetch-booli.js && grep -q "maxSoldDate" lib/sold-fetch-booli.js` succeeds (SOLD-01 paginated window fetch)
    - `grep -q "is_title_transfer" lib/sold-fetch-booli.js` succeeds (SOLD-02 retained-but-flagged)
    - `grep -q "sold_in_advance" lib/sold-fetch-booli.js` succeeds (SOLD-04 flag set)
    - `grep -q "detailScope" lib/sold-fetch-booli.js` succeeds (D-01 explicit, never-silent escalation)
    - `node lib/sold-fetch-booli.js --smoke` exits 0 (offline pure-logic checks pass)
  </acceptance_criteria>
  <verify>
    <automated>node lib/sold-fetch-booli.js --smoke</automated>
  </verify>
  <done>lib/sold-fetch-booli.js exports fetchBooliSold + fetchBooliSoldPage, paginates /slutpriser with sold-date early-stop, classifies+retains title transfers, captures cheap enrichment for all records, applies the recon-gated detail policy via an explicit detailScope option, sets sold_in_advance per the recon finding, and --smoke passes offline.</done>
</task>

<task type="auto">
  <name>Task 2: Build the thin CLI wrapper scripts/booli-sold.js</name>
  <read_first>
    - scripts/spike-booli-sold.js (lines 1-44, 119-137 — the parseArgs + main entry point being thinned)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (CLI wrapper booli-sold.js section, lines 536-561; shared patterns lines 600-682)
    - lib/sold-fetch-booli.js (Task 1 output — the lib being wrapped)
    - lib/sold-config.js (SEGMENTS, daysAgoISO, READ_TIME_EXCLUDE_DAYS)
    - .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md (Plan 03 — the operator approval marker the --detail-scope all guard checks for)
  </read_first>
  <action>
    Create scripts/booli-sold.js as a THIN CLI wrapper. First statement MUST be `process.env.SCRAPE_FORCE_OXYLABS = '1';`, then `require('dotenv').config();`, THEN require '../lib/sold-fetch-booli', '../lib/sold-config', '../lib/sold-transport' (order matters — transport's load-time guard needs the flag set first). Parse argv for: --segment <key> (default: all SEGMENTS keys), --target, --market-target, --max-pages, --min-sold-date, --max-sold-date (default daysAgoISO(READ_TIME_EXCLUDE_DAYS)), --detail-scope (fee-window|all|none). For each selected segment call fetchBooliSold(k, seg, {...opts, logger: stdoutLogger('booli-sold')}). End main() with the transport-assert pattern: `try { log('INFO', \`transport-assert: ${JSON.stringify(assertOxyUsed())}\`); } catch (e) { log('ERROR', \`transport-assert FAILED: ${e.message}\`); process.exitCode = 2; }`. Wrap entry in `if (require.main === module) { main().catch((e)=>{console.error('FATAL',e);process.exit(1);}); }`. Keep ALL arg-parsing here (do NOT leave parseArgs in the lib — per D-06 cleanup, that scaffolding moves to the wrapper). The wrapper holds NO fetch/parse logic — it only parses args and delegates. D-01 SPEND GUARD (enforce, do not merely document): if --detail-scope is "all", the wrapper MUST read .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md and refuse to run (exit 3 with a clear error) UNLESS that file contains the operator approval marker string "escalate detail (spend confirmed)". This makes the all-records ~2x-spend escalation impossible to trigger silently in a delegated/overnight run when Plan 03 did not approve it (per D-01: escalation stays the operator's call). fee-window (default) and none scopes need no such check.
  </action>
  <acceptance_criteria>
    - `node -c scripts/booli-sold.js` exits 0 (valid syntax)
    - `grep -n "process.env.SCRAPE_FORCE_OXYLABS = '1'" scripts/booli-sold.js` shows it on a line BEFORE the first `require('../lib/` line
    - `grep -q "require('../lib/sold-fetch-booli')" scripts/booli-sold.js` succeeds
    - `grep -q "assertOxyUsed" scripts/booli-sold.js && grep -q "require.main === module" scripts/booli-sold.js` succeeds
    - `grep -cE "cachedFetch|parseBooliSoldCards|booli.se/slutpriser" scripts/booli-sold.js` returns 0 (no fetch/parse logic in the wrapper — delegation only)
    - `grep -q "escalate detail (spend confirmed)" scripts/booli-sold.js && grep -q "SOLD-IN-ADVANCE-RECON" scripts/booli-sold.js` succeeds (D-01 spend guard: --detail-scope all is gated on the Plan 03 approval marker)
  </acceptance_criteria>
  <verify>
    <automated>node -c scripts/booli-sold.js</automated>
  </verify>
  <done>scripts/booli-sold.js is a thin wrapper: sets SCRAPE_FORCE_OXYLABS first, parses CLI args, delegates to lib/sold-fetch-booli, runs assertOxyUsed at the end, contains no fetch/parse logic, and refuses --detail-scope all unless 15-SOLD-IN-ADVANCE-RECON.md carries the operator approval marker (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Booli /slutpriser + /bostad HTML → fetch/parse | external untrusted Apollo JSON drives record construction |
| seed JSONL → disk | parsed records persisted to verf-soldspike/seeds |
| Oxylabs calls → cost control | each page + detail fetch consumes paid credits under MAX_OXY_CALLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11 | Denial of Service (cost) | unbounded pagination / all-records detail fetch | mitigate | Every fetch goes through cachedFetch (MAX_OXY_CALLS ceiling, _spend.json, CeilingError breaks loop). Detail scope defaults to fee-window apartments only; 'all' escalation is an explicit opts.detailScope value gated by Plan 03's operator approval — never the default (D-01: no silent 2× call count). |
| T-15-12 | Tampering | malformed Booli card/detail apollo | mitigate | Records built only from parseBooliSoldCards/parseBooliSoldDetail (Plan 01 guarded parsers); empty/missing nodes yield []/null and the loop stops on empty-page. No eval. |
| T-15-13 | Spoofing | direct-curl substituted for forced Oxylabs | mitigate | CLI wrapper sets SCRAPE_FORCE_OXYLABS before requires; assertOxyUsed at run end throws if any direct-curl success occurred. |
| T-15-14 | Information disclosure | secrets in seed output / logs | accept | Seed JSONL contains only public sold-listing fields (snake_case contract); no credentials written. Logs print url/status/counts only. |
</threat_model>

<verification>
- `node lib/sold-fetch-booli.js --smoke` exits 0
- `node -c scripts/booli-sold.js` exits 0
- Wrapper sets SCRAPE_FORCE_OXYLABS before any lib require; runs assertOxyUsed
- Detail escalation is gated behind explicit detailScope='all', never the default
- Title-transfer records are retained (not dropped) in output; sold_in_advance set per recon or null
</verification>

<success_criteria>
The Booli ingestion side is a reusable lib (paginated window fetch + single-page primitive) plus a thin CLI wrapper, covering SOLD-01..04: enriched snake_case records with soldPriceType classification (Lagfart retained-but-flagged), recon-gated detail enrichment honoring D-01 (no silent spend escalation), and a best-effort sold_in_advance flag — all under the MAX_OXY_CALLS ceiling.
</success_criteria>

<output>
After completion, create `.planning/phases/15-sold-data-ingestion-library/15-04-SUMMARY.md`
</output>
