---
phase: 15-sold-data-ingestion-library
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - scripts/sold-recon.js
  - .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md
autonomous: false
requirements: [SOLD-04]
must_haves:
  truths:
    - "An extended recon harness scans both Booli /slutpriser card-level and /bostad/<id> detail-level Apollo state for 'sold in advance' (förhandsförsäljning / sold-before-viewing) signals"
    - "The recon outputs WHERE the signal lives: card field vs detail field vs badge/typename vs soldPriceType-adjacent vs nowhere"
    - "A documented finding records the location and feeds D-01's detail-fetch policy decision"
  artifacts:
    - path: "scripts/sold-recon.js"
      provides: "Extended Stage-0 recon CLI (typename histogram, keywordScan w/ sold-in-advance terms, root-query field dump) running over the new lib/sold-transport"
      contains: "förhand"
    - path: ".planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md"
      provides: "Documented recon finding + D-01 detail-fetch policy disposition"
      contains: "sold_in_advance"
  key_links:
    - from: "scripts/sold-recon.js"
      to: "lib/sold-transport.js"
      via: "require('../lib/sold-transport') for cachedFetch/extractApollo"
      pattern: "require\\(['\"]\\.\\./lib/sold-transport['\"]\\)"
---

<objective>
Productionize the spike recon harness as `scripts/sold-recon.js` over the new `lib/sold-transport`, and EXTEND its keyword scan to locate where Booli encodes "sold in advance" (förhandsförsäljning / sold-before-viewing). This is the D-04 recon — its finding GATES the D-01 detail-fetch policy that Plan 04 (Booli fetch) implements. The phase never blocks on this flag (D-03: best-effort), but the location MUST be confirmed and documented before the Booli fetch detail policy is finalized.

Purpose: SOLD-04 requires detecting + persisting a `sold_in_advance` flag, but only after recon confirms where the signal lives. Julian's hunch is the full detail page; the spike's keyword scan never looked for it.
Output: `scripts/sold-recon.js` (extended) + `15-SOLD-IN-ADVANCE-RECON.md` documenting the finding and the resulting D-01 policy, plus a checkpoint for Julian to confirm any spend escalation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sold-data-ingestion-library/15-CONTEXT.md
@.planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md
@.planning/phases/15-sold-data-ingestion-library/15-02-SUMMARY.md

<interfaces>
From lib/sold-transport.js (Plan 02):
```javascript
// cachedFetch(url, {logger}) -> { status, html, fromCache, url }   (enforces MAX_OXY_CALLS ceiling)
// extractApollo(html) -> { nextData, apollo }
// writeJson, readJson, ensureDir, stdoutLogger, assertOxyUsed, CeilingError, ROOT
```
Recon may reuse the existing spike dumps in verf-soldspike/recon/*.apollo.json (offline) to avoid spend,
and only issue live fetches for a /bostad/<id> detail page if no detail dump already exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift + extend scripts/spike-sold-recon.js into scripts/sold-recon.js with sold-in-advance keywords</name>
  <read_first>
    - scripts/spike-sold-recon.js (full file, 168 lines — the source to lift; keywordScan call ~lines 127-133, typenameHistogram/rootQueryFields/keywordScan helpers)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (sold-recon section, lines 579-595 — the keyword list to add)
    - .planning/phases/15-sold-data-ingestion-library/15-CONTEXT.md (D-04 — recon-first; D-03 best-effort never-block)
    - lib/sold-transport.js (Plan 02 output — the new transport require target)
    - lib/sold-parse.js, lib/sold-config.js (Plan 01 outputs — sold-recon rewires its requires to these; declared in depends_on: [01, 02])
  </read_first>
  <action>
    Create scripts/sold-recon.js by lifting scripts/spike-sold-recon.js. Adjustments:
    (1) Put `process.env.SCRAPE_FORCE_OXYLABS = '1';` as the FIRST statement, then `require('dotenv').config();`, BEFORE any lib require (the sold-transport load-time guard requires this).
    (2) Change spike-common requires to `require('../lib/sold-transport')`; change spike-config requires to `require('../lib/sold-config')`; change spike-sold-parse requires to `require('../lib/sold-parse')`.
    (3) EXTEND the keywordScan call's keyword array to ADD (keep the existing transfer/broker terms): 'förhand','forhand','advance','pre-market','premarket','before viewing','innan visning','visning','kommande','upcoming','presale','pre-sale','förköp','forköp'. Label this added block with a comment `// D-04 sold-in-advance recon`.
    (4) Make the recon able to scan a Booli /bostad/<id> DETAIL page (not only /slutpriser list pages) — accept a `--detail <booliId>` or `--url <url>` arg so the operator can point it at a detail page; reuse the existing typenameHistogram + keywordScan + rootQueryFields + dump-to-json logic on that page's apollo. Prefer offline: if a matching dump already exists under verf-soldspike/recon/, scan it without a live fetch.
    (5) End with the `assertOxyUsed()` transport-assert pattern.
    Do NOT add DB or persistence logic — this is recon only.
  </action>
  <acceptance_criteria>
    - `grep -q "process.env.SCRAPE_FORCE_OXYLABS = '1'" scripts/sold-recon.js` AND the line appears before any `require('../lib/` line (force-Oxylabs guard precedence)
    - `grep -q "require('../lib/sold-transport')" scripts/sold-recon.js` succeeds
    - `grep -q "förhand" scripts/sold-recon.js && grep -q "innan visning" scripts/sold-recon.js` succeeds (sold-in-advance keywords added)
    - `grep -q "D-04" scripts/sold-recon.js` succeeds (added block is labelled)
    - `node -c scripts/sold-recon.js` exits 0 (syntactically valid)
  </acceptance_criteria>
  <verify>
    <automated>node -c scripts/sold-recon.js</automated>
  </verify>
  <done>scripts/sold-recon.js runs over lib/sold-transport, scans card- AND detail-level apollo, and its keywordScan includes the sold-in-advance term set labelled D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Run the recon and document the sold-in-advance finding + D-01 disposition</name>
  <read_first>
    - scripts/sold-recon.js (Task 1 output)
    - .planning/phases/15-sold-data-ingestion-library/15-CONTEXT.md (D-01 detail-fetch policy options; D-03/D-04)
    - verf-soldspike/recon/ (existing recon dumps — check for an existing detail-page dump before spending)
  </read_first>
  <action>
    Execute the recon against Booli sold data. FIRST scan existing offline dumps (verf-soldspike/recon/*.apollo.json) for the sold-in-advance keyword set — zero spend. If card-level dumps do NOT contain the signal, issue exactly ONE live /bostad/<id> detail fetch for a known recent Booli sold record (use a booli_id present in verf-soldspike seeds) via `node scripts/sold-recon.js --detail <booliId>`, staying under the MAX_OXY_CALLS ceiling. Then write .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md documenting: (a) WHERE the signal lives — one of {card field / detail field / badge or typename / soldPriceType-adjacent / nowhere found}; (b) the exact Apollo path or field name if found, with a sample value; (c) the resulting D-01 disposition. The disposition MUST follow D-01: if the signal is card-level/free → card-only default stands, Booli detail fetched ONLY for apartments in the fee window; if the signal proves detail-ONLY → flag the all-records-detail escalation as requiring Julian's spend re-confirmation (do NOT silently 2× call count). IMPORTANT: do NOT write the approval-marker string yet — the all-records escalation is approved ONLY at the checkpoint below. The exact marker that unlocks Plan 04's --detail-scope all guard is the literal line "escalate detail (spend confirmed)"; it is added to this doc ONLY after the operator approves it at the checkpoint, never by the agent pre-emptively; if not found anywhere → record that sold_in_advance stays null (D-03 best-effort, never block) and the phase proceeds. Include a one-line instruction for Plan 04 stating exactly which field Plan 04's parser/fetch should read (or "leave null").
  </action>
  <acceptance_criteria>
    - File .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md exists
    - `grep -qi "card field\|detail field\|typename\|soldPriceType\|nowhere\|not found" 15-SOLD-IN-ADVANCE-RECON.md` (recon location stated) — run from the phase dir
    - `grep -q "D-01" .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md` (disposition references the gated decision)
    - `grep -qi "Plan 04\|sold_in_advance" .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md` (handoff instruction present)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md && grep -qi "D-01" .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md</automated>
  </verify>
  <done>Recon executed (offline-first, ≤1 live detail fetch), finding documented with the exact field location and the D-01 disposition + Plan 04 handoff instruction.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>scripts/sold-recon.js extended for the sold-in-advance signal, plus 15-SOLD-IN-ADVANCE-RECON.md documenting where the signal lives and the resulting D-01 detail-fetch policy.</what-built>
  <how-to-verify>
    1. Read .planning/phases/15-sold-data-ingestion-library/15-SOLD-IN-ADVANCE-RECON.md — confirm the stated signal location matches reality (Julian's hunch is the full detail page).
    2. Confirm the D-01 disposition: card-only default stands UNLESS the signal is detail-only AND you approve the spend escalation. Per D-01, escalating to all-records detail fetch (which ~2× the per-segment Oxylabs call count) is YOUR call, not the agent's.
    3. If the recon recommends an all-records-detail escalation, decide: approve the elevated recurring Oxylabs spend, or keep card-only default with sold_in_advance left null for villas.
    4. ONLY if you approve all-records escalation: the executor will append the literal line "escalate detail (spend confirmed)" to 15-SOLD-IN-ADVANCE-RECON.md. Plan 04's booli-sold.js refuses --detail-scope all unless that exact marker is present, so escalation cannot be triggered silently in a delegated run. If you keep the default, the marker is NOT written and --detail-scope all stays blocked.
  </how-to-verify>
  <resume-signal>Type "approved — card-only default" OR "approved — escalate detail (spend confirmed)" OR describe the policy you want Plan 04 to implement.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped Booli detail/list HTML → recon parser | external untrusted Apollo JSON scanned for keywords |
| recon spend → cost control | live /bostad fetch consumes paid Oxylabs credits under the MAX_OXY_CALLS ceiling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Denial of Service (cost) | live /bostad detail fetch during recon | mitigate | Recon is offline-first (scans existing verf-soldspike dumps); at most ONE live detail fetch, under the lib/sold-transport MAX_OXY_CALLS ceiling and counted in _spend.json. The checkpoint gates any all-records escalation on operator spend approval (D-01). |
| T-15-09 | Tampering | keyword scan over untrusted apollo | mitigate | keywordScan walks the apollo object with property access only (no eval); lifted spike helper already string-matches values. No code execution from scraped content. |
| T-15-10 | Information disclosure | recon dumps written to disk | accept | Recon writes apollo/nextdata JSON to verf-soldspike/recon (already gitignored spike cache); contains only public sold-listing data, no secrets. |
</threat_model>

<verification>
- `node -c scripts/sold-recon.js` exits 0
- scripts/sold-recon.js sets SCRAPE_FORCE_OXYLABS before any lib require
- 15-SOLD-IN-ADVANCE-RECON.md exists with a stated signal location + D-01 disposition + Plan 04 handoff
- Checkpoint approved by operator before Plan 04 proceeds
</verification>

<success_criteria>
The recon confirms (and documents) exactly where Booli encodes "sold in advance" and the D-01 detail-fetch policy is resolved with operator sign-off, so Plan 04 implements the agreed policy — card-only default unless escalation is explicitly spend-approved, sold_in_advance left null where no reliable signal exists (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/15-sold-data-ingestion-library/15-03-SUMMARY.md`
</output>
