---
phase: 15-sold-data-ingestion-library
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sold-transport.js
  - lib/scrape-http.js
autonomous: true
requirements: [CONFIG-03]
must_haves:
  truths:
    - "A lib transport wraps the shared lib/scrape-http.js with a file-based MAX_OXY_CALLS ceiling that throws CeilingError before exceeding the cap"
    - "Forced-Oxylabs is enforced as a load-time invariant (require throws if SCRAPE_FORCE_OXYLABS is not set)"
    - "Live calls are counted in _spend.json BEFORE the fetch is issued (a forced attempt consumes credits whether it succeeds or not)"
    - "Transient Oxylabs 613-class errors (OXYLABS_API_NON_200 / OXYLABS_TARGET_NON_200) sleep before the existing single retry in the main fetch path, not only in probes"
    - "assertOxyUsed throws if any live fetch resolved via direct curl instead of Oxylabs"
  artifacts:
    - path: "lib/sold-transport.js"
      provides: "cachedFetch, extractApollo, assertOxyUsed, CeilingError, MAX_OXY_CALLS ceiling, JSONL helpers"
      exports: ["cachedFetch", "extractApollo", "assertOxyUsed", "CeilingError"]
    - path: "lib/scrape-http.js"
      provides: "613/transient sleep-before-retry in fallbackViaOxylabs (CONFIG-03)"
      contains: "fallbackViaOxylabs"
  key_links:
    - from: "lib/sold-transport.js"
      to: "lib/scrape-http.js"
      via: "require('./scrape-http') for getWithRetry/extractNextData/getOxylabsStats"
      pattern: "require\\(['\"]\\./scrape-http['\"]\\)"
    - from: "lib/sold-transport.js cachedFetch"
      to: "_spend.json"
      via: "loadSpend/saveSpend file-based ceiling tally"
      pattern: "_spend\\.json"
---

<objective>
Lift the spike's transport (`spike-common.js`) into `lib/sold-transport.js` — the file-based `MAX_OXY_CALLS` ceiling, `_spend.json` tally, `cachedFetch`, `extractApollo`, `assertOxyUsed`, `CeilingError`, and JSONL helpers — reusing (not duplicating) `lib/scrape-http.js` for HTTP/Oxylabs transport. Then extend `lib/scrape-http.js` so transient Oxylabs 613-class failures sleep before the existing single retry on the MAIN fetch path (CONFIG-03), so for-sale jobs benefit too.

Purpose: This is the cost-control + transport spine every fetch module (Plans 03/04/05) sits on. CONFIG-03 requires the spend ceiling AND a real (sleeping) 613 retry in production code, not just in probes.
Output: `lib/sold-transport.js` (new) and a minimal, API-preserving extension to `lib/scrape-http.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sold-data-ingestion-library/15-CONTEXT.md
@.planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md

<interfaces>
<!-- Existing lib/scrape-http.js public API — MUST stay unchanged. -->
From lib/scrape-http.js:
```javascript
module.exports = { getWithRetry, extractNextData, getOxylabsStats, resetOxylabsStats };
// getWithRetry(targetUrl, opts={logger}) -> { status, html }
// FORCE_OXYLABS short-circuits direct curl; goes straight to fallbackViaOxylabs.
// fallbackViaOxylabs already does ONE retry but with NO sleep between attempts.
// Oxylabs error codes: OXYLABS_API_NON_200 (Oxylabs API itself non-200, e.g. 613),
//   OXYLABS_TARGET_NON_200 (target page non-2xx), OXYLABS_NO_CREDS, OXYLABS_NET.
// A 613 surfaces as OXYLABS_API_NON_200 (res.statusCode 613 on the Oxylabs API),
//   currently retried once immediately with no backoff.
// sleep(ms) and RETRY_DELAYS_MS=[1000,2000,4000] already exist in the module.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift spike-common.js into lib/sold-transport.js (file-based ceiling + cachedFetch + guards)</name>
  <read_first>
    - scripts/spike-common.js (the verbatim source to lift, lines 1-192)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (sold-transport section, lines 216-336)
    - lib/scrape-http.js (lines 404-409 — the public exports this module consumes)
  </read_first>
  <action>
    Create lib/sold-transport.js by lifting scripts/spike-common.js. Required adjustments:
    (1) The shared-transport require changes from `require('../lib/scrape-http')` to `require('./scrape-http')` (same dir now).
    (2) Keep the load-time env-guard VERBATIM: if neither SCRAPE_FORCE_OXYLABS==='1' nor HEMNET_FORCE_OXYLABS==='1', throw "sold-transport: SCRAPE_FORCE_OXYLABS must be set to \"1\" BEFORE requiring this module". This is an invariant — sold pages are 100% Oxylabs.
    (3) ROOT path stays env-overridable: `path.join(__dirname, '..', process.env.SPIKE_DIR || 'verf-soldspike')`; CACHE_DIR = ROOT/cache; SPEND_FILE = CACHE_DIR/_spend.json; MAX_OXY_CALLS = parseInt(process.env.MAX_OXY_CALLS || '4000', 10).
    (4) Preserve cachedFetch's ceiling logic EXACTLY: before issuing, if spend.liveCalls >= MAX_OXY_CALLS throw new CeilingError; then increment spend.liveCalls and saveSpend BEFORE calling getWithRetry (a forced attempt consumes credits whether or not it succeeds).
    Export the full surface: ROOT, CACHE_DIR, MAX_OXY_CALLS, CeilingError, cachedFetch, extractApollo, extractNextData, assertOxyUsed, procStats, spentCalls, remainingCalls, getOxylabsStats, ensureDir, tsSlug, writeJson, readJson, appendJsonl, readJsonl, stdoutLogger.
  </action>
  <acceptance_criteria>
    - `node -e "try{require('./lib/sold-transport')}catch(e){process.exit(/SCRAPE_FORCE_OXYLABS/.test(e.message)?0:1)}"` exits 0 (guard fires when flag unset)
    - `SCRAPE_FORCE_OXYLABS=1 node -e "const t=require('./lib/sold-transport'); process.exit(typeof t.cachedFetch==='function' && typeof t.assertOxyUsed==='function' && t.CeilingError && typeof t.MAX_OXY_CALLS==='number' ? 0:1)"` exits 0
    - `grep -q "require('./scrape-http')" lib/sold-transport.js` succeeds (reuses shared transport, not duplicated)
    - `grep -q "_spend.json" lib/sold-transport.js` succeeds (file-based ceiling tally retained, D-07)
    - `grep -q "class CeilingError" lib/sold-transport.js` succeeds
    - `grep -c "fetchViaOxylabs\|curlOnce" lib/sold-transport.js` returns 0 (transport NOT re-implemented here)
  </acceptance_criteria>
  <verify>
    <automated>node -e "try{require('./lib/sold-transport')}catch(e){process.exit(/SCRAPE_FORCE_OXYLABS/.test(e.message)?0:1)}" && SCRAPE_FORCE_OXYLABS=1 node -e "const t=require('./lib/sold-transport'); process.exit(t.cachedFetch&&t.CeilingError&&t.assertOxyUsed?0:1)"</automated>
  </verify>
  <done>lib/sold-transport.js exists, requires ./scrape-http (no duplicated HTTP code), enforces the load-time force-Oxylabs guard, keeps the file-based MAX_OXY_CALLS/_spend.json ceiling, and exports the full surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add 613-class sleep-before-retry to lib/scrape-http.js fallbackViaOxylabs (CONFIG-03)</name>
  <read_first>
    - lib/scrape-http.js (lines 85-95 sleep+RETRY_DELAYS_MS, 108-195 fetchViaOxylabs + error codes, 197-233 fallbackViaOxylabs, 404-409 exports)
    - .planning/phases/15-sold-data-ingestion-library/15-PATTERNS.md (scrape-http 613 section, lines 513-532)
    - .planning/phases/15-sold-data-ingestion-library/15-CONTEXT.md (D-07 — confirm/extend 613 on main path)
  </read_first>
  <action>
    Extend `fallbackViaOxylabs` in lib/scrape-http.js so the single existing retry sleeps when the first attempt failed with a transient/613-class code. A 613 surfaces as `OXYLABS_API_NON_200` (the Oxylabs API itself returns HTTP 613 — credit-limit / temporary block — see fetchViaOxylabs line 144-149); `OXYLABS_TARGET_NON_200` is the other transient class. In the `catch (e1)` block, BEFORE the second `fetchViaOxylabs` attempt, add: `const transient = reason1 === 'OXYLABS_API_NON_200' || reason1 === 'OXYLABS_TARGET_NON_200'; if (transient) await sleep(3000);`. Use the existing module-level `sleep` helper (do NOT add a new one). Log a line indicating a backoff was applied (e.g. `log('INFO', \`oxylabs-fallback-backoff url=${targetUrl} reason=${reason1} sleep=3000ms\`)`). Do NOT change the module's public API (getWithRetry, extractNextData, getOxylabsStats, resetOxylabsStats) and do NOT change attempt count (still one retry) — only insert the conditional sleep. This applies to the MAIN fetch path (every getWithRetry caller, incl. for-sale jobs), satisfying CONFIG-03's "not just probes" clause.
  </action>
  <acceptance_criteria>
    - `grep -q "OXYLABS_API_NON_200" lib/scrape-http.js && grep -A30 "async function fallbackViaOxylabs" lib/scrape-http.js | grep -q "await sleep"` succeeds (sleep added inside fallback retry)
    - `grep -c "module.exports = {" lib/scrape-http.js` returns 1 AND `grep -A6 "module.exports = {" lib/scrape-http.js | grep -q "getWithRetry"` succeeds (public API unchanged)
    - `node -e "const h=require('./lib/scrape-http'); process.exit(typeof h.getWithRetry==='function' && typeof h.extractNextData==='function' && typeof h.getOxylabsStats==='function' ? 0:1)"` exits 0 (module still loads, exports intact)
    - `grep -c "function sleep" lib/scrape-http.js` returns 1 (no duplicate sleep helper added)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const h=require('./lib/scrape-http'); process.exit(h.getWithRetry&&h.extractNextData&&h.getOxylabsStats&&h.resetOxylabsStats?0:1)"</automated>
  </verify>
  <done>fallbackViaOxylabs sleeps 3s before its retry on OXYLABS_API_NON_200 / OXYLABS_TARGET_NON_200; public API and retry count unchanged; module loads cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local process → Oxylabs API | OXYLABS_USERNAME/OXYLABS_PASSWORD credentials cross to the Oxylabs Web Scraper API |
| Oxylabs response → cache/parser | external scraped content written to verf-soldspike/cache and parsed downstream |
| spend tally → cost control | _spend.json gates outbound paid calls (cost-DoS guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information disclosure | Oxylabs credentials in lib/scrape-http.js | mitigate | Credentials read only from process.env (OXYLABS_USERNAME/PASSWORD), passed via Authorization header to fetchViaOxylabs; never written to logs (existing logs print url/status/reason only). Verify no credential is interpolated into any log string in the edited fallbackViaOxylabs. |
| T-15-05 | Denial of Service (cost) | unbounded paid Oxylabs calls | mitigate | cachedFetch enforces MAX_OXY_CALLS via persisted _spend.json, counting BEFORE issue; CeilingError drains workers before the cap. This is the cost-DoS guard (CONFIG-03). |
| T-15-06 | Denial of Service | retry storm on transient 613 | mitigate | Sleep-before-retry (3s) prevents tight-loop hammering of the Oxylabs API on 613; retry count stays at one (no unbounded escalation). |
| T-15-07 | Spoofing/Tampering | direct-curl response substituted for forced-Oxylabs | mitigate | assertOxyUsed() throws if directSuccessCount>0 while live fetches occurred; load-time env-guard forces SCRAPE_FORCE_OXYLABS before any fetch. |
</threat_model>

<verification>
- sold-transport load-time guard fires without SCRAPE_FORCE_OXYLABS, loads with it set
- lib/scrape-http.js still exports getWithRetry/extractNextData/getOxylabsStats/resetOxylabsStats
- No Oxylabs credential value appears in any log line: `grep -n "OXYLABS_USERNAME\|OXYLABS_PASSWORD" lib/scrape-http.js` shows only env reads / Authorization construction, never inside a log() call
- `grep -c "require('./scrape-http')" lib/sold-transport.js` returns 1 (no HTTP duplication)
</verification>

<success_criteria>
lib/sold-transport.js reuses lib/scrape-http.js transport, enforces the file-based MAX_OXY_CALLS ceiling with CeilingError, and the force-Oxylabs invariant holds at load time. lib/scrape-http.js retries transient 613-class Oxylabs failures with a 3s backoff on the main fetch path while keeping its public API and retry count unchanged (CONFIG-03 satisfied).
</success_criteria>

<output>
After completion, create `.planning/phases/15-sold-data-ingestion-library/15-02-SUMMARY.md`
</output>
